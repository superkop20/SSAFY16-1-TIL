# 알고리즘 Stack 2-2

## 부분집합
부분집합 : 어떤 집합의 공집합과 자기 자신을 포함한 모든 부분집합  
Backtracking 기법으로 powerset만들기
- 일반적인 백트래킹 : 부분집합, 순열, 조합 등
- N개의 원소를 가진 집합의 $2^n$개의 부분집합을 표현하는 배열
- 여기서 배열의 i번째 항목은 i번째의 원소가 부분집합의 값인지 아닌지를 나타내는 값

파워셋(Powerset) = 멱집합 - 모든 부분집합의 집합

## 순열 1
단순하게 순열을 생성하는 방법
- 동일한숫자가 포함되지 않았을 때, 각 자리수 별로 loop을 이용해 표현할 수 있다.

백트래킹 사용 방법
- 부분집합 구하는 방법과 유사한 접근
- 순열에 사용되지 않은 수 조회
- 순차적으로 순회하며 원소 추가하여 배열 내의 원소들을 통한 순열 생성

## 가지치기
부분집합의 합
- 집합의 원소에 대해 각 부분집합에서의 포함 여부를 트리로 표현
- i원소의 포함 여부를 결정하면 i까지의 부분집합의 합 $S_i$를 결정할 수 있음
- $S_{i-1}$이 찾고자 하는 부분집합의 합보다 크면 남은 원소를 고려할 필요가 없음

## 순열 2
A[1,2,3]의 모든 원소를 사용한 순열
- 3개의 칸에 넣을 수 있는 수를 나열

1. 자리교환으로 순열 생성
2. 백트래킹

## 분할정복
분할정복 알고리즘 유래
- 나폴레옹 전략
- 중앙부로 들어가 연합군을 둘로 나누고 한 부분씩 격파

설계 전략
- 분할 : 해결할문제를 여러개 작은 부분으로 나눈다
- 정복 : 나눈 작은 문제를 각각 해결
- 통합 : 해결된 해답을 모은다

거듭제곱(Exponentiation)
- O(n)

분할정복 기반의 알고리즘 : O($log_2 n$)
