# 알고리즘 START

## SW 문제해결
SW문제해결 역량이란
- 프로그램을 하기 위한 많은 제약 조건과 요구 사항을 이해하고 최선의 방법을 찾아내는 능력
- 프로그래머가 사용하는 언어나 라이브러리, 자료구조, 알고리즘에 대한 지식을 적재적소에 퍼즐을 배치하듯 이들을 연결하여 큰 그림을 만드는 능력
- 문제해결 역량은 추상적
  - 향상시키기 위해서 훈련이 필요

문제해결 과정
1. 문제 읽고 이해
2. 익숙한 용어로 재정의
3. 해결계획 수립
4. 계획 검증
5. 프로그램 구현
6. 문제 복기 및 개선사항 판단

## 복잡도 분석
알고리즘의 효율
- 공간적 효율성과 시간적 효율성
  - 공간 : 필요한 메모리 공간
  - 시간 : 작업완료 소요시간
- 효율성을 뒤집으면 복잡도, 복잡도 높으면 효율성 하락
- 시간복잡도 = 기본연산 수행횟수 + 입력받는 데이터를 종합적으로 고려해서 계산하는 접근적 표기법

시간복잡도
1. 최고차항
2. 상수계수 - 무시(극한 개념) 

## 표준 입출력 방법
sys import, open 활용한 input.txt 받아오는 방법 복기

## 진법
2, 8, 10, 16진수
- 10진수 : 사람이 사용하는 0~9
- 2진수 : 컴퓨터 체계 수 0,1  
수기로 10진수 변환 실습

16진수 : 0~9, A,B,C,D,E,F
- 2진수를 더 가독성있게 사용한 방식
- 8진수로 표현하면 bit 3개

## 비트 연산
비트와 바이트
- 1bit : 0과 1을 표현하는 정보의 단위
- 1byte : 8-bit를 묶어 1 byte라고 함(메모리 주소가 부여되는 단위)

비트 연산
- 컴퓨터의 CPU 내부적으로 비트 연산을 사용하여 덧셈, 뺄셈, 곱셈등을 계산
- 사칙연산이 아닌 비트 연산을 이해하기 위한 과정
  - print(13&9)  # empersand => and
  - print(13|9)  # vertical var => or
  - print(13^9)  # caret => xor 두개를 비교(2진수 값)해서 1이거나 0이거나 확인 - 같으면 0 반환 다를때만 1
  - print(10<<2)  # 피연산자의 비트 열을 왼쪽으로 이동,
    - 레프트 연산자 -> $2^N$ 만큼 곱한 값 반환 10 * $2^2$ = 40
  - print(10>>2)  # 피연산자의 비트열을 오른쪽으로 이동
    - 라이트 연산자 -> $2^N$를 나눈 '몫' 반환 10 // $2^2$ = 2  
5 & (1<<0) : & 결과값이 0이라면 N번째 비트 값은 0이된다  

음수 표현방법
- 컴퓨터는 음수를 2의보수로 관리 맨앞자리 bit(MSB - most significat beat)는 음수/양수를 구분하는 비트 
  - 보수 : 어떤 기준숫자가 되기 위해서 부족한 만큼이 얼만큼인지? 를 뜻하는 수
- 컴퓨터가 2의 보수를 사용하여 음수를 관리하는 이유
  - 뺄셈의 연산속도를 올릴 수 있으며, +0과 -0을 따로 취급하지 않기 위해 사용  
- 1의 보수 - 모든 비트가 1이 되기 위해서 부족한 부분? 
  - 2진수에서 1의 보수 = 비트 뒤집기
  - 2의 보수 : 1의 보수에 1을 더하면 2의 보수
- 음수 표현 : 8비트 기준 MSB 뒤집어서 1되면 음수, 0이면 양수 
  - 나머지 7비트에서 수를 뒤집고 1을 더함(1의자리) 

비트와이스 NOT 연산자 - ~ (tilde) 사용  
- ~4 -> -5 출력 (뒤집어서 1더했기 떄문) , ~-4+1 = 4출력

## 실수
컴퓨터는 이진수를 사용하기 때문에 실수 표현 시 부동소수점 오류 가능
- 파이썬은 반올림을 가까운 짝수 쪽으로 한다

====
# 9.18 오프라인강의
*graph_dfs_contents 파일 참조*

## 그래프
위 아래가 존재하지 않고, 자유로운 연결관계 보유한 자료구조  
핵심 용어
- 정점 (Vertex or Node): 데이터를 나타내는 지점
- 간선 (Edge or Link): 정점 간의 연결 관계 
- 인접 (Adjacency): 두 정점이 간선으로 직접 연결된 상태 
- 차수 (Degree): 하나의 정점에 연결된 간선의 수
  - 유향 그래프에서는 진입 차수(In-degree)와 진출 차수(Out-degree)로 구분
- 경로 (Path): 한 정점에서 다른 정점으로 간선을 따라 이동할 수 있는 길
- 연결 요소 (Connected Component): 서로 연결된 정점들의 한 덩어리
- 사이클 (Cycle): 한 정점에서 출발하여 다시 자기 자신으로 돌아오는 경로

그래프의 종류
1. 무향그래프 (Undirected) : 간선에 방향이 없어 양방향 이동 가능
2. 유향그래프 (Directed) : 간선에 방향이 있어 한쪽으로만 이동 가능
3. 가중치 그래프 (Weighted) : 간선마다 비용(가중치)이 존재
4. 비순환 유향그래프 (DAG) : 방향이 있으면서 사이클은 없는 그래프
5. 트리 

## DFS
- 깊이 우선 탐색(DFS)은 시작 노드에서 출발해 갈 수 있는 경로를 한 방향으로 끝까지 탐색한 뒤, 더 이상 진행할 수 없으면 직전에 방문했던 노드로 되돌아가 다른 경로를 다시 탐색하는 방식
- 이 ‘되돌아가는’(Backtracking) 과정은 마지막에 추가된 노드를 먼저 방문해야 하므로, 자료구조 중 스택(Stack)의 후입선출(LIFO) 특성과 잘 맞습니다.
- 실제로 재귀 함수를 사용하는 DFS 구현에서는 컴퓨터 내부 호출 스택(Call Stack)을 활용해 동일한 동작을 수행

언제 무엇을 사용할까?
- 재귀 방식: 대부분의 경우, 코드가 **훨씬 간결하고 직관적**이므로 추천됩니다. DFS의 원리를 가장 잘 표현하는 방식입니다.
- 스택 방식: 탐색해야 할 그래프의 깊이가 매우 깊어 **스택 오버플로우**가 우려될 때, 또는 스택에 저장되는 데이터를 직접 제어해야 하는 특별한 경우에 사용합니다.

