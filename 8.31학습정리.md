# SW문제해결 기본 - List 1

## 자료구조와 알고리즘
학습목표
- 논리적 사고력 향상
- 문제해결 능력 향상

학습도구
- python(pypy) + pycharm(디버거 - IDE) + SWEA + AI

다른사람의 코드를 눈으로 보는것 뿐만 아니라 따라 치면서 직접 입력해보아라  

## 알고리즘
알고리즘의 성능 : 정확성 - 얼마나 정확하게 동작하는가 - TOP Target  

시간 복잡도 표시
- 빅오 표기법
- 시간 복잡도 함수중에서 가장 큰 영향력을 주는 n에대한 항만을 표시
- 계수는 생략하여 표시 Ex : an + b 에서 계수 a 제거
  * n이 커질수록 어려운 문제 - 항별로 시간복잡도 추세함수 형태 다르다

### 배열
프로그램 내에서 여러 개의 변수가 필요할 때 사용
- 일일이 다른 변수명을 이용하는 것은 비효율 적일 수 있다.
- 배열은 그림을 그려가면서 푸는 습관을 들여라

arr = [0] * 10 : 초기화 10번 수행
어떻게 저장하지? 입력을 어떻게하지? - 문제 풀이의 첫 초식  

최댓값 인덱스 : 만약 여러개의 동일한 값이 있다면? 
- 가장 왼쪽의 최댓값 인덱스

### 정렬
2개이상의 자료를 키(특정기준)에 의해 작은값부터 큰 값(오름차순), 혹은 그 반대의 순서대로(내림차순) 재배열하는 알고리즘  

버블정렬 : 인접한 두 개의 원소를 비교하며 자리를 계속 교환하는 방식 **핵심은 '인접', '교환'**
- 2개씩 큰 수 비교 반복 더 큰수를 오른쪽으로 밀기
- 1회 반복하면 구간 1칸 줄어듦 (N-1 항 삭제 - 인덱스상 N개면 0~N-1)
  * for i : N-1 => 1
    * for j : 0 -> i-1