# 오전 자기주도학습

## 실습 복습, 및 요약정리
AI 요약 결과(실습 흐름):  
- [1단계: ML 기초 & EDA]
  - 수학적 원리(손실함수·정규방정식) ➔ 데이터 탐색(EDA) ➔ 데이터 누수 방지 전처리 ➔ 분류/검증  
                          ▼
- [2단계: 딥러닝 파운데이션 (PyTorch & MLP)]
  - 텐서/Autograd(수동 미분) ➔ 모듈화(nn.Module) ➔ 배치 파이프라인(DataLoader) ➔ 표준 학습 루프  
                          ▼
- [3단계: 모달리티별 심화 (NLP & CV)]
  - • NLP: 토큰화(WordPiece) ➔ 임베딩 ➔ RNN/LSTM(병목 경험) ➔ Attention(문맥 참조) ➔ Transformers
  - • CV: CNN 특징 추출 ➔ Linear Probing(동결) ➔ Data Augmentation & Fine-tuning ➔ ViT  
                          ▼
- [4단계: 생성형 AI & 멀티모달]
  - Diffusion(SANA) 이미지 생성 ➔ CLIP 멀티모달 유사도 평가 ➔ 합성 데이터셋 구축 및 전이학습  
                          ▼
- [5단계: LLM 엔지니어링 & RAG 에이전트]
  - 프롬프팅(Zero/Few-shot/CoT) ➔ LLM as a Judge 품질 검증 ➔ 임베딩/청킹/Vector DB ➔ LangGraph RAG

## 과제복습, 요약정리
- [1단계: ML 기초 & EDA]
  - 수학적 원리(손실함수·정규식) ➔ 데이터 탐색(EDA) ➔ 데이터 누수 방지 전처리 ➔ 분류/검증   
                          ▼
- [2단계: 딥러닝 파운데이션 (PyTorch & MLP)]
  - 텐서/Autograd(수동 미분) ➔ 모듈화(nn.Module) ➔ 배치 파이프라인(DataLoader) ➔ 표준 학습 루프  
                          ▼
- [3단계: 모달리티별 심화 (NLP & CV)]
  - • P: 토큰화(WordPiece) ➔ 임베딩 ➔ RNN/LSTM(병목 경험) ➔ Attention(문맥 참조) ➔ Transformers
  - • CV: CNN 특징 추출 ➔ Linear Probing(동결) ➔ Data Augmentation & Fine-tuning ➔ ViT  
                          ▼
- [4단계: 생성형 AI & 멀티모달]
  - Diffusion(SANA) 이미지 생성 ➔ CLIP 멀티모달 유사도 평가 ➔ 합성 데이터셋 구축 및 전이학습  
                          ▼
- [5단계: LLM 엔지니어링 & RAG 에이전트]
  - 프롬프팅(Zero/Few-shot/CoT) ➔ LLM as a Judge 품질 검증 ➔ 임베딩/청킹/Vector DB ➔ LangGraph RAG  

====
# 파인튜닝 및 AI모델 활용
컴퓨터는 숫자를 어떻게 이해할까?

## 리소스 효율적 AI 모델
효율적 연산과 AI에 대한 이해와 설명 가능성 학습

### 컴퓨터의 수 체계
수 체계
- 십진수(decimal), 이진수(binary) 등

2진수의 표현 길이를 줄이기 위해 16진수$2^4$ 표현법 사용  

1자리를 차지하는 값 - Bit  
8자리 - Byte , 4자리 - nibble
- 8비트 1, 16진수로 표현  
Most Significant Bit(MSB) : 이진수 가장 왼쪽 비트, 가장 큰 자리값  
Least Significant Bit(LSB) : 가장 오른쪽 비트, 가장 작은 자리값  

N자리의 이진수, 십진수
- 가능한 표현의 수 ${x^N}-1$ 개 의 수 표현 가능  

Carry-out 시그널 : 튀어나온 수{맨 앞자릿수} - 버린다 (4bit 이진수 표현)  

1-1. 부호있는 수 체계  
- 부호-크기 표현방식(SMR) : MSB 부호비트 0이면 양수, 1이면 음수 표현
- 2의 보수 표현 방식() : 음수 표현, 해당 수 절댓값 이진수 사용, 모든 비트를 반전한 뒤 1을 더하는 방식    
부호 크기 표현방식 한계:
  - 기존 덧셈 문제 발생 -> 0이 나오는 연산에서 문제 발생 가능
2의 보수 표현방식 
  - 기존 덧셈 정상적 동작  

비트 폭 확장 : N-bit 수를 M-bit로 확장 (M>N) Ex 4->8
- 부호확장: 최상위 비트를 복사해 상위 비트를 채움(값 일정)
- 제로확장: 상위 비트를 모두 0으로 채움(패턴 일정)  

이진수 체계 발전
- int 자료형 기존 최소 2B 에서 사용
- 4B 자료값을 쓰는 long 자료형과 겹치는 문제 발생
- windows 환경 가급적 long 자료형 사용 '지양'

고정 소수점 수 체계
- 이진 소수점 위치 실제 저장하지 않는다.
- 덧셈과 뺄셈 동일하게 동작  
- 곱셈, 나눗셈 scailing factor 체크!({HW-hardware}는 정수입력이라 가정하고 동작하기에 사용자의 의도 중요)
  - 소수계산 float 복잡도 및 연산시간 단축 위해 사용하는 방법

### 부동소수점 수 체계
고정 소수점표현 사용 시 너무 많은 비트 필요로 함  
지수활용/ 큰, 작은 수 더 적은 문자로 표현 -> 유효숫자의 개념 도입  
부동소수점
- 지수를 활용하여 수 표기
  - 가수 x 밑 ^ 지수로 나누어 수 표기  
- 소수점이 고정되지 않고 둥둥 떠다닌다고 해석

가수는 고정 소수점 체계를 사용해야함 -> 정규화  
유효숫자 처리방법 : 반올림, 올림, 버림, 절삭
- 오류 최소화하기 위한 다양한 rounding 기법 존재

지수적 표현을 이진 수 체계에 어떻게 접목  
- 부호 S, 1bit
- 지수 E, $e$ bit
- 부호없는 가수 M, $s$ bit
$$
F = (-1)^S \cdot M \cdot \beta^E
$$  
* 부동소수점은 유일한 표현이 아님에 주의 : 가수의 비트 수 한계로 같은 표현을 무한이 만들 수 없음
* 일반적으로 0으로 시작하는 패턴을 피하고자 함(유효숫자 최대한 확보) -> 정규화
  * 두 부동소수점 수 비교 간단화
* 정규화된 표현은 0을 포함하지 않음
  * 0을 표현하기 위한 특수패턴 정의, 가장 일반적인 표현 : M=0
* 가장 일반적으로 사용되는/기록되는 지수표현 -> 
$$
E = E^{\text{true}} + \text{bias}
$$
  * bias : 미리 정해진 상수
  * $E^{\text{true}}$ : 실제 지수(2의 보수표현)  *52p 한번더 듣기*
* 바이어스 지수 장점
  * 지수를 0/양수영역 이동시켜 기록된 지수를 부호-없는 수 표현으로 해석하게 만듬
* 정규화된 가수와 바이어스 지수
  * S,E,M 표현 방식의 대소 비교가 부호-없는 정수 표현의 경우와 동일해짐
* 부동소수점 표현범위
  * 0이 표현 범위 내에 포함되어 있지 않음

부동소수점 표준(IEEE 54 Standard) - 가장 널리 사용되는 이진 부동소수점 표준  
IEEE Single Precision 포맷
- 주요 특징
  - Base-2
  - Hidden Bit 사용 : 가수 정규화, 맨 앞의 1을 $f$에 저장하지 않음
  - 8-bit 지수에 bias를 127로 사용(256가지 지수 표현 중 특수 패턴 정의)

### 연산과 복잡도
고정 소수점 수 체계의 연산
- 이진수 체계 덧셈, 뺄셈, 곱셈, 나눗셈
  - 덧셈 시 상한초과 상황 -> 자리올림 출력 신호 유/무 확인(4bit 부호 없는 정수의 덧셈)
- 곱셈 : 기존 10진수 체계의 곱셈과 동일
- 나눗셈 : 10진수와 과정 동일 -> 몫과 나머지의 출력 동일(재귀적인 특징이 있음)
  - 보다 빠른 곱셈과, 나눗셈 구현을 위한 노력 지속중
- 일반적으로 연산속도 관점에서 덧셈~뺄셈 << 곱셈 << 나눗셈 - (오른쪽으로 갈수록 더 느림)
  - 연산 규모에 비해 곱셈과 나눗셈의 연산 속도는 차이가 매우크다!

가수의 덧셈/뺄셈 전에 지수를 동일하게 만들어야함 - 연산속도가 느려지는 원인  
정규화된 부동소수점 수 체계의 덧셈/뺄셈 - 생각보다 복잡
부동소수점 수 체계의 곱셉
- 부호 : 부호가 같으면 + 다르면 -
- 지수 : 두 지수를 더함
  - 바이어스 지수 사용하고있다면 지수덧셈이후 bias 값을 한번 빼야함
- 가수 : 두 가수를 고정소수점 곱셈처럼 연산(지수연산과 병렬처리 가능)

부동소수점 수 체계의 나눗셈
- 제수가 0인경우
  - 우선적 상한초과 처리, 몫은 무한대
- 피제수와 제수가 모두 0인경우 
  - 표준 방식 일 때 NaN 처리
- 나머지 계산 : 대부분 하드웨어 수준에서 제공되지 않는다

부동소수점 연산의 복잡도
- 일반적으로 연산속도 관점에서 곱셈<덧셈~뺄셈<<나눗셈 (나눗셈은 구현 방식에 따라 복잡도 다를 수 있음)

## 모델 경량화와 효율적 파인튜닝

### 모델 경량화의 필요성
AI/ML 과포화 시대  
문제점:
- 모델 크기 문제 : 모델 자체가 너무 커짐
- 컴퓨팅 리소스 문제: DeepLearning에 의해 모델이 성장
- 학습 비용 : All you need is money - 모델 1회 훈련당 천만달러 정도 소요

모델 경량화(Model Compression)
- 선택이 아닌 필수(특히 서비스/추론 영역)
  - 일반적으로 경량화 정도가 좋아지면 모델 성능이 떨어짐 + 어떤 성능을 개선할 것인가?(모델크기, 속도, 전력)
- GPU와 궁합이 좋은 기법 vs 궁합이 좋지 않은 기법(전용 가속기로 효과 극대화 필요)

### Quantization/Pruning/Distillation
#### 양자화 
- 연산/메몸리 부하를 줄이는 가장 직관적인 방법
  - Clipping range에 따른 Scaling factor 정하는 것 첫단계

QAT vs PTQ
- Quantization-aware training
- 학습할때부터 줄어든 비트를 적용할 것인가 - 최초에 있는 비트에서 모델 사이즈 점점 줄여나가는 기본 구조
  - 학습단계에서 양자화 함께 수행(학습데이터 필요) 
  - 동일 accuracy 기준 압축률 높일 수 있음 - 동일하게 압축한 경우 Accuracy 높다 but, 학습복잡도 상승(retrain/fine-tuning 접근)
  - CNN 등 오리지널 학습복잡도/데이터가 크지 않은 경우 쉽게 적용 가능
- Post-training Quiantization
  - 사전학습된 모델 바탕 양자화 수행 -> training 할때 양자화를 고려해서 학습
  - 적절한 calibration 데이터를 사용하여 경량화 효율을 높일 수 있음

모델 사이즈에 얼마나 집중할 것인가?
- Weight-activation vs Weight-only
- Dequantization 과정을 얼마나 허락할 것인가?
  - 연산 효율 기준 W8/A8 Quant 유리 but 모델 축소에서는 비효율(상황마다 선택의 폭이 다르다)
  
Symmetric(대칭) vs Asymmetric(비대칭)   
- Activation 및 weight의 분포를 어디까지 반영할 것인가?
- 하드웨어 연산기 수준의 지원은?
  - 영점고정 vs 데이터에 맞춘 영점 이동

Integer vs Floating-point
  - Floating-point quantiation 방식? : AI/ML 연산을 위해 고려된 다양한 FP표현
  - 구글 BF16 : 유효 숫자만 7로 자름
  - FP 32 , TF32 (range 선택)  -> A100 GPU로부터 사용 시작
  - 8비트 : exponent에 부여하는 숫자 선택

Mixed-precision quantization
- Layer 별로 최적화된 양자화 적용(하드웨어 지원이 필요할 수 있음)

#### 가지치기(Pruning)
- 불필요한 weight 제거
  - 저장해야하는 weight 적어지면서 모델 크기 감소
  - 살아남은 weight 위치 기록 위한 추가정보 필요(indexing data, meta data 등)
  - quantization이 더이상 되지 않는 상황에서도 pruning이 가능한 경우가 많음
    - 4비트 이후 양자화가 거의 불가능 할때 가지치기를 해도 accuracy 유지될 수 있다
  - but, GPU상에서 속도 이득을 보기에 어려운 경우 많음  
양자화 - 사용하는 비트 수를 줄이겠다.  
가지치기 - 사용하지 않는 비트는 삭제

Unstructured vs Structured
- 추론 효율과 accuracy 방어 사이의 선택
- Channel/layer 수준의 극단적인 structured pruning 활용 가능
  - Unstructured 방식 - 정확도 강점 but hardware 성능이나 자원 필요
  - 2:4 pruning 시 transformer 이후 복잡도 폭발 - CNN 수준은 가능, LLM 수준 불가능

Indexing data format
- 가장 일반적으로 쓰는 CSR(compressed sparse row) -> but, pruning ratio가 극단적으로 높아야 GPU 가속
  - cuBLAS : Dense matrix에서 사용하는 CUDA 가속 라이브러리(밀집)
  - cuSPARSE : SPARSE matrix 사용 (희소)
- 보통 극단적인 상황에서 예시 (HW 성능이 매우 떨어지나 모델이 큰경우 / 매우 작은 device에 넣을 모델을 생성할 때 축소를 위한 작업)

가능 여부
- 양자화와 비슷한 양상 : 학습데이터, 작은 모델등과 LLM의 경우 순서, 적용에서 차이
- CNN기반 이미지 처리는 80% 이상도 가지치기 가능(1~2% 정확도 drop)
- LLM 계열 50% 내외 수준
- GPU 호환 여부 확인 , 전용가속기 필요성

Pruning의 효과
- CNN : 2배정도 가속할 수 있다 - 정확도 감소 감수할 수 있을 때
- LLM : perplexity 상승, accuracy 하락

#### 지식 증류
작은 모델을 만드는효과적 방법
- 매우 큰 모델에서 출발(Teacher)
- 어떻게 학생을 훈련시키는가
  - 두가지 loss 정의 : Total loss = student loss(truth - student) + Distillation loss(teacher - student)
- 조교 활용(Assistant model) : 학생과 선생 모델 간극 좁히기

LLM Distillation
- 상대적으로 작은 LLM 학습에서 기존 LLM 활용
- 학습에 사용하는 데이터가 너무 크면 여전히 문제
  - 합성데이터 활용 -> 학생모델 fine-tuning

### LoRA/QLoRA
#### 파인튜닝
- 모델 경량화 관점
  - 원본 모델 크기 줄이기
- 모델 서비스 관점(특히 LLM)
  - 일반적인 능력은 강한 원본모델
  - 보다 특정영역/도메인에 특화시키기

다양한 튜닝 방법론
- In-context 학습
- Full fine tuning : 특정 도메인의 데이터를 사용하여 원본 모델의 모든 weight를 fine tuning

파라미터 효율적 파인 튜닝(Parameter Efficient Fine Tuning, PEFT)
- LLMM을 도메인 특화 시키는 과정의 복잡도 개선
  - 원본을 전체 튜닝하지 않고도, 일부만 특화시켜서 바꿔서 튜닝
  - 그 일부의 weight를 어떻게 정하고 얼만큼으로 지정할 것인지 선택하는 문제
- Adapter layer
  - 입력원본 모델 내부에 새로운 layer 추가(adapter layer, MLP형태)
  - 특화데이터 활용해 adapater layer만 학습
  - Full tuning 보다는 효율적이지만, 상당히 많은 추가 weight 필요, 대신 높은 표현력 제공
  - 원본모델의 구조가 바뀌는 것도 감수해야함
- prompt tuning(임베딩 layer 수준의 변동)
  - 입력 임베딩 앞/중간에 학습가능한 가짜 토큰 추가
  - 아주 적은 추가 weight로 입력을 조정하여 특정 동작 유도
  - 매우 가볍고 배포가 쉬움, but 표현력의 한계로 제한적 성능 발휘
- LoRA(Low-Rank Adaptation)
  - 특정 weight matrix에 low-rank update 추가
  - 원본 weight 고정, 아주 조금 lowrank 추가하여 학습, 전문가로의 튜닝 가능
  - 높은 표현력, adapter layer보다 훨씬 적은 추가 weight(핵심내용 - essense 데이터)
- QLoRA(Quantized Low-Rank Adaptation)
  - 경량화(quantization) + LoRA
  - 원본모델을 강하게 양자화 -> 4비트 근사치로 압축
    - low-rank weight는 16비트, 32비트의 높은 해상도 유지하면서 학습
    - 재학습 할 때 비트 수는 늘리되 특화가 아닌 두루두루 새롭게 학습
  - 전체 모델의 복잡도를 대폭 줄이면서 목표로 하는 task에 대해서는 적어도 높은 성능을 달성할 수 있음
  - 범용적으로 성능을 발휘하거나, 특화할 수도 있음 -> 원본을 극단적으로 줄여도 재학습을 하면 성능을 거의 유사하게 발휘할 수 있음(Fine tuning 개념 접목)
- 실습 : Lora, QLora 활용
  - 최신 파인튜닝 라이브러리 활용, 요구되는 파라미터 이해



