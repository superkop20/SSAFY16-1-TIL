# OOP 2 & Exception

## 상속
한 클래스의 속성과 메서드를 다른 클래스가 물려 받는 것
클래스 간의 상하관계가 형성(부모 - 자식)
- 상속을 통해 기존 클래스 속성과 메서드 재사용, 기능 확장
- 클래스간의 계층구조 형성, 관계표현 및 구체적 클래스 생성
- 유지보수 용이 : 특정 부분만 수정 - 수정범위 최소화
Ex  게임 캐릭터 클래스 하위 마법사, 전사 클래스 생성
* 상속 - 하위클래스(상위클래스)로 지정

### 클래스 상속
상속을 통해 상위클래스 공통 메서드를 하위클래스에서도 사용

### 메서드 오버라이딩
부모클래스의 메서드를 같은이름, 같은 파라미터 구조로 재정의
* 파라미터 구조 : 함수(x,y) -> x,y의 구조와 형태가 동일
오버로딩 : 함수(x) -> (x,y,z) 

### 다중 상속
둘 이상의 상위 클래스로부터 여러 행동이나 특징을 상속받을 수 있음
같은이름의 속성/메서드 겹치면 '순서' 중요(탐색시 종료)
이 탐색 순서를 MRO(Method Resolution Order)(x,y) x먼저 탐색
1. 자식클래스 우선 2. 왼쪽부모 우선 3. 중복방문 방지 특성
- 공통부모는 마지막 탐색

#### MRO(메서드 결정 순서)
파이썬이 메서드를 찾는 순서에 대한 규칙
순서가 복잡해지면 MRO 메서드 호출

### super()함수
MRO에 따라, 현재클래스의 부모클래스의 메서드나 속성에 접근하게 해주는 내장함수
- 단순히 호출 뿐만아니라 다중 상속 시 순서에 따라 메서드 실행 위함
단일 상속 : super init을 통해 겹치는 메서드 호출 뒤 속성 초기화
다중 상속 : super는 MRO 순서상 다음을호출하는 구조
super는 인스턴스 생성과, 호출의 시작점에 따라 달라지는 메서드

## 에러와 예외

### 디버깅
버그 : 소프트웨어 오류 또는 결함, 예상된 동작과 실제동작 사이 불일치
방법 1. print함수 활용, 2. 개발환경 제공기능 활용, 3. python tutor 등

### 에러
프로그램 실행 중에 발생하는 예외 상황
문법에러 VS 예외

### 예외
프로그램 실행 중에 감지되는 에러
- 내장예외 : 예외상황을 나타내는 예외 클래스(예시 - 교재참조)

#### 예외 처리
예외 발생 시 프로그램이 비정상적 종료되지 않고, 적절하게 처리하는 방법
- try-except 구조 : try 코드, except 예외
- 복수 예외 처리 - 여러 종류의 예외에 대응하기
하나의 try 블록에서 여러 예외가 발생할 수 있음 
* except 뒤에 예외를 쓰지 않으면 나머지예외 모두를 통칭(최후방어선)
- else - finally : try구문 전체 형태
else : 발생하지 않을때, finally : 예외무관 항상 실행

#### 예외처리 주의사항
내장 예외의 상속 계층구조 주의 (클래스 -> 상속)
내장예외클래스는 상속 계층구조를 가짐 
except 절로 분기 시 반드시 하위클래스를 먼저확인하도록 작성
- 구체적 -> 범용적

## 참고
- as 키워드
예외객체 : 예외 발생 시 예외 정보 담고있는 객체
except 블록에서 예외객체 받아 상세한 예외정보 활용
- try-except, if-else 서로 다른 역할의 코드이다.
EAFP, LBYL[try-except, if-else]
EAFP : 허락보다 용서가 더 쉽다(예외처리 중심 코드 작성)
LBYL : 도약하기 전에 되돌아보기(값검사 중심 코드 작성)
EAFP - 설계, 프레임워크, 백엔드(예외 예측 어려운 경우)
LBYL - 문제풀이, 알고리즘(예외상황 미리 방지)

### 클래스의 의의
당장 알고리즘에는 클래스가 안 쓰일 수도 있지만
더 복잡한 프로그램 만들 때 객체지향 개념을 통해 효율적 코드관리에 도움
클래스는 백엔드, 큰 프로젝트 담당 시 OOP개념을 통해 접목 시킬 수 있다.

### 월말평가 예시문제
혼자서 최대 2시간 정도 고민해보고 답을 도움받고
아예 지우고 빈칸에다가 같은 문제를 여러번 풀어서 구조에 대한 이해
안내된 양식에 맞춰서 풀 수 있도록 구조적인 연습
과목평가 - 재평가 기회 1회 부여
월말평가 - 재평가 없음
예시문제 우선 - > 온라인 실습실 - > 교재 연습문제 순 다양하게 풀이