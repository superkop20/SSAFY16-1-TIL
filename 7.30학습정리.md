# Web
www - world wide web
Web site - 여러개의 웹페이지로 구성된 것
web page - 웹사이트를 구성하는 하나의 요소
- HTML, CSS, Javascript

## 웹 구조화
HTML : 웹페이지의 의미와 구조를 정의하는 언어
* 비선형성, 상호연결성, 사용자 주도적 탐색
Markup language : 태그등(각주)을 이용하여 문서나 데이터의 구조를 명시하는 언어 (데이터의 구조와 의미를 정의하는데 집중)

## Structure of HTML
<html> </html> 전체 페이지의 콘텐츠를 포함
닫는 태그가 없다 = 내용물이 없다(자체의 기능)
head : 메타데이터(설명, 설정등 컴퓨터 식별) * 비가시성
title : 즐겨찾기 시 표시되는 제목
body : 문서의 내용, 페이지 표시 모든 콘텐츠
html element : 여는태그, 닫는태그, 내용으로 구성
html attribute(속성) : 사용자기준에 맞도록 요소 설정, 다양한 방식으로 요소의 동작을 조절하기 위한 값
목적 : 추가적인 기능, 내용 담고싶을 때
CSS 스타일 적용위해 해당요소 선택하기위한 값
작성규칙 : 
1. 요소이름 - 속성 사이 공백
2. 하나 이상의 속성, 속성 사이 공백 구분
3. 속성 값은 큰따옴표로 감싼다

* open in browser : Alt + B로 브라우저 열기
주석 - ctrl + / 
이미지 태그 : 닫는 태그 없어도 됨

### 대표적인 HTML text structure
Heading &paragraph : h1~6, p
lists : ol, ul, li
Emphasis & importance : em, strong

## CSS(Cascading Style Sheet)
웹페이지의 디자인과 레이아웃을 구성하는 언어
스타일 적용 우선순위 : 인라인 > 내부 > 외부
인라인 : HTML 요소 안에 style 속성값으로 작성(지양)
내부 : head 태그 안에 style 태그에 작성
외부 : 별도 CSS 파일 생성 후 HTML link 태그를 사용해 불러오기

### CSS 구문
선택자 : 누구를 꾸밀지 지정하는 부분
선언 : 어떻게 꾸밀지에 대한 구체적인 한줄 명령
';'(세미콜론) 선언 종료를 의미하는 부호
속성 : 바꾸고 싶은 스타일의 종류
값 : Valis

#### CSS 선택자
종류 : 기본 선택자 / 결합자
전체선택자 (*) :  html 모든 요소
요소 선택자 : 지정한 모든 요소
**클래스 선택자 '.'(dot) 주어진 클래스 속성을 가진 모든 요소를 선택**
아이디 선택자 (#) : 주어진 아이디 속성을 가진 요소 선택(고유)
속성 선택자 ([]) : 주어진 속성이나 속성값을 가진 모든 요소 선택

#### CSS 결합자
자식 결합자 (>) : 첫번째 요소의 직계 자식만 선택
자손 결합자 (공백) : 첫번째 요소의 자손요소들 선택

### 명시도
요소에 적용할 CSS 선언을 결정하기 위한 알고리즘
css : 마지막에 나오는 선언이 사용됨 (동일한 가중치일 때)
importance > Inline 스타일 > 선택자(id-class-요소) > 코드 순서

### 선언 : 값과 단위
CSS 선언 : 선택된 요소에 적용할 스타일을 구체적으로 명시하는 부분
속성 : 스타일링하고싶은 기능, 특성(미리 정해준 키워드 사용)
값 : 속성에 적용될 구체적인 설정
값의 단위 : 절대 단위 / 상대 단위
절대단위 "px" 픽셀기준 절대단위, 직관성/예측가능성
상대단위 "em" 부모요소 size기준 크기결정, 유연성/중첩 문제
-> "rem" 최상위요소 html 기준 크기결정(root em)
상대단위 "%" : 기준 부모요소 대비, 속성별 기준점 다름
68p 단위비교 정리

### CSS Box Model
웹 페이지의 모든 HTML 요소를 감싸는 사각형 상자 모델
내용, 안쪽여백, 테두리, 외부간격으로 구성되어 요소의 크기와 배치 결정
margin top - 위에서 픽셀부여(아래이동){상하좌우 확인}
- shorthand 속성(단축속성) : border, margin, padding

Css - content box를 크기의 기본값으로 인식
border box로 설정 바꾸고 보통 시작

=====================
### 참고

CSS 상속 : 상속되는 속성 vs 상속되지 않는 속성
명시도 계산기 : 무엇이 이기는지

#### HTML 스타일가이드
html 태그 소문자 사용 강력 권장
태그명 속성명 모두 소문자
속성값 ""(큰따옴표) 사용 일반적

연속된 공백을 하나로 처리, Enter키로 줄바꿈 해도 인식 X
*에러 출력 없음*
- 코드구조와 포맷팅 
일관 들여쓰기(2칸), 선택자.속성 새 줄 작성, 각 요소한줄 하나 작성
중첩된 요소 한단계 더 들여쓰기, 중괄호 앞 공백
속성 뒤 콜론과 공백, 마지막 속성 뒤 세미콜론
- 선택자 사용
class 선택자를 우선적으로 사용, id/요소선택자 회피

속성과 값 : 소문자 작성, 0값 단위 X
명명규칙 : 의미있고 목적 나타내는이름 사용
케밥 케이스(kebab-case) 사용
약어보다는 전체 단어 사용
CSS 적용 스타일 : 인라인스타일 사용 지양

#### MDN(Mozilla Developer Network)
웹개발자와 디자이너를 위한 종합적인 참고자료
정확성 및 신뢰성, 최신 웹 기술, 명확한 설명과 예제


**Emeet 엔진 활용 타이핑 편리화**
===================================

# CSS Layout

## display 속성
박스타입 종류 : block타입, Inline 타입
Block - 하나의 독립된 덩어리처럼 동작
줄바꿈 발생
(inline 방향 = 좌우, block 방향 = 상하)
대표 div 태그
Inline - 문장안의 단어처럼 흐름에 따라 자연스럽게 배치되는 요소
줄바꿈 발생하지 않음(크기만큼만 영역 차지)
상하좌우 속성 사용 X
대표 span 태그

### Normal flow 
일반적인 흐름 또는 레이아웃을 변경하지 않은 경우 웹 페이지 요소가
배치되는 방식 block, inline 사례

### 기타 display 속성
inline-block 타입 : 줄바꿈없이 크기지정 가능 * 보충
none :요소를 화면에 표시하지않고 공간 부여 X
반응형/JS 활용 시 사용
flex 타입

## CSS position
CSS layout : 위치, 크기 조정하여 디자인 결정
CSS position : 요소를 normal flow 에서 제거하여 다른위치로 배치
- 다른 요소 위에 올리기, 화면 특정위치에 고정시키기 등
네가지 방향속성을 이용해 위치 조절, 겹치는 요소 중첩 순서 조절 가능
유형 5가지
1. static : 기본형(normal flow)
2. relative : 원래위치(static)을 기준으로 이동(방향속성)
다른요소 레이아웃에 영향을 주지 않음
3. absolute : 요소를 기본형에서 제거(테트리스), 요소 차지하는 공간이 없어짐
가장 가까운 relative 부모요소 기준 이동
앱솔루트 추가 전 relative 위치 설정
4. fixed : 요소 제거, 현재 화면영역(viewport)기준으로 이동
스크롤 해도 고정.
5. sticky : relative와 fixed 결합
스크롤 위치가 임계점 도달 전 relative, 도달 후 fixed 고정

### Z-index
요소의 쌓임 순서를 정의하는 속성(입체감)
정수값을 사용해 z축 순서 지정, 값이 클수록 요소가 위로감

### CSS Flexbox
요소를 행과 열 형태로 배치하는 1차원 레이아웃[큰 틀 짜는 형식]
flex container, flex item의 2가지 요소 구성
main(중심) 축, cross(교차) 축 2가지 -> 서로 수직
* main축만 기억하기
메인축 : flex item들 배치되는 기본 축
flex container : 부모요소 -> 1차자식요소 : flex item
flex item : 컨테이너 내부 레이아웃되는 항목

### Flexbox 속성
1. flex container 지정(display 속성 flex 설정)
2. flex-direction : item 나열 방향 지정
3. flex-wrap : 다른행에 배치할지여부 설정(화면 너비 확대, 감소)
4. justify-content : 메인축따라 item 정렬, 간격 조정
5. align-content : 여러줄의 flexitem / 줄들 사이 공간 분배 지정
  - flex 아이템이 두줄 이상일때만 의미
6. align-items : 컨테이너 내부 item들의 교차축 정렬방법 지정
7. align-self : 개별적 정렬 (배치, 공간분배, 정렬로 분류)
8. flex-grow : 남는 행 여백을 비율에 따라 각 flex item에 분배
item이 컨테이너 내 확장하는 비율을 지정 <-> shrink
flex-basis : 초기 크기 값 지정(우선적)

### flex-wrap 응용
반응형 레이아웃 작성 : 다양한 디바이스와 화면 크기에 자동으로적응하여
콘텐츠를 최적으로 표시하는 웹 레이아웃 방식
flex-grow, flex-basis 활용 반응형 레이아웃작성

### 참고
- 마진 상쇄 : 두 block 타입요소의 margin top과 bottom이 만나
더 큰 margin으로 결합되는 현상(일관성, 단순성)
2배줄때 하나로 상쇄한다. (*좌우는 해당 없음)

박스타입별 정렬 - 요소별 block(margin:auto) inline(text-align)
inline-block(text-align)
