# 참 함수와 측정오차
## 참 함수
Feature(입력정보)와 Label(정답, 목표값) 사이의 실제 평균 관계   
머신러닝 알고리즘을 통해 모델이 최대한 가깝게 '근사(approximation)' 하고자 하는 목표
- 직접 관측 불가능, 오차가 포함된 데이터(점) 만 관측 가능
## 측정오차
측정 기기의 한계, 환경적 요인에 의해 데이터에 섞인 오차
- 데이터 = 참 함수 + 오차(f* + E)

독립성: 입력 특성 $X$와 측정오차 $\epsilon$은 서로 독립적이며 상관관계가 없습니다 ($Corr(X, \epsilon) = 0$)  
평균: 측정오차의 기댓값(평균)은 $0$입니다 ($E(\epsilon) = 0$)

# 지도학습의 목적
- 지도학습 : 정답라벨이 있는 훈련 데이터를 사용해 예측 모델을 학습하는 방법
- 목적 : 훈련데이터로 학습한 모델이 새로운데이터에서도 정확한 예측을 수행하도록

훈련이 잘 됐는지 어떻게 평가?
- 학습모델의 성능 평가는 모델이 처음보는 데이터로 평가(일반화를 검증)
- 훈련데이터 성능이 아무리 좋아도 새로운 데이터 성능 떨어지면 실전엔 사용 불가(일반화 오류 최소화 지향)

# 오버피팅과 분포 변화
## 오버피팅
훈련 데이터의 우연한 패턴/잡음까지 외워버려서 훈련에서는 잘 맞지만 테스트에서는 성능이 나빠지는 현상  
- 현상 : 훈련오류 급격히 낮음, 테스트오류 높음/요동
- 표본 의존, 불안정, 일반화 실패
## 분포변화
분포변화로 인한 오류 : 훈련 데이터 분포와 테스트 분포가 다름으로(환경,계절등) 성능이 떨어지는 현상  
분포 변화로 인한 에러 증가는 모델이 과적합하지 않아도 발생 가능

# 결정계수 R^2 (회귀 설명력)
결정계수 : Label(y)의 분산 중에서 Feature로 설명되는 비율  
1에 가까울수록 설명력이 높고, 낮을수록 설명력이 낮음  
모델의 예측값이 실제값과 가까울수록 분자가 작아짐 -> 수식에서 빼는 값이 낮아짐 -> 결정계수 높음
- "평균만 쓰는 단순한 예측"보다 모델이 실제값을 얼마나 더 잘 맞추는지를 0~1 사이 값으로 나타냄
- 모델 성능을 직관적으로 평가할 때, 여러모델 비교, 전반적 성능표현  
- 모델이 형편없어 예측값 오류가 너무 커지면 결정계수 음수 가능  

# p-value 해석
p < 0.05 - 통계적으로 유의미(우연한 발생일 확률이 5% 미만이다)  
p >= 0.05 - 통계적으로 무의미(우연한 발생일 확률이 5% 이상이다)

1. 기울기와 표본오차 계산
2. t-score 계산 t = 기울기 추정치/ 표준오차
  - 우리가 발견한 신호(기울기)가 단순잡음(표준오차) 보다 얼마나 강한가?
  - 기울기가 크고 오차가 작을수록 t점수 커짐
3. 분포곡선에서 꼬리면적(p-value 확인)
- 통계학자들이 만들어 둔 종 모양의 $t$-분포 곡선 위에 계산된 $t$-점수를 올려놓습니다.
- $t$-점수가 양쪽 끝(꼬리)으로 멀리 갈수록(극단적일수록), 그 바깥쪽의 넓이($p$-value)는 점점 좁아져 $0$에 수렴하게 됩니다.
- 이 꼬리 넓이($p$-value)가 0.05 보다 좁아지면 "우연히 나올 수 없는 극단적인 결과 $\rightarrow$ 통계적으로 유의하다"고 결론을 내립니다.  

# 클러스터링과 표준화
## 비지도학습
레이블 없이 데이터의 구조, 패턴, 집단을 찾아내는 학습
- 출력 : 정답예측이 아니라 구조/요약/표현(embedding)
## 클러스터링
데이터 안에서 하위 집단(클러스터)을 찾는 기법들의 총칭  
집단 내부는 서로유사, 집단 간은 상이하도록 데이터 분할  
예시 : 시장 세분화(다수의 지표를 가진 사람들, 특정성 하위집단 식별)  
- 대표 클러스터링 기법 : K-평균(K-means), 계층적 군집  
  - K-means: K(클러스터 수)를 미리 정해 분할
  - 계층적 군집(hierarchical): K를 사전에 고정하지 않음
    * 덴드로그램 :줄기와 잎 그림과 같은 연쇄 그래프 활용해 표현
## 클러스터링 시 주의점
스케일링 : 표준화(평균 0 표준편차 1로 입력변수 변환) 필요
 - 변수 단위 차이에 따른 영향 큼(거리를 재는 작업이므로 feature 별 분포 차이 표준화)

# 로지스틱 회귀의 변환(로짓 변환)
이진 분류 모델에서 함수의 출력 범위가 모든 입력에 대해 0~1 사이의 범위를 가지는 함수 활용
- 시그모이드 함수 : S자 형태의 곡선 함수 오일러수 e의 -z 제곱수로 수식 표현
## 오즈(odds)와 로짓 변환
오즈 : Odds : 성공(y=1) 확률이 실패(y=0) 확률에 비해 몇배 더 높은가를 나타낸다
- 성공확률/실패확률

로짓변환 = Log odds 오즈에 로그를 취한 함수형태
- logit(p) = log (성공확률/실패확률)
- $$\log\left(\frac{P}{1 - P}\right) = \beta_0 + \beta_1 X$$  
  - X가 1단위 변화할때 마다 기울기 만큼 변동

# Deep network
2개의 shallow 네트워크를 하나로 합성  
합성 함수 원리 -> 합성 네트워크 층별 출력을 통해 입력 x -> y -> y' 출력(접기)
### Shallow 네트워크, Deep 네트워크 비교
히든유닛 개수 같을 때 depp네트워크 표현력이 높다  
조각적 선형 구역(꺾이는 점) Deep > Shallow 네트워크 * 중첩되므로 다양  
같은 파라미터 수 사용하더라도 표현의 효율성이 높아서 deep 뉴런 네트워크 활용  

# 경사하강법
손실함수 L[$\phi$]를 최소화하기 위해 파라미터 $\phi$를 반복적으로 갱신  
기울기 계산 : 손실함수 L[$\phi$]를 파라미터 $\phi$에 대해 편미분 진행  
- 파라미터 업데이트 -> 기울기(미분값)의 반대방향으로 이동해야 손실 함수가 줄어듬  
장점 
- 해석적 해(Closed-form Solution)를 구하기 어려운 복잡한 비선형 신경망에서도 손실을 안정적으로 줄여나갈 수 있습니다  
- 아래로 볼록한 볼록(Convex) 문제에서는 전역 최솟값(Global Minimum)에 안정적으로 도달합니다  
단점 : 연산비용 부담, 비볼록 문제 취약성(지역최소값), 학습률 민감성
* 새들점(안장점) - 다변수 함수에서 기울기가 0이지만 극대값도 극소값도 아닌, 말의 안장 모양처럼 방향에 따라 다른 성질을 보이는 특별한 지점  
*기울기가 0 이라고 Loss가 0인것은 아님*
## 확률적 경사 하강법
무작위 확률로 샘플된 일부 데이터(미니배치)만 사용하여 기울기 계산  
확률적 경사 하강법의 특성
* 무작위 샘플데이터를 활용한 미분으로 경로의 무작위성이 있음
* Local 최소점에 빠질 위험이 상대적으로 적음 (국소 최솟값, 노이즈가 있지만 타당한 업데이트, 계산비용 절감, 수렴 특성)

**1강**
============
# LSTM 게이트
기울기 소실 문제를 해결하기 위해, 1997년에 제안된 RNN의 한 종류   
- 특징 : 시점 $t$ 에서 RNN은 길이가 $n$인 벡터 hidden state와 cell state를 가진다
  - $h_t$ 는 단기정보, $C_t$는 장기정보 저장  
Cell state : 정보들을 어떻게 처리할지 의사 결정을 하기 위해 존재
- gating function :sigmoid 구조  
특정 시점에 대해 선택적인 판단이 가능하도록 하는것이 Cell state  
RNN은 단지 맥락과 현재의 단어의 정보들이 합쳐질 때 변환 구도를 어떻게 할 것인가

각 스테이트 마다 3가지게이트 Forget, Input, Output 게이트를 통해 어떤 정보를 지우고,쓰고, 읽을 지 결정
매 시점마다 게이트의 각 요소 : 열림(1), 닫힘(0), 혹은 그 사이의 값으로 설정
## 3가지 Gates
- Forget : 이전 cell state에서 무엇을 버리고 무엇을 유지할 지 결정(0인 부분은 날리고, 1인부분은 남김)
- Input : 새 정보 중 얼마나 cell state에 쓸지 결정
- New cell content : cell에 기록될 새로운 후보
- Output : cell state 중 얼마나 hidden state로 내보낼지 결정

# 언어모델
인간의 두뇌가 자연어를 생성하는 능력을 모방한 모델
- 단어 시퀀스 전체에 확률을 부여하여 문장의 자연스러움 측정  
대표적 언어모델 N-gram 언어모델 * n-gram: 연속된 n개의 단어 묶음
- 다양한 n-gram이 얼마나 자주 등장하는지 통계를 수집하고, 이를 활용해 다음 단어 예측

사용 예시 : SMT(Statistical Machine Translation)  
- 번역 모델 vs 언어 모델
    - 번역 : 단어와 구가 어떻게 번역되어야 하는지 모델링 (말뭉치로 학습)
    - 언어 : 유창한 영어 문장을 쓰는 방법 모델링(영어데이터 학습)
- SMT 한계 :
    - 구조적 복잡성, 많은 수작업, 언어 별 자원 구축 필요 
    - 유지 및 확장 어려움
- 이후 기계번역 연구 -> Nerural Machine Translation(NMT) 로 전환

# seq2seq와 Transformer
NMT : 인공 신경망 활용 번역 -> Seq2seq 발달
## seq2seq
Encoder : 입력문장에 담긴 정보 인코딩  
Decoder : 인코딩된 정보를 조건으로 하여 타겟문장(출력)을 생성 
- 인코더 디코더 하나의 통합 네트워크로 연결
    - 전체 네트워크가 End-to-End로 동시에 최적화

## 모델 학습
- Teacher Forcing : 모델이 스스로 예측한 단어 대신 정답 단어를 디코더 입력으로 강제로 넣어줌, 안정적이고 빠르게 학습
  * 과적합과 같은 오류 발생 가능 - 훈련은 잘되지만 학습상 오류 가능  
토큰 출력 방법
- Greedy Inference : 토큰을 출력하는 방법 중 하나로, 각 단계에서 가장 확률이 높은 단어 선택
  * 되돌리기가 불가능 - 오답 수정 불가(한 가지 후보)
- Beam Search : 매 단계마다 k개 후보 유지 - 후보가 EOS(끝) 도달, 완성된 문장 리스트추가 - EOS 문장 충분히 모이면 탐색종료 - 각 후보들의 점수를 로그 확률의 합으로 구해 최종 선택
  * k가 1이면 inference와 같다

## Transformer
self-attention을 핵심 메커니즘으로 하는 신경망 구조이다.
encoder-decoder 구조로 설계된 신경망 모델 / decoder가 언어 모델과 같은 방식으로 동작
- Multi-Headed Attention
문장에서 같은 단어라도 여러이유로 다른 단어에 주목할 수 있다.
하지만 단일 Self-attention head 로는 한가지 관점에서의 단어 간 관계밖에 파악할 수 없다.
-> 따라서 여러 attention head를 두어 다양한 관점에서 동시에 정보를 파악한다.
Attention Head 1 : 단어의 문맥적 관계에 Attention
               2 : 단어의 시제에 Attention
               3 : 명사에 Attention
관계나 주목하는 방식에 따라 같은 단어 $a$ 값이 달라질 수 있음
- Scaled Dot Product
query와 key의 차원이 커질수록 두 벡터의 내적 값도 자연스럽게 커진다.
값이 너무 크면 softmax 함수가 출력이 지나치게 뾰족해져, gradient vanishing 문제 발생 가능
$$
\text{output}_l = \text{softmax}(X Q_l K_l^T X^T) * X V_l
$$
$$
\text{softmax}([40, 50, 45]) \cong [0.000045, 0.99326, 0.00669]
$$
-> 내적 값을 그대로 사용하지 않고, 나눠주어 스케일 조정 : 
$$
\text{output}_l = \text{softmax}\left( \frac{X Q_l K_l^T X^T}{\sqrt{d/h}} \right) * X V_l
$$
나누는 값 $\sqrt{d/h}$ = 8로 스케일링
$$
\text{softmax}([5.00, 6.25, 5.625]) \cong [0.1573, 0.5489, 0.2938]
$$
값이 안정적으로 분포, 학습 속도 상승 및 안정성 상승
- Residual Connection
깊은 신경망 층이 깊을수록 학습 어려움, layer가 전체를 예측 X. 기존입력과의 차이만 학습
- Layer Normalization
층이 깊어질수록 히든벡터 값 확대/축소 학습 불안정
각 레이어 단위에서 히든벡터 값 정규화
Transformer decoder는 여러개의 디코더 블록 쌓아올려서 만든 구조 : 언어모델처럼 단방향 문맥만 활용
<-> Encoder 양방향 문맥을 모두 활용할 수 있음(입력문장 의미적표현 변환, 각 단어 양방향 문맥 모두 반영한 벡터로 인코딩)
차이점 : self-attention에서 masking 제거
- Encoder-Decoder 트랜스포머 인코더 디코더 : self-attention만 하는 것이 아니라, encoder의 출력 표현을 참조하는 cross-attention 추가
- Cross-Attention : 수행 시 Query - decoder, Key, Value - encoder에서 가져옴
기존 모델에 비해 성능과 학습효율 모두 향상됨

# 어텐션
어텐션은 각 디코더 시점에서, 인코더가 만들어 놓은 여러 정보 중 현재 디코더가 어떤 정보에 얼마나 집중해야 하는지를 가중치로 계산하는 방법입니다.  
고정길이 벡터에 모든 의미 압축 - 정보손실(bottleneck problem)
- seq2seq 한계

Attention 과정 정리
1. Query와 Value 사이 유사도 점수 계산(Dot product, Multiplicative, additive 등)
2. Softmax를 통해 확률분포 얻기
3. 분포를 이용해 values 가중합 -> context vector  

효과(어텐션 메커니즘 장정)
1. NMT 성능 향상
2. Bottleneck problem 해결
3. 기울기 소실 문제 완화 - 멀리 떨어진 단어도 직접 연결
- BLEU(Bilingual Evaluation Understudy)는 기계 번역이나 생성형 AI의 텍스트 생성 품질을 평가하는 대표적인 성능 측정 지표
    - 해석가능성 : 모델의 의사결정 과정 해석 가능
    - 정렬 : 단어와 단어간의 매핑 관계 자연스럽게 학습

# 셀프 어텐션
RNN의 한계점 1. 장기의존성 2. 병렬화 (시퀀스 길이에 따른 문제점)
 
Self-Attention : 문장내 모든 단어가 서로 직접 상호작용하여 1. 장거리 의존성을 효율적으로 포착, 2. 병렬처리를 가능하게 하는 매커니즘
## Self-Attention
Attention 매커니즘을 한 문장 내부에서 적용 : Self-Attention(인코더 내, 디코더 내에서의 어텐션)  
장점
- 순차적으로 처리해야 하는 연산 수가 시퀀스 길이에 따라 증가하지 않는다.
- 최대 상호작용거리 =O(1) 모든 단어가 각 층에서 직접 상호작용(문장 내 완료), 시퀀스에서의 모든 단어를 업데이트 하기위해 동시에 비슷한 맥락으로서 연결  

과정 
1. 각 단어를 Query, Key, Value 벡터로 변환한다.
  * Query : 단어 i가 다른 단어로부터 어떤 정보를 찾을지 정의  
  * Key : 단어 i가 자신이 가진 정보의 특성을 표현
  * Value : 실제로 참조되는 정보 내용을 담음
  * 어텐션 단어 i 표현 Q,V / 셀프어텐션 단어 i표현 Q, K, V
2. Query, Keys 간의 유사도 계산해 softmax 확률분포 계산
3. 각 단어의 출력을 Values의 가중합으로 계산 

self-attention의 한계 
1. 순서정보 부재 : 단어 간 유사도만 계산
2. 비선형성 부족 : 가중평균연산이라는 선형결합에 불과, 복잡한패턴/깊은표현력 담기 어렵다.
3. 미래 참조 문제 : self-attention은 모든 단어를 동시에 보기 때문에, 생성되지 않아야할 미래 단어를 참조
- 한계 해결 1 - Positional Encoding
각 단어위치 i를 나타내는 위치 벡터를 정의해, 단어 임베딩 값에 더해 최종 입력으로 사용한다.
두 가지 방법 : 서로 다른주기 사인/코사인 함수 합성(SPE), 위치벡터를 모두 학습파라미터로 설정해 데이터에 맞춰 최적화(LAPE)
  * 순서 정보의 부재를 해결하기 위함
- 한계 해결 2 - Feed-Forward Network 추가하기
각 단어 출력 벡터에 feed-forward network를 추가
  * 깊이, 비선형성 확장
레이어와 레이어 사이 비선형 함수 추가하여 모든 단어에 대해 사용하여 비선형적인 구조 형성하도록
- 한계 해결 3 - Masked Self-Attention
어텐션 스코어 계산 시 미래 단어 항목 음의 무한으로 설정하여, 계산을 수행할 때 반영되지 않도록 하는 방법
셀프어텐션은 기본적으로 모든단어(미래 포함)를 동시참조하기 때문
  * 미래 참조 문제 해결

# 멀티-헤드 어텐션
Multi-Headed Attention
- 문장에서 같은 단어라도 여러이유로 다른 단어에 주목할 수 있다.
- 하지만 단일 Self-attention head 로는 한가지 관점에서의 단어 간 관계밖에 파악할 수 없다.
> 따라서 여러 attention head를 두어 다양한 관점에서 동시에 정보를 파악한다.  

Attention Head 1 : 단어의 문맥적 관계에 Attention
               2 : 단어의 시제에 Attention
               3 : 명사에 Attention
관계나 주목하는 방식에 따라 같은 단어 $a$ 가중치 값이 달라질 수 있음

# 개방형과 폐쇄형 LLM
텍스트 파운데이션 모델/ LLM
- 기존 대비 1. 더 큰 모델(>7B)이 2. 더 많은 데이터(>1T)에서 학습되어 창발성이 나타나기 시작한 언어모델 
  - 일반적으로 거대 언어모델은 GPT와 같이 다음 토큰 예측을 통해 많은 텍스트 데이터에서 사전 학습된 트랜스포머 기반 모델 의미
  - 기준이 계속 바뀌며 크기가 거대모델의 절대적 기준은 아님
## 폐쇄형
폐쇄형(closed) 거대 언어 모델 : 예시(gpt, claude, gemini) 등 세부정보 공개 X하여 사용되는 모델  
일반적 특징
* GPT : 유저 수 최다, 전반적 성능  
* Claude : 안전 지향, 코딩작업  
* Gemini : 가장 긴 입력출력 지원, 뛰어난 멀티모달  
## 개방형 
개방형(open sourced) 거대 언어 모델 : LLaMA(Meta), Gemma(google), Qwen(Alibaba)  
* 장점 : 1. 무료, 2. 모든 정보(모델 구조, 소스코드) 공개  
* 단점 : 충분한 계산 자원(GPU 등) 필요, 상대적으로 폐쇄형 모델에 비해 성능이 낮음  

# 지시학습
주어진 지시에대해 어떤 응답이 생성되어야 하는지 학습  
- 학습방법 자체는 기존모델(BERT)에서 지도 추가 학습과 동일  
- 기존언어모델은 각 태스크마다 별도의 지도 추가학습 및 모델 저장  

Idea : 모든 자연어 태스크는 텍스트 기반 지시와 응답으로 표현할 수 있지 않을까? (예 : 감정분석, 번역)
학습방법 : 주어진 입력을 받아서 이에 대한 응답을 따라하도록 지도 추가학습  
데이터에 따른게 아니라 '지시'의 형태를 학습하여 방법 자체를 학습하는 과정  
하나의 태스크 데이터를 지시데이터로 바꾸는 방법은 무한하게 가능하다. (정답이 없음)  
다양성 증대를 통해 더 강력한 학습능력을 발휘할 수 있음  
-> 실험결과 제로샷 형태에서도(프롬프트만 작성) 학습데이터 향상  
- 성능향상 핵심요소
1. 학습 태스크의 개수 : 다양한 종류의 지시 학습할수록 성능 향상
2. 추가학습하는 모델의 크기 : 특정규모 이하에서는 효과성 떨어짐
3. 지시를 주는 방법 : 자연어 지시로 사람에게 대화하듯이 가장 효과적

# 선호학습
지시학습의 한계 : 주어진 입력에 대해 적절한 하나의 응답이 있다고 가정, 정해진 답의 경우 자연스러움  
but 정해지지 않은 개방형태스크(번역 등)에서는 한계 존재 -> 더 좋은 응답 생성 추구  
선호학습 : 다양한 응답 중 사람이 더 선호하는 응답을 생성하도록 추가학습  
* 다양한 응답은 모델이 생성, 응답간의 선호도는 사람이 제공  

ChatGPT 만드는 핵심 알고리즘. ChatGPT == InstructGPT + chat data  
InstructGPT 핵심 아이디어 : 사람의 피드백을 통한 강화학습(RLHF)
* 사람의 피드백 = 응답에대한 선호도  

step 1. 지시 학습을 통한 텍스트파운데이션모델의 추가학습 : 다양한 지시입력 수집, 정답데이터 생성  
step 2. 사람의 선호 데이터를 수집하여, 보상 모델을 학습 : 선택지는 모델이 생성, 선호도는 사람이 생성  
 - 사람과 일치한 선호도를 출력하도록 모델 지도학습  

step 3. 보상이 높은 응답을 생성하도록 강화 학습을 통해 추가 학습  
핵심 : step1,2 에서 보지 못한 질문에 대해 사람의 추가적인 개입 없이 학습된 모델들을 통해 추가 학습이 진행 

결과 : 유저의 지시를 얼마나 잘 수행하는 지를 사람이 직접 평가
- 단순 프롬프팅이나 지시 학습에 비해 발전된 지시 수행능력  
- 모델 크기가 커질수록 동일한 방법에서도 전반적인 지시수행능력이 꾸준히 향상됨  
얼마나 안전한 응답을 생성하는지 평가
기존대비 해로운응답과 거짓말(Hallucinations) 덜 생성하는 결과 도출 - 안전하다  

# LLM 안전 정렬  **
다음토큰 예측기반 거대 언어 모델의 한계
- 사람의 지시에 대해 올바르지 않은 응답을 생성하거나, 유해한 응답을 생성할 수도 있음
  - 탈옥, 환각 등 문제점 발생 가능  
LLM 안전 정렬은 대규모 언어 모델의 출력이 유해하지 않고 인간의 가치관과 윤리적 기준에 부합하도록 모델을 조정하는 과정

주요 개념 : 유해성 방지, 인간가치 부합, RLHF, 레드팀 훈련  
주요 한계 : 피상적 정렬, 과잉 거부, 부작용-다른 가치관 왜곡

정렬학습 : LLM의 출력이 사용자의 의도와 가치를 반영하도록 하는 것(사후학습 과정 - 사람에 가깝도록)
- 지시학습: 지시에 대해 어떤 응답 생성
- 선호학습: 상대적으로 어떤 응답을 더 선호해야할지

# RLHF 학습 절차
InstructGPT 핵심 아이디어 : 사람의 피드백을 통한 강화학습(RLHF)  
'Reinforcement Learning from Human Feedback'
1. Step 0 사전학습
2. step 1(SFT). 지시 학습을 통한 텍스트파운데이션모델의 추가학습
 - 실제 유저로부터 다양한 지시 입력을 수집하고, 해당 입력에 대해 훈련된 사람 주석자들이 정답 데이터 생성
3. step 2(RM). 사람의 선호 데이터를 수집하여, 보상 모델(Reward Model)을 학습
 - 선택지는 모델이 생성, 선택지에 대한 선호도는 사람이 생성  
 - 사람과 일치한 선호도를 출력하도록 모델 지도학습  
4. step 3(PPO). 보상이 높은 응답을 생성하도록 강화 학습을 통해 추가 학습  
 - 핵심 : step1,2 에서 보지 못한 질문에 대해 사람의 추가적인 개입 없이 학습된 모델들을 통해 추가 학습이 진행 
 - 지시 학습된 모델을 보상 모델 기반 강화 학습을 통해 한번 더 추가 학습
* 사람의 피드백 = 응답에대한 선호도  

# Top-K, Top-P 샘플링
거대언어모델의 자동회귀 생성  
* 학습이 완료된 거대 언어 모델은 어떻게 응답을 생성할까? -> 순차적 추론을 통한 "토큰별 생성" 

디코딩 알고리즘 - 여러개 존재, 조절하는 여러개의 argument가 있다. 실제로는 널리 쓰이는 하나의 사례에 집중
1. Top-k Sampling : 확률이 높은 K개 토큰들 중에서만 랜덤하게 확률에 따라 샘플링
2. Top-P Sampling(or Nucleus Sampling) : K를 고정하는 대신, 누적확률(P)에 집중하여 K를 자동으로 조절

# CNN 공간 구조 == 모델구조?
기본구조 : 합성곱레이어 - 풀링층 - 평탄화층 - 완전연결층

합성곱 레이어  
* 입력 이미지를 필터와 연산하여 특징 맵(feature map)을 뽑아내는 모듈
* 1차원 구조로 변환하는 FCN과 달리 3차원 구조를 그대로 보존하면서 연산
* Convolution : 필터를 이미지 상에서 이동시키면서 내적을 반복수행, 내적으로 구한 모든 값을 출력 제공
    - CNN 이전부터 존재하던 수학적인 연산

중첩과 수용 영역(Receptice Field)
* CNN 레이어는 이미지 작은 부분인 '지역 정보'추출에 유리하게 설계
* 이미지 활용 다양한 작업에서 전체(맥락) 의미 파악이 필요
  * 지역정보 + 이미지 전체 맥락 이해/활용하기 위한 설계 필요.
* 수용영역이란?
  * CNN이 이미지 처리하면서 한 번에 볼 수 있는 영역의 크기 / 네트워크의 시야
* 고해상도 이미지 처리시, 많은 레이어를 통과해야함.
연산, 비용 학습가능성 고려시 비 실용적
* 해법 : 입력 사이즈를 줄여 모델에 입력

풀링 : 효율적 연산 및 위치 변화의 강건성 확보
* CNN 레이어의 출력을 줄여 연산효율성 확보 - 입력 크기 감소
* 맥스풀링 : 정해진 커널사이즈(2*2)로 이미지를 나누어 각 영역 내 가장 큰 값을 선택하는 연산

합성곱신경망(CNN)의 구성
[합성곱 - 활성화 - 풀링] 반복 -> 평탄화 -> 완전연결층 -> 출력   
특징 추출부 : "무엇이 보이는가"       분류부 : "그래서 무엇인가"  

# CNN출력 크기 계산
통상 여러개의 필터가 있다고 가정, 활성화 맵 - n개  
출력해상도 = 입력해상도 - 필터해상도 + 1  
입출력 해상도를 유지하고 싶다면
* 출력값 중 정의되지 않은 경우, 0 혹은 가장 가까운 출력값으로 대체/참조(패딩) 

# CNN 파라미터 수 계산
모델 상수(파라미터) = 각 레이어 별 계산해 나온 W 가중치 개수  
$W_t$ = $(C_{\text{out}} \times C_{\text{in}} \times K_h \times K_w)$
- 출력채널수 x 입력채널 수 x 필터 가로 x 필터 세로

# CNN과 ViT
전역 관계 학습
- CNN : 지역적 영역에서 수용 영역을 넓혀 전역 이해
- ViT : 수용영역을 넓히지 않아도 전역 맥락 이해 가능

ViT 장점 :
* 전역 문맥을 한번에 고려 가능
* 시계열/시퀀스 데이터의 경우, 순서 고려 가능(위치 인코딩)
- ViT는 거대 데이터 세트 사전학습 시 우수
*ViT 학습 기술 중요*
- 막대한 상수 보유
- 정규화 기법/데이터 증강 기술(좌우 변환, 슬라이싱 등) 매우 중요
  * 약한정도의 증강은 거의 항상 성능 향상에 도움이 된다.

단점 : 
- 대규모 학습 자원 필요
- 학습 데이터 규모가 작을 경우, CNN보다 성능 저하
- 한번에 고려할 수 있는 토큰이 제한적

활용 : 
- 이미지 분류/탐지/분할/생성에서 모두 SoTA 성능 달성 * Sota : state-of-the-Art
- 멀티모달 기준에서 기준 아키텍쳐
- 기준모델에 활용중  

- CNN : 지역적 특성, 장거리 맥락 이해 떨어짐, 데이터순서 반영 X

# ReLU
활성화함수  
$f(x) = max(0,x)$  
장점
- 양의 영역에서 포화 x -> 기울기 소실 문제 크게 줄어듦
- 계산 효율성 높음 
- 학습속도 빠름 -> 시그모이드 tanh대비 6배이상 빠른수렴  

단점
- 죽은 뉴런 문제
- 0을 기준으로 비대칭 -> 왜곡 존재
## Leaky ReLU
$f(x) = max(0.01x,x)$   
장점 : 
- ReLU의 모든 장점 수용  
- 죽지 않는 뉴런(Dead ReLU)

문제점 
- 음수 영역 기울기값 설정의 임의성(여전히 너무작으면 소실, 크면 출력 왜곡 가능)
- 0 중심 대칭이 아님(한쪽 치우치는 경향 발생 가능)
- 항상 ReLU보다 우세하지 않음(일부문제 성능차이 거의 없음)  

# 파인튜닝
파인튜닝(Fine-tuning)이란 방대한 범용 데이터로 사전학습(Pre-training)된 파운데이션 모델의 가중치(Parameters)를 초기값(출발점)으로 삼아, 해결하고자 하는 특정 작업(Downstream Task)이나 도메인의 데이터셋을 활용해 모델의 전체 또는 일부 파라미터를 추가적으로 미세 조정하여 최적화하는 학습 방법론
## Fine-tuning
특징추출부도 포함해서 모델의 전체 파라미터를 우리 데이터셋에 맞춰 다시 학습  
이유 : 우리 데이터만의 특수한 질감이나 형태에 더 예민하게 반응하도록 하기 위해서  
- 큰 조정 : 폭발 가능, 
- 작은 조정 : 조금씩 개선 -> 학습률은 보폭 개념

핵심
1. 사전학습 활용 
 - 일반적인 특징을 이미 학습한 가중치를 초기값으로 사용
2. 다운스트림 태스크(Downstream Task / 특정 도메인) 적응
 - 해결하고자 하는 구체적인 목적(예: 특정 의료 질병 분류, 고객상담 챗봇 등)의 데이터셋에 맞춤화
3. 작은 학습률을 통한 가중치(파라미터) 미세 조정
 - 기존 지식 파괴/폭발을 방지하며 새로운 도메인 특성에 맞추기 위해 작은 학습률로 모델 갱신

# 데이터 증강(Data Augmenation)
학습데이터에 무작위적 변환을 하여 데이터의 다양성을 높이는 기법  
사물의 본질적인 특징 학습 
* 장점 : 
  - 과적합 방지
  - 일반화 성능 향상
  - 부족한 데이터 극복  
* 단점 :
  - 데이터의 본질이 훼손될 수 있다 (Ex: 숫자 9, 6/ 글자 좌우반전)
  - 학습의 난이도가 올라감, 학습시간 늘어남
  
합성과 증강의 차이  
* 합성 : 새로운 데아터 생성(소설 집필)
* 증강 : 기존 데이터를 변형하여 양을 늘림(소설의 문장순서 변경, 단어 교체)
  - 증권사, 헬스케어 등 실제 데이터를 직접적으로 확인하기 어려운 분야에서 유용

# CLIP
CLIP : Contrastive Language-Image Pre-training, by OpenAI  
CLIP 모델 구조 : 언어에 대한 피쳐 출력, 언어와 이미지 유사도 학습    
- AI가 언어와 시각을 통합해서 이해하는 방식을 보여준 패러다임 전환 제시
- 파운데이션 모델로서 특징
  - 입력 : 학습하지 않은 새로운 도메인의 입력데이터 좋은 성능(제로샷 전이)
  - 출력 : 자연어를 이용해 한번도 본 적 없는 카테고리도 텍스트 설명만으로 출력 정의 가능(언어 인터페이스)

대조학습기반의 언어 - 이미지 사전학습  
- 인터넷 데이터를 통한 지도학습을 통해 자연어 기반 시각 개념 학습
- 다양한 이미지 - 자연어 쌍으로 학습(4억개의 텍스트-이미지 쌍)
  - Alt-text(대체 텍스트) : 이미지를 텍스트로 설명해주는 짧은 문구
  - 텍스트 인코더 : Transformer
  - 이미지 인코더 : ViT-B or Resinet-50
## CLIP 구조
CLIP = Encoder Only 구조 사용  
토큰단위 입력 , Attention 매커니즘 구성, L길이 입력토큰 - D차원 특징벡터 배열 형태 입력  
## CLIP 학습
대조학습(Contrastive learning)
- 학습기준
  - 목표 이미지를 대응하는 텍스트와 가깝게
  - 일치하지 않는 여러텍스트와는 멀게
  - 학습목표 : 의미에 따른 거리 감각을 학습학도록 유도

CLIP 학습 - Softmax Recap
- 소프트맥스 입력값 : Logit
- 함수 적용된 확률값 : Probabilites
- T = temperature

# CLIP Zero-shot 분류
모델이 특정 분류 학습을 전혀 거치지 않고도, 자연어 텍스트 프롬프트를 활용해 새로운 이미지 카테고리를 즉시 인식하고 분류하는 기술  
제로샷 이미지 인식기
- 텍스트로 원하는 물체 카테고리 리스트 준비
- 텍스트 기반 카테고리 리스트를 텍스트 임베딩으로 변환하여 Vector DB준비
- 쿼리이미지와 비교해서 가장 높은 점수의 카테고리 반환
## 동작 원리
- 대조학습 : 두 모달리티 같은 벡터공간에 매핑하도록 학습
- 텍스트 프롬프트 생성 : 분류할 클래스 이름을 텍스트 인코더에 입력
- 유사도 계산 : 이미지인코더와 텍스트 인코더가 출력한 벡터간의 코사인 유사도 측정
- 최종 예측 : 가장 높은 유사도 점수 텍스트 레이블 선택
## 장점
- 추가학습 불필요 : 재학습 필요성 X
- 유연성 : 사용자가 원하는 임의의 단어를 라벨로 지정해 즉시 분류 수행
- 광범위한 응용 : 다양한 분야에서 활용 가능 - 이미지 검색, 콘텐츠 필터링 등

# 온디바이스 VLM
VLM : Vision-Language Model  
이미지나 영상 같은 시각 정보와 텍스트(언어)를 동시에 이해하고 처리할 수 있는 인공지능(AI) 기술
- CLIP 기반 VLM 리스트
  - BLIP-2 , InstructBLIP , LLaVA, MiniGPT-4, mPLUG-Owl
- SigLIP 기반 리스트
  - PaLI-X, SmolVLM
## 온디바이스
Small Vision-Language Models(sVLM)  
- OpenVLM - sVLM - SmolVLM(허깅페이스) - Moondream 0.5B  
- Moondream : 모바일 기기나 엣지 디바이스에서의 실시간 실행을 염두에 두고 개발
    - 2억개의 파라미터 구성, 8비트 양자화시 479MB, 실행메모리 996MB 수준으로 작음
    - 4비트 양자화시 다운 375MB 메모리 816까지 감소
        - 양자화 : 모델 파라미터를 정밀도를 낮춰 크기와 메모리를 줄이는 기술
### Gemini Nano
온 디바이스용 경량 Gemini, 스마트폰 등 디바이스 내부에서 직접 실행될 수 있도록 설계  
기타 - 갤럭시 온디바이스 AI, Qwen2.5-VL, Phi-3.5, DeepSeek-VL2, Gemma 3

# Diffusion 이미지 생성
생성 모델 기초 - Denoising Diffusion Probabilistic Models (aka Diffusion model)  
- 메커니즘 : 노이즈로부터 점진적으로 노이즈를 제거하며 이미지 구체화 하는 생성방식
- 학습법 : 합성 노이즈를 입히고 그 노이즈를 예측하는 문제를 반복 학습하게 유도(가우시안 노이즈 - 정규분포)
- 단점 : 느림  

동작 과정
- 순방향 : 이미지 파괴
  - 미세한 노이즈 조금씩 -> 최종적으로 가우시안노이즈(정적) 상태
- 역방향 : 이미지 복원
  - 완전한 노이즈(가우시안)에서 시작
  - 노이즈 살짝 걷어냄
- 횟수 반복 : 너무 적으면 형태 붕괴, 너무 많으면 시간 소요 큼

특징
- 텍스트 변환, 오픈소스, 클라우드 활용
- 안정성, 실패율 낮음

한계 
1. 일관성 부족
2. 문맥 이해 부족
3. 느린 속도 및 비용
## LDM(latent diffusion model)
압축된 저차원의 '잠재공간'에서 노이즈를 제거하는 과정을 거쳐 연산속도와 효율성을 높인 모델
동작 과정
- 인코더 활용 -> 거대한 이미지 잠재벡터로 압축(Convolution)
- 잠재 벡터의 노이즈 섞고 지우는 작업
- 디코더 활용 -> 선명한 이미지로 복원
## Stable Diffusion
LDM + CLIP 구조  
일반 PC에서도 이미지 생성 가능  
현실적 한계
- 추론 속도
- 의미론적 오류
- 구조적 결함
- 높은 하드웨어 성능

