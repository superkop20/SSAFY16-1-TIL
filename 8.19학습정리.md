# 파운데이션 모델

## 파운데이션 모델
과거 백본(척추)모델 -> 현대 파운데이션 모델 로 명명 방향 변동 중  
파운데이션 모델 성능의 원천
- 스케일의 규모
  - 규모의 법칙 : 많이 가르치고, 복잡한 모델일 수록 똑똑함
- 자기지도 학습
  - 스스로 자체 상관관계를 통해 판단하여 학습

## 이미지 파운데이션 모델
### GAN
1세대 - GAN (2014)  
- 적대적 생성 신경망(Generative Adversarial Network)
- 두 개의 신경망을 통해 이미지 생성
  - 생성자:이미지 생성 "위조범"
  - 판별자:진위를 가려냄 "경찰"
- 갈수록 더 정교한 방향으로 학습

GAN 동작 방식  
- 입력 - 생성(벡터기반)
- 특징 : 선명하고 사실적 결과물
- 무작위 노이즈 조금씩 바꾸면 이미지도 바뀜
  - 이미지의 특징을 제어할 수 있는 가능성 열림
- 한계 : 두 모델의 균형을 맞추기가 너무 어려움, 학습이 제대로 안되는 '모드 붕괴'현상 발생
  - 모드붕괴 : 학습할수록 생성자가 속이기 가장 쉬운, 몇가지 이미지만 반복생성
### DIffusion Model
동작 과정
- 순방향 : 이미지 파괴
  - 미세한 노이즈 조금씩 -> 최종적으로 가우시안노이즈(정적) 상태
- 역방향 : 이미지 복원
  - 완전한 노이즈(가우시안)에서 시작
  - 노이즈 살짝 걷어냄
- 횟수 반복 : 너무 적으면 형태 붕괴, 너무 많으면 시간 소요 큼

특징
- 안정성, 실패율 낮음

한계 
1. 일관성 부족
2. 문맥 이해 부족
3. 느린 속도 및 비용
-> 게임 산업에 전반적인 영향과 전환국면으로의 연결 가능성 시사  
### LDM(latent diffusion model)
동작 과정
- 인코더 활용 -> 거대한 이미지 잠재벡터로 압축(Convolution)
- 잠재 벡터의 노이즈 섞고 지우는 작업
- 디코더 활용 -> 선명한 이미지로 복원

## 멀티모달(2021)
서로 다른 형태의 데이터를 함께 이해하고 처리하는 AI 기술

- 과거 : 이미지모델 = 레이블만 학습, 텍스트모델 = 글자만 학습
- 현재 : 이미지, 이미지설명 통째로 학습, 이미지-언어 묶음

CLIP - 멀티모달 시대를 연 주인공 (학습하지 않은데이터에도 좋은 점수 산출)
### CLIP(2021)
- WIT(WebImage Text)라는 4억개의 이미지-텍스트 쌍을 기반으로 학습
- "대조학습" 기반 학습 짖

동작 방식
1. 이미지, 텍스트 인코더 각각 사진/글 벡터변환
2. 이미지/텍스트 벡터를 각각 쌍으로 보며 정답이면 가깝게, 오답이면 멀게 설정(Cosine 유사도)
3. 2번 과정 4억번(데이터 수 만큼) 반복하면 관련내용들끼리 모여있는 거대한 잠재 공간 발생
- 거대 잠재공간 내 이미지 텍스트 쌍을 통해 새로운 문장을 가져와도 각각 개념 조합할 수 있음
### Stable Diffusion
LDM + CLIP 구조  
일반 PC에서도 이미지 생성 가능  
현실적 한계
- 추론 속도
- 의미론적 오류
- 구조적 결함
- 높은 하드웨어 성능
### 지식 증류
방대한 지식을 작고 가벼운 모델에게 효율적으로 전달하는 모델 압축 기술  
하는 이유
- 경량화
- 속도
- 성능 유지
### SANA
- SANA: Efficient High-Resolution Image Synthesis with Linear Diffusion Transformers  
  - 초경량 고해상도 텍스트-이미지 생성 오픈소스 AI 모델  
  - 기존 대비 파라미터 수 20배 적음, 최대 4K이미지 생성 가능  
=============================

*에듀싸피 코딩테스트 합격 후기 게시판 확인*

# 과목평가 수식 출제 가능성 높은 개념
- confusion matrix
- random forest
- F1-score
- 정밀도, 재현율
- 파라미터 수 계산
