# OOP(객체 지향 프로그래밍)

## 절차지향과 객체지향
- 절차 지향 프로그래밍 : 함수와 로직 중심 작성(순차적 처리)
데이터(변수)와 기능(함수)를 별개로 다룸
한계 : 규모가 커질수록 관리 어려움, 코드 수정시 범위파악 어려움

- 객체 지향 프로그래밍 : '하나의 객체'로 묶어서 다룸
틀(클래스) 안에 데이터와 기능을 함께 담는다
데이터를 넘긴다 -> 객체에게 시킨다 라는 사고구조 변경
- 객체지향 : 수동적인 데이터가 능동적인 객체로 변화한 것
* 절차지향 : 객체지향은 대조되는개념이 아님

## 객체와 클래스
객체 : 실존하는 사물 추상화 - 속성과 동작을 가짐
클래스 : 객체를 만들기 위한 설계도, 데이터+기능 묶는법 제공 '틀'
클래스로부터 여러 개의 객체를 쉽게 만들어낼 수 있다
Ex 가수 클래스, 자동차 클래스(유사 속성간의 단위 객체를 포함하는 개념)
객체 특징 : 속성, 메서드, 고유성

### 클래스
init 메서드 = 생성자 메서드, 새로운 객체 만들 떄 필요한 초기값 설정
* 인스턴스 메서드

### 인스턴스('개별 물건')
클래스를 통해 생성된 객체(틀에 맞춘 결과물, 독자적인 데이터)
"~~ 클래스의 인스턴스이다." 
클래스 == 타입 (그동안 사용한 기본 자료형도 클래스이다.)
str, lnt 등 * 교재 37~40페이지 참고

### 클래스 구성 요소
생성자 메서드 : 인스턴스 생성 시 자동 호출(변수 초기화)
인스턴스 변수(속성) : 각 인스턴스 별 고유한 속성, self.변수명 
클래스 변수(속성) : 모든 인스턴스 공유, 클래스내부 직접정의

#### 클래스, 인스턴스변수
동일한 이름으로 인스턴스 변수 생성 시 
클래스변수가 아닌 인스턴스 변수에 먼저 참조
예제 pi 변수 생성 인스턴스 변수에 대입 시 변경 값 출력

### 메서드
클래스 내부 정의 함수 : 객체가 어떻게 동작할지 정의
"이 작업이 무엇에 속한 일인가"를 기준으로 메서드 종류를 고름
- 인스턴스 메서드 : 클래스 내부에 정의되는 메서드의 기본.
반드시 첫번째 인자로 (self)를 받음 - self 고정 강력 권고
인스턴스(문자열, 정수) 를 컨트롤 하기 위해 자기 자신을 첫번쨰 인자로받음
* self가 없으면 인스턴스 구현을 할 수 없음
- 생성자 메서드 : 인스턴스 객체가 생성될 때 자동으로 호출
인스턴스 메서드의 한 종류로서 작동(초기화 설정)

#### 클래스 메서드
@classmethod 데코레이터를 사용해 정의
호출 시, 첫번째 인자로 해당 메서드를 호출하는 클래스가 전달됨
cls 인자를 받아서 클래스 속성 컨트롤에 사용

#### 스태틱(정적) 메서드
클래스, 인스턴스와 상관없이 독립적으로 동작하는 메서드
@staticmethod 데코레이터 사용 정의
자동으로 전달받는 인자가 없음 
* 클래스 : 알고리즘 문제해결에서는 거의 쓰이지 X
주로 현실세계 객체활용으로 구현을 할 수 있는방법으로서 배우는 부분

- 정리 77p
메서드 호출은 서로 교차하여 가능(사용은 정해진 것 만) 클래스/인스턴스
기술적인 가능여부(can)과 논리적인 사용의도(should)를 구분
고민을 통해 설계와 구조 기본원리에대한 의도를 생각해보아라

#### 이름공간(namespace)
클래스 정의, 해당하는 이름공간 생성
인스턴스 생성 - 객체생성 및 독립적인 이름공간 생성
독립성을 통해 충돌이나 영향 방지하며 동작가능 

#### 매직메서드(Double underscore '__')
특수한 동작을 위해 만들어진 메서드(인스턴스 메서드)
특정상황 자동 호출 
__str__ (self) : print에 의해 호출, 객체출력을 문자열로 변경
데코레이터 : 다른함수의 코드를 유지한 채로 수정, 확장위해 사용함수(덧댐)

