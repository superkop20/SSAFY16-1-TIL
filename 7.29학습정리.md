# git workflow

## Feature Branch Workflow
git collaborate 협업구조 시작
repo settings -> 초대자 id 초대
각자의 clone 받아 branch 생성 (git switch -c '브랜치이름')
git push origin '브랜치이름' -> git pull request 생성
PR 충돌여부 확인 후 merge -> master브랜치로 병합완료
* 목적 : master 브랜치 안정성 확보, 여러 개발 충돌없이 진행
시점 : 팀원간 하나의 프로젝트 진행시 표준적인 방식으로 채용

## Forking workflow(clone)
다른 사용자의 원격 저장소를 자신의 계정으로 복사하는것
내 저장소(fork)에서 자유롭게 작업, 괜찮으면 원본에 제안(PR)하기
git clone -> git remote add upstream '원본 repo 주소'
기능 생성 후 내 깃에 push
해당 브랜치 원본저장소 PR -> merge 완료 후 master브랜치 병합
새로운 작업 할때마다 해당 과정 방법
* 목적 : 원본프로젝트 코드, 안정성 최고수준 유지, 외부기여 코드 안전 검토
시점 : 오픈소스 프로젝트, 다수 외부자 참여 시 사용

- github / gitlab 용어차이 -> repo / project, PR / Merge Request

## 파일 시스템
- pathlib : 파일시스템 경로를 객체 지향적으로 다룰 수 있게 해주는
PSL - 파이썬 표준 라이브러리
운영체제별로 경로 문자열이 다르다 -> 문자열이 아닌 객체로 다뤄 차이를 자동처리
- from pathlib import path

encoding - 문자 인코딩 방식 저장(어떤 규칙 사용 문자 저장, 해석 지정)
with 구문 - 블록이 끝나면 파일을 자동으로 닫아줌
'a' = append
file info
리스트 방식 : 전부 출력해서 책상위에 쌓아두는 방식
제너레이터 방식 : 다음사진 요청 올때마다 한장씩 뽑아주는 방식

* : 앞에 어떤 이름이 와도 파일을 찾는다
? : 반드시 해당 문자열이 포함되어야 함

### JSON
import json
JSON : Javascript Object Notation(자바스크립트 객체 표기법)
Key-value 딕셔너리 형태의 문자열 타입을 가짐
사용을 위해 형변환이 필요, 파이썬 json 표준 라이브러리 제공
load - JSON 파일 파이썬 객체 형태로 변환
dump - 다시 JSON 형태로 변환

**VSC 터미널 결과값 길때 Alt + S 누르면 입력창으로 가짐**