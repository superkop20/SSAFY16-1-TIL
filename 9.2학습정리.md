# 알고리즘 List 2

## 2차원 리스트
### 2차원 배열
1차원 리스트를 묶어놓은 리스트 - 괄호가 2중이라서 2차원  
2차원 리스트 선언 : 세로길이, 가로길이 필요  
python 데이터 초기화를 통해 변수선언, 초기화 가능  

배열 순회
- n x m 배열의 n*m 개의 모든 원소를 빠짐없이 조사하는 방법
-  ''   배열의 크기와 저장된 값이 주어질 때 합을 구하는 법
  * S - 열의 합, for문 두개에서 열 순회 for, 행 순회 for 사이에 행의 합 h = 0 정의
  * 정의는 for문 밖에서 미리 내리고 참조 및 값에 대한 동작 시행

지그재그 순회
왼쪽 -> 오른쪽 -< 으로 순회  
나머지를 통해서 두 행의 인덱스 합이 특정 숫자로 같다
- 합을 인덱스 해서 중첩 for문

### 델타
이동할 각 방향의 행과 열의 변화량을 미리 배열로 정의해두고, 반복문을 통해 인접 칸에 접근하는 방식입니다. 이 변화량 배열을 '델타(delta)' 또는 '방향 벡터'라고 부릅니다.   
'방향키를 미리 만들어 둔다'  
델타를 활용한 2차원 배열 탐색
- 2차배열의 한 좌표에서 4방향의 인접 배열 요소를 탐색하는 방법
- 인덱스 칸(i,j) 의 상하 좌우 칸 (ni, nj)
  * i -1, +1 / j -1, +1  방향별로 더할 값 2차원 리스트 di[], dj[]

델타 응용
- N x N 배열에서 각 원소를 중심으로, 상하좌우 k칸의 합계 중 최댓값(k=2)
- ex 파리채 문제처럼 커버 범위 확인
- max_v 지정 후 연산

전치행렬
- 행 - 열 방향 교차

i, j 의 크기에 따라 접근하는 원소 비교
* i < j (우상단) / i > j(좌하단) : for i , for j , if
* i==j(우하향 대각) for i f(arr[i][j])
* N-1-i == j (우상향 대각)

### 부분집합
부분집합의 합
- 유한개의 정수로 이루어진 집합이 있을 때, 이 집합의 부분 집합 중에서 조건에 맞는 경우를 찾는 문제
- 부분집합의 원소를 모두 더한 값이 0이되는 경우가 있는지를 알아내는 문제

부분집합 생성
- 완전검색 기법으로 부분집합 합 문제 풀기
  - 우선 집합의 모든 부분집합을 생성한 후에 각 부분집합의 합을 계산합니다.
- 주어진 집합의 부분집합을 생성하는 방법 생각해보기

부분집합의 수
- 집합의 원소가 n개 일때, 공집합을 포함한 부분집합의 수는 $2^n$개
- 이는 각 원소를 부분집합에 포함/ 미포함 경우의 수 모두 곱한 것과 같다  

부분집합 원소 표현
* arr[i] 원소가 포함되지 않으면 bit[i] == 0
* arr[i] 원소가 포함되면 bit[i]== 1

**SWEA 파리퇴치 문제 풀어보기 !! - 델타 문제풀이 핵심**

# 실습
노션 List 2-1 기본 + 추가문제 싹다 풀어볼 것.  
IM 통과를 위한 핵심 

오늘 푼 것
- 기본 : 색칠하기
- 추가 : 파리퇴치
- 남는시간 도전 : 전기버스
