# 알고리즘 Stack 2-1

## 후위 표기법 변환
문자열로 된 계산식
- Stack을 이용하여 값을 계산할 수 있음
- A+B - 중위표기, a b + - 후위표기

중위표기법의 후위 표기법 변환 방법 1
1. 수식의 각 연산자에 대해 우선순위에 따라 괄호를 사용하여 다시 표현
2. 각 연산자를 그에 대응하는 오른쪽 괄호의 뒤로 이동
3. 괄호 제거
  - ((A*B)-(C/D))
  - ((AB)*(CD)/)-
  - AB*CD/-

변환방법 2 (Stack 이용)
1. 입력받은 중위표기법에서 토큰을 읽는다
2. 토큰이 피연산자면 토큰을 출력
3. 토큰이 연산자(괄호포함)일때
  - 식으로 설명
4. 과정 마지막 : 스택에 남아있는 연산자를 모두 pop하여 출력한다.
 - 스택 밖의 왼쪽 괄호는 우선순위가 가장 높으며, 스택안의 왼쪽 괄호는 우선순위가 가장 낮다.

* isp(in stack precedence) 
  - 스택 안에있는 연산자 우선순위
* icp(in coming precedence)
  - 새로 들어오는(아직 stack에 없는) 연산자의 우선순위

*연산과정 - Stack python 실습파일 참조*

## 후위표기법 연산
후위표기법 식을 stack을 이용하여 계산
1. 피연산자를 만나면 스택에 push한다
2. 연산자를 만나면 필요한 만큼의 피연산자를 스택에서 pop하여 연산하고, 연산 결과를 다시 스택에 push한다
3. 수식이 끝나면 마지막으로 스택을 pop하여 출력한다.

## Stack 응용
### Backtracking
후보해를 구성해 나가다가, 해가 될 수 없다고 판단되면 되돌아가서(Backtrack) 다른 후보를 찾는 방법
- 완전탐색을 효율적으로 구현하는 방법
- 가능성이 있는 해를 추가해가며 완전한 해인지 검사
- 추가한 해가 가능성이 없는 경우 다른 해를 추가할 수 있는 이전 상태로 되돌아 가서 계속 탐색
* 최적화(Optimization) 문제와 결정(decision) 문제에 적용

백트래킹과 DFS(완전탐색)
- DFS는 그래프의 모든 노드 탐색, 백트래킹은 완전탐색 문제에 대한 접근방법
  - 상태공간트리를 DFS로 탐색하는것 - 백트래킹
- Pruning(가지치기)
  - 백트래킹은 후보해가 가능성이 없다면 더 이상 그 경로를 따라가지 않고 중지
- 경우의 수가 많은 문제
  - N! 인 경우의 수를 가진 문제는 모든 경우의 수를 탐색할 수 없습니다.
  - 백트래킹을 적용하면 일반적으로 경우의 수 줄어든다
  - 최악의 경우 지수함수시간(Exponential Time)을 요하므로 처리가 불가능

백트래킹 기법
- 어떤 노드의 유망성을 점검한 후에 유망하지 않다고 결정되면 노드의 부모로되돌아가 다음 자식노드로 이동
- 어떤 노드를 포함한 경로가 해답이 될 수 없으면 유망하지 않다고하며, 반대로 해답가능성이 있으면 유망하다고 함
- 가지치기 : 유망하지 않는 노드가 포함되는 경로는 더 이상 고려하지 않음

백트래킹 진행 절차
- 상태 공간 트리의 DFS 실시
- 각 노드가 유망한가 점검
- 만일 그 노드가 유망하지 않으면 해당 노드의 부모노드로 돌아감
- 돌아간 노드에서 검색을 계속함

**미로찾기**
- 백트래킹으로 시작지점(*)과 목표지점(!) 사이의 경로 찾기
  - 무한루프에 빠지지 않도록 DFS를 기반으로 미로를 탐색합니다.
  - 운이 좋으면 모든 칸을 방문하지 않고 답을 찾을 수 있습니다
  - 최악의 경우 모든 칸을 방문하고 답을 찾게 됩니다.
  - 최단 거리를 찾는 경우 기존 최단 거리보다 더 먼 칸은 유망하지 않으므로 방문할 필요가 없습니다.

미로찾기 알고리즘 1
- Stack에 현재 위치를 push하고 다음 칸으로 이동
- stakc을 이용하여 지나온 경로를 역으로 되돌아감

미로찾기 알고리즘 2
- 현재 위치에서 유망한(갈수있는) 모든 칸을 push하고 하나를 pop해서 이동
 
N-Queen 문제
- NxN 체스판에 N개의 퀸을 놓을 수 있는가? 또는 경우의 수는?
- 상하좌우 방향, 대각선 방향에 다른 퀸이 없어햐 한다
  - DFS , 4개를 무조건 놓아보는 방식
  - 실습 코드 참조

DFS VS 백트래킹
- 순수한 DFS : 155 노드 탐색
- 백트래킹 : 27노드(좀 더 효율적)

====
# 후위표기법 추가강의
1.1 알고리즘 개요
1. 후위 표기법 식을 왼쪽부터 순회
2. 피연산자(숫자 등)를 만나면 스택에 push
3. 연산자를 만나면
   1. 스택에서 피연산자 두 개(또는 필요한 개수)를 pop하고, 
    1.1 먼저 꺼낸 피연산자는 오른쪽에 위치
   2. 해당 연산을 수행한 결과를 다시 스택에 push
4. 식이 끝나면, 스택에 하나 남은 값이 결과 값.

중위 표기법 → 후위 표기법 변환 방법  
알고리즘 개요
1. 중위 표현식을 입력 받으면서 시작
2. 피연산자(숫자, 문자 등)는 바로 출력(후위 표기법 결과에 추가)
3. 왼쪽 괄호(()는 무조건 스택에 push.
4. 오른쪽 괄호())는 왼쪽 괄호를 만나기 전까지 스택에서 pop하여 출력. 
  - 단, 왼쪽 괄호(()도 pop하되 출력은 하지 않음.
5. 연산자(+, -, *, / 등)는 스택의 top의 요소(isp)와 본인(icp)의 우선순위를 비교하여
  - 스택 top의 연산자가 현재 연산자보다 우선순위가 높거나 같으면(isp ≥ icp) 스택에서 pop하여 출력
  - 그렇지 않으면(본인 우선순위가 더 높다면) 스택에 push
6. 모든 입력(중위 표기식)을 처리한 후, 스택에 남은 연산자를 모두 pop하여 출력

연산자 우선순위(숫자가 클수록 높다)  
| 토큰 | isp | icp | 특징 |  
| '(' | 0    | 4   | 스택 안에 있으면 우선순위 가장 낮게, 새로 들어오면 가장 높게 |  
| *, / |2    | 2   |   
|+, - | 1    | 1   |

