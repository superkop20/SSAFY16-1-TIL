# Post-training (Instruction-tuning, RLHF)

## Pre-training vs Post-training
거대 언어 모델 학습  두 가지 단계
- 사전 학습 : 텍스트 데이터를 통해 언어와 지식 학습 (학습목표: 다음단어 예측)
  - 사전학습 후 모델 예시 - 단어 예측 강점, 질문 대답 X  
- 사후 학습 : 사람이 원하는 방식으로 유용하게 만드는 단계
  - 대표 기법 -RLHF, DPO, RLVR 등

## Instruction tuning
사전학습 한계 해결 위해 '파인튜닝' 진행(step1 pretrain -> finetune)  
파인튜닝 : 사전학습된 모델에 특정 작업/도메인에 맞게 추가 학습 과정  
Instruction-tuning : 사람이 내린 지시문(instruction)을 따르도록 학습하는 단계
  - 정답 레이블 요구, 다양한 태스크 적응 중점
  - 중간 과정에서 답변 생성, 태스크 정리 과정, 새로운 태스크에서 평가를 진행

- 더 많은 태스크 가진 데이터 학습
  - 데이터와 모델의 크기가 핵심
  - Super-NaturalInstructions 1.6만개+ 태스크, 3백만개+ 예시 구성 - 모델 어떻게 평가?
- MMLU 벤치마크 : 57개 지식 요구 태스크 언어모델 성능 평가
  - K-MMLU : 한국어에 맞춰서 Korean Knowledge 평가
- MMLU - 지식중심에서 빠르게 눈에띄는 성과 거두고 있음 (시계열 그래프 p.22)

기존 : 명령 이해 어려움, 튜닝 이후 : 명령,지시(instruction)를 이해하고 답변을 출력하는구조로 변경  
알파카 : 대학 연구실에서도 7B급의 모델 생성 - GPT + LLaMA 7B 데이터셋으로 생성된 모델  
- 기존 : Full fine-tuning (전체 데이터 리소스 학습)
- 현대 : PEFT(LoRA, Adapter 등) 일부 데이터만 학습하여 fine tuning
  - LIMA : Less is More for Alignment - 적어도 high quality 중요 (It 강점)
Alignment : 사용자가 원하는/부합되는 상태, 목표로하는 기술들이나 훈련
-> training이 아닌 Alignment 개념상으론 그정도로 많은 양의 훈련이 필요하지 않을 수 있다.

슈퍼바이저 훈련(지도학습) 아무 데이터나 주고 최대한 그 데이터를 흉내/모방하도록 하는 훈련  
-> 선호 비선호가 없음
## Reinforcement Learning from Human Feedback
It 한계
1. 개방형/창의적 생성 태스크 정답 존재 X
2. 언어모델링 모든 토큰레벨 오류 동일시, 어떤 오류는 치명성이 더 크다
   - Loss = -log p(y) 계산을 하다보면 같은 토큰 단위도 값이 다양화 
3. 사람이 만든 답변(정답레이블)이 최적이 아닐 수 있다.
   - 인간의 선호를 만족시켜야함

인간의 선호도(Human preference) 란?  
- 다양한 모델의 답변이 다르게 출력되고 사람마다 원하는 답변의 내용이나 양이 다름

핵심 : Human-in-the-loop ML  
인간의 선호를 반영한 최적화(Optimizing for human preferences)
- 언어 모델 학습 시 : 지시문 x, 응답 y 요약에 대한 human reward 얻을 수 있다면 점수 높을수록 좋음
- 언어모델 응답 -> 기대보상 최대화 목표
세타 파라미터 값을 가지고 $\hat{y}$라는 해를 계속 생성해왔다.   
$$
\mathbb{E}_{\hat{y} \sim p_\theta(y|x)} [R(x, \hat{y})]
$$

RLHF 파이프라인 큰 그림(3 Step)  
- 시범데이터 수집 및 감독학습 -> 비교 데이터 수집 및 보상 모델 학습 -> 강화학습(PPO)으로 정책 최적화
  - 1단계 : Instruction-tuning
  - 2단계 : 사람의 입력에 대한 score를 내는 모델 학습, 사람 선호도와 부합되는 결과값 훈련
  - 3단계 : 응답을 내고 보상을 받는 순환구조에서 보상을 최대화 하도록 추가훈련 혹은 적용(PPO)

인간의 선호도를 어떻게 모델링 할 것인가?   
직접적인 점수 비교는 좋은 방식이 아님(개인편차에 대한 오류 가능성 존재)  
- 응답을 비교하는 방식을 통해 데이터를 수집하여 리워드 모델 학습  
**Ex)**

* **Human preference:** $y_2 > y_1 > y_3$
* **Pairwise Labels:** $y_2 > y_1, \quad y_1 > y_3, \quad y_2 > y_3$

$$
\begin{aligned}
\text{Loss} &= -\sum \log \sigma\left( RM(x, y^+) - RM(x, y^-) \right) \\
&= - \log \sigma\left( RM(x, y_2) - RM(x, y_1) \right) \\
&\quad - \log \sigma\left( RM(x, y_1) - RM(x, y_3) \right) \\
&\quad - \log \sigma\left( RM(x, y_2) - RM(x, y_3) \right)
\end{aligned}
$$
reward model 결과값 : 숫자로서 선호를 표현할 수 있도록 비교결과 도출  

강화학습(최적화)  
- 딥러닝과 별개의 분야, 다양한 분야에서 적용 및 연구 되어왔음(유명한 모델 : 알파고)
- LLM에 적용하기 어려움, 안정적인 학습을 위해 강화학습 적용

RLHF : 더 나은 성능 향상 제공  
0.5 Reference summaries - 평균치 수렴 0.5로 상정
- RLHF 결과 0.5 초과
주의점: 리워드 모델이 잘 작동하는지 확인
- 충분히 많은데이터로 학습한 리워드 모델 : 인간평가수준 근접
Chat GPT : Instruction-tuning + RLHF 
- RLHF의 발전을 토대로 LLM 중 처음으로 상업적 모델로 활용

## What's next?
리워드 모델링 한계점
- 인간의 선호도 일관성 부족
  - 리워드 해킹은 강화학습에서 자주 발생하는 문제
  - 의도치 않게 답변, 결과가 흘러가는 것이 리워드 해킹
- 챗봇들은 정답의 여부와 관계없이 생산적, 도움되어보이는 정답 생성  
- 이러한 결과들은 환각 문제 야기
  - 선호도 학습한 리워드모델 더 일관성 부족


### PPO(Proximal Policy Optimization)
리워드를 최대화 하되, SFT된 모델과 많이 달라지지 않도록 proximal 하게 수렴(페널티(KL Divergence)를 주어
점진적으로)  
Reward model, SFT model, RL model -> 3가지 모델 메모리가 필요함  
- 리소스 부족한 상태에서는 유의미한 훈련 어려움(대형 모델, 인프라 필요) 
### DPO(Direct Preference Optimization) 
별도의 보상 모델(Reward Model) 학습이나 복잡한 강화학습(PPO) 과정 없이, 선호도 데이터셋(선택된 답변과 거부된 답변)을 이용해 언어 모델을 직접 최적화하는 단일 손실 함수  
RLHF 에서 'RL' 제거  
-> DPO Loss로 유도
- PPO의 목적 함수를 수학적으로 변환하여, 선호 답변($y_w$)의 생성 확률은 올리고 비선호 답변($y_l$)의 생성 확률은 낮추는 단일 분류 손실 함수(Cross-Entropy 형태의 Loss)로 유도
- 직접 훈련 해보고 평가를 받는 것 = on-policy training(PPO)  
- 시키는 지시사항/충고를 받아들여서 그대로 훈련 = off-policy training(DPO)
- 보상 모델과 Critic 모델이 필요 없어 메모리 리소스를 대폭 절감할 수 있고 구현이 훨씬 안정적입니다.
### RLVR(Reinforcement Learning with Verifiable Rewards)
답이 분명한 문제들은 정답 여부로 리워드 부여하자  
- 대표적 성공 사례 DeepSeek-R1
Verifiable Reward : 맞히면 1, 아니면 0으로 훈련(명확한 보상 책정)  
- 단순화된 데이터를 통해 수학적인 Reasoning model 
====
# Retrieval-Augmented Language Models(Information Retrieval, RAG)

## Basic COncepts of Retrieval-augmented LM
### Basic Concepts
추론 시 외부 데이터 저장소를 불러와 활용하는 언어 모델  
- 기존 : 질문을 넣고 LM 출력
- R-based : Datastore로부터 추출

Datastore :
- 가공되지 않은 대규모 텍스트 코퍼스
쿼리 : 
- 검색 질의  
인덱스
- 검색 가능 항목들 체계적 정리  
검색  
- 가장 관련성 높은 정보 찾아내는 과정  
RAG 
- 사용자의 질문에 답하기위해 관련 정보를 datastore에서 검색해와서, 언어모델이 생성단계에 함께 활용하는 방법  

### Information Retrieval
정보 검색(IR) : 사용자의 질의에 맞는 정보를 대규모데이터에서 찾아 제공하는 과정
- Ex : 웹 서치, 아이템 서치, 추천 시스템 등  
RAG를 통해 정확하고 최신의 답변 생성  

Retriever 종류
- Sparse Retriever(어휘적 유사도 기반)
  - 전통적 IR 기법, 쿼리(입력)와 문서간의 정확한 용어일치에 기반 
  - Ex : TF-IDF, BM-25
  - 장점: 단순성, 효율성, 투명성
  - 단점: 제한된 의미 이해 - 쿼리와 실제 의미 사용단어 매칭 오류문제
- Dense Retriever(의미적 유사도 기반)
  - 쿼리와 문서를 표현하기 위해 dense vector(임베딩)을 활용해 의미적 유사도 기반
  - DPR, Contriever, Openai-embeddings 등  
  - 임베딩 모델(BERT), Bi-encoder, Cross-encoder
  - 장점 : 의미적 이해(문맥과 의미 포착), 풍부한 쿼리 표현
  - 단점 : 높은 연산 비용, 제한된 투명성, 데이터 및 모델 의존성
어휘적 유사도 수식
$$
\text{w}_{x, y} = \text{tf}_{x, y} \times \log\left(\frac{N}{\text{df}_x}\right)
$$
TF : 단어가 문서에 얼마나 자주 등장하는지, IDF: 단어가 얼마나 드물게 등장하는지  
의미적 유사도
- Bi-enco  두문장 따로 인코딩
  - 매우빠르고 대규모 적합, 정확도 낮음
- Cross-enco 두문장 함께 처리
  - 느리지만, 세밀한 상호작용 포착, 정확도 높음  
서로 상황에 따른 상호보완적인 시스템 구축 필요

### Retrieval-augemnted LM
RAG : 정보검색부터 답변 생성까지의 프레임워크  
Nonparametric Knoledge, parametric Knowledge로 구분된 지식을 잘 통합하여 활용하도록 모델링

Why?
1. 거대 언어모델은 모든 지식을 다 자신의 파라미터에 저장하지 못한다.
 - 사전학습 데이터에 자주나타난 쉬운정보 기억 경향성
 - RAG: 자주 등장하지 않는 정보 큰 효과 - LLM이 잘 아는 정보 부정적인 영향 가능
  - 모델이 파라미터 내에서 모든 정보를 공정하게 저장하지 못한다.
2. 거대언어모델이 보유한 지식은 시대에 금세 뒤쳐짐, 갱신 어려움
 - 현재의 지식편집 메서드 확장성 부족
 - but, 저장소는 업데이트 쉬움, 확장성 만족
3. 거대 언어모델의 답변은 해석과 검증이 어려움
4. 기업 내부정보와 같은 보안정보는 언어모델 학습에 활용되지 않음

RaLM 파이프라인
- 검색증강 언어모델 : 언어모델에 질문과 더불어 검색엔진 결과를 함께 이용
  - 일련의 과정들을 Langchain을 통해 구현 및 실습 가능

고려해야할 이슈, Challange  
1. Context를 어떻게 구성해야 하는가?
  - 언어 모델의 컨텍스트 길이를 늘려야한다.
2. RAG 결과는 검색모델 성능에 의존 -> 검색 노이즈에 취약
  - 정확하지 않은 유사정보가 언어모델이 답하는 것을 방해 가능
  - Context안의 정보 이용하려는 경향성 -> 검색 노이즈가 환각 증가
  - 노이즈와 함께 훈련 -> 고의로 틀린 답과 맥락을 제공하고 수정
  - Noise robustness : 외부문서에 노이즈(관련없는정보)가 포함되어있어도 올바른 답을 찾아내는 능력
  - Negative Rejection : 관련 정보가 없을 때 모델이 답변 거부하도록 훈련
3. LLM의 사전지식과 컨텍스트 간의 충돌 발생
  - Context 위에서 Grounding 학습 강화
  - Context 없을 때, 답변 회피/거절 학습
4. 복잡한 추론 필요 & 문서가 명확한 사실에 대한 오류를 포함할 때
  - Information Integration
    - 외부 문서에 모든 답 포함(중첩 참조 필요) - Agent 시스템으로의 발전(RAG는 단지 파이프라인)
  - Counterfactual Robustness 
    - 반사실적 외부 문서 - 오류에 대한 수정을 할 수 있는 능력

그럼에도 검색엔진 한계와 개선이 필요하다
====
# LLMs with Tool Usage(LLM Agent, Tool Use, MCP)

## Basic Concepts of LLM Agents
### Agent이란?
센서를 통해 환경(real world)을 인지하고, 액추에이터를 통해 환경에 대한 액션을 통해 영향을 미치는 것으로 간주될 수 있는 모든 것  
sensor -> (percepts)지각 -> 행위 결정 하는 것이 Agent

강화학습 - 의사결정의 과학  
LLM Agent?
- LLM을 핵심 구조로 삼아 환경을 이해하고 행동을 수행하는 에이전트
- LLM first view : 기존 LLM을 활용한 시스템을 에이전트로 만든다
- Agent first view : LLM을 통합하여 언어를 활용한 추론/의사소통 가능  
- 무엇이 Agent?
  - 웹 탐색 - Yes
  - OS 의 파일을 검색하고 생성하는 LLM 시스템 - yes
  - 정보 검색, 생성하는 LLM - 아마 No (점진적이지 않음)
  - 복잡한 추론을 하는 O1같은 LLM - No

에이전트 요건
- 도구사용, 추론과계획, 환경표현, 환경이해, 상호작용/의사소통
#### LLM Agent 프레임워크
Action - Planning  
Controller / Tool set / perceiver / Environment  
- 컨트롤러, 퍼시버 - 연결하거나 혼용 가능(역할에 따른 구분적 묘사)  
VLM, LLM : 이제는 의사결정 모델, planning에 집중학습하는것을 선호

### TOOL Usage in LLMs
TOOl : 언어모델 외부에서 실행되는 프로그램에 연결되는 함수 인터페이스
- LLM은 함수 호출과 입력 인자를 생성함으로써 이 도구를 활용 가능  
 - Physical interaction-based tools : agent - realworld
 - GUI based tools : User - tools -> CVA(class variance authority)
 - program-based tools : developer - server

도구 사용 패러다임
- 도구 사용 : 두 모드간의 전환 (training)
  - 텍스트 생성 모드
  - 도구 실행 모드
- 도구 사용 유도 방법(tool learning)
  - 추론 시 프롬프트
  - 학습(training: tool learning)
    * code missing : LLM 내 다국어 사용 시 의미 혼탁 문제

툴 러닝 방식
- 검색엔진 사용하기 위해 인간행동 모방(모방학습 - Imitation Learning)
- 지도학습 + 강화학습, 단 6000개 데이터만 필요
- 가장 간단하고 직관적인 방식
  - 장문 질의응답에서 뛰어남, 때로 인간보다 좋은 성능
- Meta : Toolformer
  - 모델 스스로 학습데이터 생성, 지도학습 모델
  - 검색엔진 뿐만 아니라 여러 API 활용
  - 데이터 생성 방식
    - API 호출 샘플링 : LLM이 기존 데이터셋에서 API호출후보 생성
    - API 실행 : API호출 실제 실행
    - API 호출 필터링 : 문맥에 유용한지 평가 및 필터링(도움되는 정보만 남김) 진행
- ToolLLM 
  - 툴 다양성 범용성 타겟팅한 연구
  - 1.6만개+ API 활용, 대규모 학습 데이터셋 & 특화 언어모델 & 평가 벤치마크
  - 데이터 수집 방법
    - API 수집 -> API기반 명령문(instruction) 생성 -> 어노테이션 & 필터링
- 최근 툴러닝 : 멀티모달 툴 러닝, 강화학습
  - MLLM 기반 도구 정의 및 활용 연구 (GUI 등)
  - 지도학습을 넘어 에이전트 강화학습 도입 연구
    - 학습 훈련 과정에 대한 연구 PRM(process reward model)

### Model Context Protocol(MCP)
Why MCP? 
- 외부 툴 활용 연구 급증하며 회사/모델마다 각기 다른 툴호출 및 스키마 개발
- 문제점
  - 호환성 부족, 재사용 어려움

MCP 
- 언어모델이 외부 툴과 상호작용하기 위한 표준화된 방식으로 정의한 프로토콜
- 각각의 Agent, LLM이 구축하는 calling 에서 JSON 등으로 양식통일로서 표준화
  - 투명성, 호환성 재사용성 등의 강점 확보  
LLM - 지능, APP - SW / 여러 갈래 연결들 : MCP  
권한 부여와 분리 및 구분을 어떻게 할 것인지 정리

현재
- MCP 학계/업계 모두 표준으로 확립
- Web, AI 개발을 구분 양쪽 생태계 모두 표준적/호환 가능한 개발 가능
====
# AI Agents & Langchain(AI Agents, Langchain)
## Environment Representation & Understanding
환경과 에이전트 활용 예시들
- 챗봇, 로보틱스, 임바디드(Embodied) 에이전트, 게임, 소프트웨어 개발

환경의 표현 : 텍스트(text)
- ALFworld 
  - 텍스트 기반 임바디드 에이전트 시뮬레이션 엔진
  - 물리적 세계 정보 텍스트 기반으로 환경을 표현

환경의 표현 : 이미지
- Touchdown
  - Google Street View 기반 내비게이션 및 지시 따르기 데이터 셋
  - 도시환경 이미지 내에서 경로 탐색, 시각적 환경과 텍스트 연결
  - 이미지, 텍스트, 공간감에 대한 지능이 있어서 벡터 표현 가능하도록  
이미지 기반 표현의 문제점
- 에이전트로서 좋은 성능 -> 세부적 이해 필요
  - OCR(광학문자 인식), 웹페이지 광고 삽입 및 이동 등
- 많은 모델 이런 태스크 실패, 일정수준의 학습을 통해 개선 가능

환경의 표현 : 텍스트기반 웹
- WebArena
  - 웹환경을 텍스트로 표현(HTML,DOM,Accessibillity Tree)로 표현해 에이전트 상호작용 가능

복잡한 환경에 대한 이해 : 일부지식은 LLM내에 포함, 다른지식 실시간 상호작용을 통해 발견해야함  
1. 환경 특화 프롬프트
  - 환경에 맞게 수동으로 프롬프트 제작하여 에이전트가 지시 따르도록 유도
  - 문제점 : 일반화 - 새로운환경 성능 저하
2. 비지도 프롬프트 유도 - 메모리 설정
  - 성공적으로 수행된 워크플로우를 기억, 이를기반 새로운 프롬프트 생성하여 제공
  - 환경에서의 상호작용을 메모리에 통합
  - 사람이 수동설계 X, 에이전트가 경험을 통해 자동생성 및 일반화
  - 특정 사람의 경험을 cloning 하여 유사하게 생성 
3. 환경 탐색
  - 모델이 환경탐색할때 보상을 부여하여 학습을 유도 -> 호기심 기반 보상
  - 강화학습에서 예측 불가능한 상태공간 진입할 경우 보상 증가
4. 탐색 기반 궤적 기억(시행착오)
  - BAGEL
    - 명령어를 샘플링하여 실행, 더 정확한 새 명령어로 궤적 재라벨링
    - LM Agent가 환경 탐색, LM-labeler가 이를 바탕으로 지시를 다시 정제
  - 탐색과 자기교정을 통해 데이터 생성
## Reasoning & Planning 
Controller : Planning 종류
- 국소적 계획(Local)
  - 한 단계씩, 매스텝마다 사용할 하나의 툴 결정
  - 단순, 직관적 , 장기 의존성 문제
- 전역적 계획(Global)
  - 전체계획 경로 한번에 생성
  - 여러개 툴 조합 시퀀스 형태 결정
  - 효율적, 복잡한 환경 실패 가능

### Local Planning
ReACT
- 추론과 액션을 결합하여 에이전트가 환경과 상호작용 하도록 하는 방식
- 단순한 추론/행동분리를 넘어, 두과정을 경합해 상황적응형 계획 가능
- 다양한 도구 사용, 웹탐색, 추론기반 Q&A 등 높은 성능
### Global Planning
Plan-and-Solve prompting
- 전체 계획 수립, 순차적 문제 해결
- 단순한 방식보다 체계적, 안정적 추론
- 복잡한 문제해결 오류 축적 감소
### 오류 식별과 회복
에이전트는 에러/실수에서 회복할 방법 필요
- Reflexion
  - 수행한 궤적 평가 후 잘못된 부분을 Reflection 단계에서 분석 및 재시도
  - 스스로 반성 후 개선된 행동 수행
  - 다양한 태스크 성능 회복 및 향상
### 계획 재검토
CoAct
- 에이전트가 실행 도중 계획을 재검토, 수정가능
- 두 개 에이전트 협력 -> 재계획, 피드백 수행  
실행과정에서 초기 불안정 보완가능, 장기작업에서 안정성, 성공률 향상

## Langchain
LLM 기반 애플리케이션을 빠르게 개발할 수 있는 오픈 소스 프레임워크  
특징
- 다양한 LLM provider와 통합, 모델/회사별 차이를 공통 인터페이스로 관리 가능

*RAG w/ Langchain  튜토리얼링크 : https://python.langchain.com/docs/tutorials/rag*
*Agent w/ Langchain : https://python.langchain.com/docs/tutorials/agents/*

