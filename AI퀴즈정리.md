# AI & 기계학습 기초 1

## AI, ML, DL의 정의
AI : 주어진 환경/데이터를 인지,학습,추론을 통해 목표 달성을 하도록 예측, 행동 선택, 계획하는 시스템
ML(머신러닝) : AI범주 내에서 데이터로부터 학습하여 목적을 달성하는 접근방법론
DL(딥러닝) : ML범주 내에서 신경망함수를 사용한 학습방법론

## 데이터와 학습의 이해
머신러닝의 순환 : 반복적 개선 루프
데이터 - 모델 - 학습 - 평가(파이프라인) 반복적 개선 과정

### 데이터 구성요소
머신러닝은 규칙 직접 코딩x, 데이터에서 규칙 학습
데이터의 분포와 관계가 머신러닝의 학습 결과를 결정
Feature(특성) : 모델이 예측에 사용하는 입력정보(예측,판단근거)
Label(목표값) : 모델이 예측하려는 정답(학습 목표값)

### ML 실생활 예시
유튜브 추천 : Feat(영상정보, 사용자정보), Label(사용자 피드백)
스팸메일 분류 : Feat(메일제목,발신자 등), Label(스팸/정상)

## 단일 피쳐 기반 학습
### 1D(dimension) 피쳐 기반 학습
Feature가 하나일 때 머신러닝이 학습하는 가장 단순한 형태
$income_i$ = $f$*(미지의 참 함수 곡선)(Years of education) + $e_i$ 측정오차
데이터셋 D: 30명의 Yoe(피쳐)와 Income(라벨) 쌍 - X, Y 값
학습을 하는 대상이 f*/ 실제 데이터는 점으로 표현 - 측정오차값 필연
관측가능과 관측 불가능의 차이를 명확히 구분
- 어떤 함수가 가장 관계를 잘 나타내는지 학습해야함

### 모델과 가설 공간(순환 2단계)
학습 : 입력(feature) -> 출력(label) 관계를 찾는 과정
평균관계를 하나의 함수로 표현함(가장 잘 나타내는,)
가설공간 : 관계를 표현할 수 있는 모든 후보함수들의 모음(나의 선택)
피쳐, 라벨공간 위에서 정의된 함수들의 집합 *f* 'search 범위 상정'
모델 : 가설공간 *f*에 속한 특정함수 f
* 선형 : 직선 , 비선형 : 곡선 의미
데이터 : 점(분포), 모델 : 함수 f 

### 학습이란
학습 : 주어진 데이터에서 정답을 가장 잘 맞추도록 모델의 규칙을 조정해가는 과정
데이터 D -> 가설공간 *f* -> 선택 모델 f
학습에 필요한 3가지 : 데이터, 가설공간, 선택기준(손실함수)
* 손실함수 : 어떤함수가 더 좋은지 판단하는 척도(숫자값)
- 학습과정 : 선택 - 예측 - 계산 - 조정 - 완성
데이터가 문제제시 - 가설공간 선택지 제공 - 학습이 최적의 함수 선택

## 복수 피쳐 기반 학습
### 2D(dimension) 피쳐 기반 학습
feature(특성) 2개와의 관계를 나타내는 미지의 참 함수
Income = $f$*(x1,x2)+ e(오차)
- surface(면의 형태로 함수 출력) : 관측 불가능 [단면(X), 높낮이(Y)]
실측가능한 데이터(점) 으로 최적 함수 도출
평평한 면 f : 선형, 구부러진 면 f : 비선형
목적 : 미지의 참 함수 찾기(점과 오차가 존재)

### 일반적 용어 정리 및 모델 가정
income - Y로 표기 -> $Y$ = $f$*($X$) + $e$
i번째 feature : Xi 로 표기
일반적인 p차원 피쳐(p개의 피쳐) 벡터 X = X1~Xp 포함관계 Rp(실수 집합)
모델(함수형)-f*(참함수): Rp -> R,  Y = f*(X)+ e
측정오차 e: e값은 피쳐X와 독립(상관관계 0), 평균 = 0

### 왜 f(-)을 학습하는가?
예측 : 잘 학습된 함수가 있으면, 새로운 입력 X에서 Y예측 가능
중요 특성 파악 : 피쳐들 중 어떤 특성 중요하고 어떤 특성 무관 파악
해석 가능성 : f의 복잡도에 따라 어떻게 영향을 미치는지 이해

----------------------------
# AI & 기계학습 기초 2
지도학습 : 처음보는 문제도 잘 푸는 AI 만들기
입력 + 정답을 가지고 예측 규칙을 배우는 방법
목표 : 처음보는 데이터에서도 예측 성능 향상

## 지도학습의 개념
### 지도학습이란?
지도학습 : 정답라벨이 있는 훈련 데이터를 사용해 예측 모델을 학습하는 방법
목표 : 훈련데이터로 학습한 모델이 새로운데이터에서도 정확한 예측
### 지도학습의 종류(회귀 vs 분류)
회귀 : 예측하고 싶은 결과값이 '숫자'일 때 사용(output 기준)
분류 : 예측하고 싶은 결과값이 '범주'일 때 사용(category)
### 손실 함수(Loss Function)
모델의 예측이 정답에서 얼마나벗어났는지 숫자로 표현하는 함수
학습 : 이 숫자가 작아지도록 모델을 조정
회귀 - 평균제곱오차(정답과 차이가 커질수록 손실이 커진다)
분류 - 교차 엔트로피(정답을 확신할수록 0에 가까워짐)
### 지도학습 용어
특성(feature, x) : 예측에 사용되는 설명 변수(예측함수 f의 input)
라벨(label, y) : 맞춰야하는 정답(실제값)
예측값(y hat) : 학습과정에서 모델이 내놓은 결과(숫자/범주)
오류 : 손실함수로 계산 = y hat - y (모델의 영점 오차)
측정오차 : 학습으로 줄이는 것이 아님(노이즈, 자연적 변수 등)
* 변수 위에 ㅅ모양 hat : 예측값 이라는 뜻

## 회귀
### 회귀 문제
회귀 : 입력으로부터 숫자(output)을 얼마나 정확히 예측할까?
특징 : 라벨 및 예측모델의 출력은 연속적
### 회귀 오류 : 평균제곱오차(Mean Squred Error)
예측이 얼마나 정확한지 : 예측정확도를 숫자로 측정하는 지표
MSE(평균제곱오차) : 각 데이터에서 정답과 예측의 평균 제곱 차이 값
RMSE =  $\sqrt{MSE}$
* 식은 분산 - 표준편차 원리와 완전히 같음. 단지 기준값만 예측/평균차이
### 회귀 설명력 : $R^2$ (결정계수)
결정계수 : Label(y)의 분산 중에서 Feature로 설명되는 비율
분자 : 실제값 - 모델 예측값
분모 : 실제값 - 실제값들의 평균값
$R^2$ =  1 - 예측분산/실제분산 *보통 1에 가까워질 수록 좋은 모델
모델이 형편없어 예측값 오류가 너무 커지면 결정계수 음수 가능
### MSE vs R^2
둘 다 모델의 정확도를 평가하기 위한 정량 지표
MSE : 구체적인 오차 크기가 중요할때, 실제 손실 비용 계산
$R^2$ : 모델 성능을 직관적으로 평가할 때, 여러모델 비교, 전반적 성능

## 분류
### 분류 문제
입력특성(feature)으로부터 출력이 범주값으로 나타나는 예측문제
특징 : 범주 라벨(이진(T,F)/다중) 미리 정해진 범주
### 분류 정확도
분류모델 : MSE, $R^2$ 사용 어려움
정확도 : 전체 예측 중에서 올바르게 맞춘 예측의 비율
Accuracy= 올바르게 맞춘 개수 / 전체 예측 개수
지시함수 II : 조건이 참이면 1, 거짓이면 0을 반환하는 함수
II(A) = 1 if A true
- 불균형 데이터의 경우 치우치게 예측해도 정확도가 높아보일 수 있음
결론 : 정확도는 단순히 맞춘 비율, 다른지표도 함께 봐야한다.
### 혼동 행렬
예측과 실제 값 사이의 관계를 행렬 형태로 표현
TP(True Positive) : 실제 양성, 예측 양성
TN(True Negative) : 실제 음성, 예측 음성
FP(False Positive) : 실제 음성, 예측 양성(오탐지)
FN(Fasle Negative) : 실제 양성, 예측 음성(누락) 
정밀도(precision) : 양성이라 판정한 것 중 진짜 양성 비율
재현율(sensitivity) : 진짜 양성 가운데 잡아낸 예측 양성 비율
F1-score : 정밀도와 재현율의 조화평균
F1 = 2 * (정밀도 * 재현율) / (정밀도 + 재현율)
### 분류 손실 : 교차 엔트로피
두 확률 분포 간의 차이를 정량적으로 측정하는 정보 이론의 지표
(정확도, 혼동행렬은 평가지표 - 맞음/틀림만 세므로 예측 확률이 좋아져도 값은 그대로)
학습중 '정답을 얼마나 확신했는지'까지 반영하는 손실 필요
정답에 준 확률이 높을수록 손실이 작아짐
확률이 낮으면 손실이 급격히 커짐(음수 log 원리 : 1보다 작은 값 로그 = 항상 음수, 절댓값을 통한 산출로서 앞에 음수부호 붙임)
교차 엔트로피 : 손실 = -log(정답 범주에 준 확률)
회귀의 MSE에 해당
정확도 정밀도 재현율 = 결과 '평가'지표/교차엔트로피 = 학습에서 줄여나가는 손실

## 학습의 목적
### 학습의 목적
새로운 데이터에서 예측하기
새로운데이터에서도 정확한 예측을 수행
일반화 오류의 최소화를 지향(새로운 데이터에서 틀리는 걸 감소)
### 오버피팅(Overfitting, 과적합)
훈련 데이터의 우연한 패턴/잡음까지 외워버려서 훈련에서는 잘 맞지만 테스트에서는 성능이 나빠지는 현상
현상 : 훈련오류 급격히 낮음, 테스트오류 높음/요동
- 안 좋은 이유
표본 의존, 불안정 : 훈련데이터 - 일부 표본이므로 우연한 잡음이 섞임. 
이것에 과하게 맞추어 학습하면 샘플 몇개만 바뀌어도 예측이 크게 흔들림
일반화 실패 : 보지못한 데이터 오류가 커짐, 모집단 성능 격차 벌어짐
### 오버피팅에 대한 오해
오버피팅 != 분포변화로 인한 에러 증가
분포변화로 인한 오류 : 훈련 데이터 분포와 테스트 분포가 다름으로(환경,계절등) 성능이 떨어지는 현상
분포 변화로 인한 에러 증가는 모델이 과적합하지 않아도 발생 가능
- ex: 훈련데이터 실사진 vs 테스트데이터 애니메이션
분포 변화 : 데이터의 출처나 관측조건이 달라서 데이터의 통계적 특성(분포)이 바뀐 상태
### 언더피팅
오버피팅 vs 언더피팅 의 균형
모델이 단순하거나 학습이 완성되지 않음 -> 중요한 패턴을 놓침
해결 실마리 : 더 많은 데이터, 테스트데이터활용 모델. 교차검증

--------------------------
# AI & 기계학습 기초 3
## 테스트 성능 평가
훈련오류 vs 테스트 오류
훈련오류 : 모델을 학습시킨 같은 데이터에 다시 적용해 계산한 오류
테스트 오류 : 학습에 쓰지 않은 새 관측치에 대해 모델을 적용했을 때의 평균 예측 오류(실제 시행 결과)
훈련오류 = 문제집 점수, 테스트오류 = 시험 점수 -> 다르다
모델 복잡도에따라 훈련오류 계속 감소, 테스트오류 - U자형
목적 : 테스트오류의 U자형의 바닥이 되도록하는 적절한 모델 찾기
적당히 공부해서 점수가 가장 잘 나오는 지점 찾기
테스트예측오류 계산-이상적케이스 : 충분히 큰 별도 테스트 데이터셋(어려움)
대안 : 재표본화를 통한 테스트 오류 추정
데이터를 나눠 여러 번 "훈련-평가"를 반복해 테스트 오류를 가늠
방법 : 검증셋, K겹 교차검증

## 검증셋 접근
### 검증셋 방법
검증셋(홀드아웃) 방법 : 가용샘플들을 무작위로 훈련셋/검증셋으로 분할
훈련셋으로 모델 적합, 검증셋으로 예측 후 검증오류 계산
### 검증셋 절차 
검증절차 : 데이터 순서 무작위 셔플링 후 두 부분으로 분할(훈련/검증)
학습은 훈련셋 , 성능평가는 검증셋에서 수행
### 검증셋 방법 예시
자동차데이터
목표 : 선형(1차)모델부터 고차항(다항식) 모델 비교
전체 데이터를 무작위로 훈련셋/검증셋 분할(무조건 50% 아님)
### 검증셋 접근의 한계
검증셋 방법의 단점 : 표본 분할 결과에 따라 테스트오류 추정치가 매우 가변적
전체데이터 학습때보다 성능이 낮게 추정(테스트오류 과대추정) 가능
학습에 부분데이터만 사용하므로 오류를 과대추정
전체데이터 학습경우보다 예측 성능이 낮을 수밖에 없다.

## K-겹 교차검증(K-fold Cross-Validation)
### K-겹 교차검증
테스트오류 추정의 표준적 접근(문제집 5등분 모의고사 5번 보기)
추정치는 모델 선택과 최종 모델의 테스트오류 규모 파악에 활용
데이터 전체를 크기 동일 K개폴드 무작위 분할
폴드 k -> 1~K 검증, 나머지 K-1개를 훈련에 사용
K번 반복 후 평균오류로 테스트 오류를 추정
데이터를 먼저 셔플링 -> n개의 데이터를 '겹치지않는' K개 그룹으로 분할
그룹별로 번갈아 검증셋, 훈련셋
K개의 MSE 평균해 테스트 오류 추정
### K-겹 교차검증 오류 계산
* 폴드 집합 $C_1, \dots, C_K$, 각 폴드 크기 $n_k$

$$
\text{CV}(K) = \sum_{k=1}^{K} \frac{n_k}{n} \text{MSE}_k
$$

* 여기서, $\text{MSE}_k = \frac{1}{n_k} \sum_{i \in C_k} (y_i - \hat{y}_i)^2$ ($K$-폴드를 검증셋으로 두고 나머지로 적합한 예측 $\hat{y}_i$)
  * $\text{MSE}$(평균제곱오차): 예측이 얼마나 틀렸는지를 숫자로 표현한 값 (오차를 제곱해 평균)

* $K = n$이면 **Leave-One-Out 교차검증(LOOCV)**
  * 100개 문제($n$)로 100개의 서로 다른 모의고사($K$)를 만드는 것.
k번째 조각(n개) 를 넣어 예측값($\hat{y}$) 생성
k번째 조각 평균제곱오차 MSE_K 계산
K개 단계에서 얻은 MSE_K들을 K로나누어평균낸 값 : CV(K)
Leave-One-Out 교차검증에서 검증셋 크기는? 1
### Leave-One-Out 교차검증
K=n이면 검증셋 1개 교차검증 폴드 n개
n번 반복하여 나온 n개의 MSE 평균으로 테스트 오류 추정
### K-겹 교차검증 비교
LOOCV cs 10-겹 CV 비교 
99문제 학습 후 1문제로 평가 *100 vs 90문제학습, 10문제로 평가 * 10
두 방법 경향과 최적차수 비슷 - 적절한 K를 선택하여 교차검증 시행
시뮬레이션 : 참/추정 테스트 MSE
10-겹 CV 추정 : 테스트 성능에 대한 추정의 좋은 대안

------------------------
# AI & 기계학습 기초 3
## 비지도학습
레이블(정답) 없이 데이터의 구조 패턴 집단을 찾아내는 학습
대표과제 : 군집화, 차원축소, 밀도추정/이상치탐지
비지도 vs 지도학습
지도 : 입력+라벨로 예측모델 학습
비지도 : 입력만으로 구조 학습(클러스터링)

## 클러스터링(군집화)
데이터 안에서 하위 집단(클러스터)을 찾는 기법들의 총칭
집단 내부는 서로유사, 집단 간은 상이하도록 데이터 분할
예시 : 시장 세분화(다수의 지표를 가진 사람들, 특정성 하위집단 식별)
- 대표 클러스터링 기법 : K-평균(K-means), 계층적 군집
K-means : K(클러스터 수)를 미리 정해 분할
계층적 군집(hierarchical):K를 사전에 고정하지 않음
* 덴드로그램(dendrogram)
### K-means 클러스터링
클러스터 결과 다른색 = 다른 클러스터에 속해있다.
K-means 표기 : 인덱스 집합 C1~Ck(관측치)
모든 클러스터 합집합 = 전체집합
모두 다른 클러스터에 대해 교집합 = 공집합
하나의 데이터는 하나의 클러스터에만 속한다.
좋은 군집화 = 내부변동이 작은 분할
목표 : 클러스터 거리.차이의 합이 최소가 되도록 분리
* 클러스터 != 분류(지도학습), 비지도학습은 무엇이 정답이다가 없음(분리)
#### K-means 클러스터링
K-means 알고리즘
초기화 : 관측치들에 무작위로 클러스터 임시 부여
반복 : 할당이 더이상 바뀌지 않을 때 까지
1. 각 클러스터의 중심 계산(특성 평균 벡터)
2. 각 관측치를 가장 가까운 중심 클러스터에 재할당
특성 : 평균거리 계속 감소, 결과 더 좋아짐
최적해를 항상 찾는게 X -> 초기값에따라 지역 최솟값으로 수렴 가능
- 지역 최소값(Local minimum) 문제라고 칭함
#### K-means 알고리즘 단계별 진행
무작위 클러스터 초기화 - 중심 계산 - 클러스터 재배정
중심 계산 - 클러스터 재배정 지속 반복
#### K-means 클러스터링에서 초기값의 영향
서로 다른 초기 레이블에서 최종 분할과 
목표값(패널 상단 숫자)이 달라짐
무작위 초기화 = 초기값이 랜덤(여러 번 시도를 권장)
클러스터 내부 변동(within-cluster variation)이 작도록 지속적 시도
### 계층적 군집
#### 계층적 군집
K-means는 클러스터 수 K를 미리 알거나 지정해야한다.
계층적 군집 : K 고정 X, 전체 구조를 덴드로그램으로 제공
상향식 - 데이터 객체(잎) -> 몸통 구조로 합체
예시 : 덴드로그램에서 수평선 높이(거리)를 기준으로 가위질(나누기)
- 사후적으로 분리 개수 정할 수 있음
#### 계층적 군집 알고리즘(상향식)
n개 관측에 대해 쌍별 비(非)유사도 계산, 각 관측치를 하나의 클러스터
i개 클러스터 간 쌍별 비유사도를 모두 계산 가장 유사한 두 클러스터 병합
병합 후 남은 i-1개의 클러스터 사이 새 비유사도 다시 계산
i=1, ~ n에 대해 반복
#### 계층적 군집 단계별 진행
데이터들이 점차 큰 클러스터로 합쳐지는 과정
매 단계 클러스터들 끼리 병합이이루어짐
1개의 단일 클러스터가 될 때까지 진행
클러스터 내부 요소 여러개 일 때 거리 측정
- 우선순위(링크 유형)따라 선정
계층적 군집 계산량 : 모든 클러스터 쌍 간의 거리를 계산해야함
데이터 양 증가 - 계산량 증가 (K-means에 비해 많음)
#### 계층적 군집의 링크 유형
링크(link)의 유형 - 군집간 거리 선정
Single(최소거리) 링크 : 쌍별(pair)거리중 최소값
Complete(최대거리) 링크 : 쌍별(pair)거리중 최소값
Average(평균거리) 링크 : 쌍별(pair)거리중 평균
#### 링크 유형에 따라 계층적 군집 결과
같은 데이터라도 링크선택에 따라 클러스터링 결과가 달라질 수 있음
하나만 하는 것이 아니라 다른 종류 링크도 사용해라
### 클러스터링 시 주의점
체크리스트
스케일링 : 표준화(평균 0, 표준편차 1로 입력변수 변환)가 필요한가? Yes
몇 개의 클러스터가 적합한지는 정답 없음
=================================
# AI 기계학습 방법론 1
## 선형회귀
관계를 직선형태로 근사하여 예측하는 통계적 방법
Ex - 광고데이터 관련 질문

## 단순선형회귀
### 단순선형회귀
단일 설명변수를 이용한 선형회귀
한개의 설명변수(X)와 하나의 반응변수(Y) 사이의 선형 관계 찾는 방법
* hat표기 $\hat{b}$ 는 추정값을 의미함
### 최소제곱법
실제 관측값과 예측값의 차이(잔차)를 제곱해 합한값을 최소화 하는 방법
-> 잔차제곱합을 최소화하는 직선을 찾기 위한 계수 기울기, 절편 추정
기울기 = 0이 되도록 2차항의 잔차제곱이 최솟값
### 단순 선형 회귀 : 광고데이터
TV광고비(X)와 제품판매량(Y)의 선형 관계 예측
단순선형회귀 적용, 각 데이터 잔차제곱을 가장 작게 만드는 직선 선택
베타 hat -> 기울기, 절편에 대한 추정치
계수 해석 : 두 집단 평균의 차이가 표준오차에 비해 얼마나 큰지 나타내는 값(T 점수) 높을수록 통계적 유의
유의성 검정 : 계수의 p-value 계산 (0.05 미만일경우 유의미함)
X - Y 관계가 존재
모형적합도 $R^2$ = 결정계수(1에 가까울수록 가능성 높음)
기울기 계수(coefficient)의 선형성은 값 자체가 아닌 p-value나 t점수에 따라 결정됨

## 다중 선형 회귀
### 다중선형회귀란
- 단순: X - Y 한 쌍, 한 가지 관계만 고려
- 다중: 복수 요인을 함께 고려한 반응변수(Y) 설명
- $p$개의 독립변수: $x_1, x_2, \dots, x_p$
- $\beta_1 \dots \beta_p$: 각 독립변수에 대한 회귀계수(모수). 영향력의 크기와 방향을 의미
- **모형식:** $Y = \beta_0 + \beta_1 x_1 + \dots + \beta_p x_p + \epsilon$
- 해석: 다른 변수를 고정한 채 $x_i$가 1단위 증가할 때 $Y$가 평균적으로 $\beta_i$만큼 변화함
### 다중선형회귀의 추정과 예측
여러변수(X들) 로 반응변수 Y를 동시에 예측하는 모형
- 예측값: $\hat{y} = \hat{\beta}_0 + \hat{\beta}_1 x_1 + \dots + \hat{\beta}_p x_p$
- 추정방법: 실제 값과 예측 값의 차이(잔차)를 제곱해 합한 값(RSS)을 최소화
- RSS가 최소일 때 얻어지는 계수 $\hat{\beta}$가 최적 추정치
### 다중선형회귀 계수추정유도(행렬표현)
계수 추정의 수학적 유도
- 행렬 표현: $\mathbf{y} = \mathbf{X}\boldsymbol{\beta} + \boldsymbol{\epsilon}$
- 최소제곱법: RSS 최소화
- 편미분 값을 통해 기울기 = 0 인 지점에서 $\hat{\boldsymbol{\beta}}$의 해(closed-form) 도출
- 벡터의 $T$ (Transpose): 전치행렬 (가로 세로 전환)
### 다중선형회귀 결과
계수(coefficient) 가 음수냐 양수냐는 무관
t점수와 p-value를 따름
결정계수가 높다 = 모델 설명력이 높다 -> 단순선형회귀

## 선형회귀 주의사항
훈련데이터 성능만으로 확정 어려움
테스트 성능 평가 필요 : 새로운 데이터에 대해 과적합 문제 발생 가능
상관관계 != 인과관계, 선형관계를 보이는 것과 인과관계는 다른개념

# AI 기계학습 방법론 2
* 로지스틱 회귀
## 분류
### 분류란?
분류 : 정해진 범주 중 하나로 지정하는 것
범주형 변수 : 수치의 크고 작음이 아니라 유한한 범주로 표현하는 변수
분류함수 목표 : 분류함수를 학습하여 입력x가 속할 범주 예측
### 예시:신용카드 연체
신용카드 사용량 - 소득 산점도
각각 독립변수, 연체여부 = 종속변수
연체자는 신용카드 사용량이 높은구간 분포
비 연체자는 사용량이 낮은 쪽에 주로 분포 
### 분류문제에 선형회귀를 써도 되나?
선형회귀는 분류문제 사용 부적합 : 이진분류 문제
선형회귀 예측값 제한하게 갖도록 할 수 없다.
선형회귀는 확률이 0 미만이거나 1보다 클 수 있어 확률에 부적합
### 분류 문제에서 적합한 모델
대안 - 로지스틱 회귀
시그모이드 함수를 활용해 예측값 무조건 0~1범위 내 보장
순서가 없는 범주를 확률로 직접 예측하는 적절한 분류 방법

## 로지스틱 회귀
### 로지스틱 회귀 모형식
시그모이드 함수에서 모든 실수 z에 대해 y는 0과 1 사이
z가 양의 무한 - y가 1로수렴, 음의무한 - y가 0으로 수렴
시그모이드 함수에 $z = \beta_0(절편) + \beta_1 x$(기울기) 대입
**모형식:** $P(x) = P(Y=1|X)$ (x가 주어졌을 때 Y가 1일 확률)
P(X;b) 모수벡터표현 ->$\boldsymbol{\beta} = [\beta_0, \beta_1]^T$
결국 시그모이드 함수 절편, 기울기 구하는 과정
### 로지스틱 회귀 vs 선형
로지스틱함수는 선형함수 내포
Odds : 성공(y=1) 확률이 실패(y=0) 확률에 비해 몇배 더 높은가를 나타낸다
-> 성공확률/실패확률
로짓변환(logit) = Log odds 오즈에 로그를 취한 함수형태
$\text{logit}(p) = \log\left(\frac{p}{1-p}\right) = \beta_0 + \beta_1 x$
로지스틱 모형식은 선형모형식 + 시그모이드함수
로짓 변환 시 선형회귀 모형식으로 표현 가능 (X가 1단위 증가할 때 logit(p)가 $\beta_1$만큼 증가)
### MLE 활용 모수 추정
우도(likelihood) : 현재의 확률 분포(모델)가 주어진 데이터를 얼마나 잘 설명하는지 나타내는 지표
모델학습 우도값 최대화 : MLE(maximum likelihood estimation)
우도 최대화(최대한 극단 값에 가까워 지는 부분)
y가 1일때 확률값이 1에 최대한 가까우면 좋겠다
y가 0일때 1-p해서 1에 가깝도록 곱 계산 
확률의 곱은 계산하기 어려우므로 로그를 취해 합으로 치환 $\rightarrow$ **로그 우도 함수:** $\log L(\boldsymbol{\beta})$
$\log L(\boldsymbol{\beta})$를 미분하여 기울기가 0에 근접하도록 최적화하여 $\boldsymbol{\beta}$값 추정
### 로지스틱회귀 결과
X 1단위 증가 할때 logit(p)가 B만큼 증가 한다는 선형관계 도출
카드 사용량 이외의 입력 추가하여
다항 분류 계싼 가능 - 계수를 통한 상관관계 계산

# AI 기계학습 방법론 3
- 신경망 모델(Neural Network)

## 모수적 함수로서의 선형 모델
### 단순(1D) 선형모델:모수적함수
최소제곱 손실함수 손실함수 LOSS
$$
L[\phi] = \sum_{i=1}^{I} (\text{f}[x_i, \phi] - y_i)^2
$$
$$
= \sum_{i=1}^{I} (\phi_0 + \phi_1 x_i - y_i)^2
$$
**"최소제곱 손실함수"**
### 단순 선형모델 학습
순차적으로 모수(parameter)를 조정하면서 찾아내는 선형회귀
가장 fit 하는 선형들을 찾아나가는 과정
 * Loss - 손실함수, Slope - 기울기, Intercept - 절편

## Shallow 네트워크
### Shallow 네트워크
입력층 - 은닉층 1개 - 출력층 으로 구성된 신경망구조
장점 : 복잡한 비선형 패턴 학습가능, 구조간단, 과적합 위험 낮음
Ex : 간단한 분류, 회귀, XOR 같은 비선형 결정 바운더리 필요문제
- 단순(1D) 선형회귀
$$
\begin{aligned}
y &= \text{f}[x, \phi] \\
&= \phi_0 + \phi_1 x
\end{aligned}
$$
- Shallow 네트워크의 예
$$
\begin{aligned}
y &= \text{f}[x, \phi] \\
&= \phi_0 + \phi_1 \text{a}[\theta_{10} + \theta_{11}x] + \phi_2 \text{a}[\theta_{20} + \theta_{21}x] + \phi_3 \text{a}[\theta_{30} + \theta_{31}x]
\end{aligned}
$$
### Shallow 네트워크 vs 1D 선형회귀
예시 y = 각각 다른 숫자들로 이뤄진 x에대한 1차식의 묶음
### Shallow 네트워크 : 활성화 함수
\text{a}(활성화 함수) - 각각의 1차식이 0 이상이면 출력, 0미만인경우 0출력
### Shallow 네트워크 : 모수(parameter)
parameter : 학습 대상
모수가 주어지면 추론(계산,예측)할 수 있다.
훈련데이터 주어지면 손실함수를 정의하고, 손실을 최소화하도록 모수조정
### Shallow 네트워크 : piecewise linear함수
입력 구간을 나누어 조각별 선형 함수를 만든다.
모수가 달라지며 input은 같지만 모양이 달라짐
### Shallow 네트워크 : hidden units
각각의 식(x 1차항)을 hidden units -> $h_1$ $h_2$ $h_3$ 각각으로 할당
$$
\begin{aligned}
h_1 &= \text{a}[\theta_{10} + \theta_{11}x] \\
h_2 &= \text{a}[\theta_{20} + \theta_{21}x] \\
h_3 &= \text{a}[\theta_{30} + \theta_{31}x]
\end{aligned}
$$
### Shallow 네트워크 : 각 단계별 계산
선형 변환 - 각 h함수(x 1차항) 부분 표현
활성화함수 a 적용 -> 꺾인선 발생
$phi_i$ 각각 $h_i$에 곱함 -> 기울기/ 형태 변환
각각의 함수 합 -> 최종 f(x) 도출
### 네트워크 도식화
각 파라미터는 (출발노드의 값)x(가중치)를 출력 노드에 더한다.
-> 단순화
슬라이드 1-2 88p

## Shallow 네트워크의 표현력
### 더 많은 hidden unit 기능
- (일반적인) D개의 hidden units:
$$
h_d = \text{a}[\theta_{d0} + \theta_{d1}x] \quad\quad y = \phi_0 + \sum_{d=1}^{D} \phi_d h_d
$$
### Hidden Unit을 많이 두면
변곡점은 그대로 가져감 -> hidden unit이 늘어나면 임의 1차원 함수를 원하는 정확도로 근사 가능(5, 10, 20 ...)
### 보편적 근사 정리(Universal approximation theorem)
"Hidden Unit을 충분히 많이 갖는다면 얕은 신경망은 임의 연속함수를 임의의 정밀도로 근사할 수 있음"

## 다중 출력/입력
### 2개 출력의 네트워크
input 1개에 y output 2개 
hidden unit 그대로에 가는 출력만 여러 갈래로 나뉘는 구조
꺾이는 점은 hidden unit으로 결정되므로 각각 y만 다르고 점은 같다
### 2개 입력의 네트워크
입력 x가 2개일때 hidden unit으로 들어가서 y 1개 도출되는 과정
### 2개입력 : 단계별 계산
선형 변환 -> x1, x2 에 대한 표기
활성화함수 a를 선형에 그대로 전체괄호 곱(면 출력이잘린다-꺾이는 점)
hidden unit에 상수 $phi$ 곱 적용 - 면의 기울기가 변동(색의 채도)
3개 함수 합 = 결과
### 2개입력 : From input to output
3차원 공간좌표로 여러 면의 합체본 도출
임의의 개수 입력 - hidden unit - 출력 
$$
h_d = \text{a}\left[ \theta_{d0} + \sum_{i=1}^{D_i} \theta_{di} x_i \right] \quad\quad y_j = \phi_{j0} + \sum_{d=1}^{D} \phi_{jd} h_d
$$

## Deep 네트워크
### 2개의 네트워크를 하나로 합성
**Network 1: h**
$$
\begin{aligned}
h_1 &= \text{a}[\theta_{10} + \theta_{11}x] \\
h_2 &= \text{a}[\theta_{20} + \theta_{21}x] \\
h_3 &= \text{a}[\theta_{30} + \theta_{31}x]
\end{aligned}
\quad\quad\quad\quad
y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3
$$

**Network 2: h'**
$$
\begin{aligned}
h'_1 &= \text{a}[\theta'_{10} + \theta'_{11}y] \\
h'_2 &= \text{a}[\theta'_{20} + \theta'_{21}y] \\
h'_3 &= \text{a}[\theta'_{30} + \theta'_{31}y]
\end{aligned}
\quad\quad\quad\quad
y' = \phi'_0 + \phi'_1 h'_1 + \phi'_2 h'_2 + \phi'_3 h'_3
$$
### 합성 네트워크의 층(layer)별 출력
합성함수 : x-> y , y -> y' x-> y'
### "접기" 비유
1차 함수에서 만들어진 그래프 종이를 한 면으로 합침
-> 접힌 종이에서 점으로 표현 그거를 다시 펼쳐내서 그래프 확인
-> x값 1차 2차 면에 출력됨
### Shallow 네트워크, Deep 네트워크 비교
히든유닛 개수 같을 때 depp네트워크로 표현력이 높다
조각적 선형 구역 Deep > Shallow 네트워크
표현의 효율성이 높아서 deep 뉴런 네트워크 활용
### 2D input : 2개의 네트워크를 하나로 합성
각각 다른 종류 히든유닛 : 갯수가 다르거나 패턴이 다름
합성하여 최종 y' 합성면 추출

## Deep 네트워크 수식 표현
### 2개의 네트워크를 하나로 합성 : 복잡한 수식
네트워크 1, 2의 히든유닛 -> y값을 1번 네트워크로 치환해서 전개
### 새로운 변수 활용
네트워크 2 의 전개된 수식을 새로운 파라미터 사용으로 간결화
### 2층 네트워크의 표현
네트워크를 합성함수로 보고 풀이
두번째 히든유닛들의 전활성값 확인 (4단계중 3단계)
이 시점에서는 출력 3개를 갖는 1층 네트워크 (h' 값이 나오기 전)
### 2층 네트워크 : 단계별 계산
네트워크 1 첫번째 layer 출력 -> h' 네트워크 2 연결
그대로 a, 상수 곱 , 함수 3개 합 출력 f(x)
### 용어의 단순화 : 벡터 및 행렬 표현
$$
\begin{aligned}
\begin{aligned}
h_1 &= \text{a}[\theta_{10} + \theta_{11}x] \\
h_2 &= \text{a}[\theta_{20} + \theta_{21}x] \\
h_3 &= \text{a}[\theta_{30} + \theta_{31}x]
\end{aligned}
&\quad\longrightarrow\quad
\begin{bmatrix} h_1 \\ h_2 \\ h_3 \end{bmatrix} = \text{a}\left[ \begin{bmatrix} \theta_{10} \\ \theta_{20} \\ \theta_{30} \end{bmatrix} + \begin{bmatrix} \theta_{11} \\ \theta_{21} \\ \theta_{31} \end{bmatrix} x \right]
\end{aligned}
$$
$$
\begin{aligned}
\begin{aligned}
h'_1 &= \text{a}[\psi_{10} + \psi_{11}h_1 + \psi_{12}h_2 + \psi_{13}h_3] \\
h'_2 &= \text{a}[\psi_{20} + \psi_{21}h_1 + \psi_{22}h_2 + \psi_{23}h_3] \\
h'_3 &= \text{a}[\psi_{30} + \psi_{31}h_1 + \psi_{32}h_2 + \psi_{33}h_3]
\end{aligned}
&\quad\longrightarrow\quad
\begin{bmatrix} h'_1 \\ h'_2 \\ h'_3 \end{bmatrix} = \text{a}\left[ \begin{bmatrix} \psi_{10} \\ \psi_{20} \\ \psi_{30} \end{bmatrix} + \begin{bmatrix} \psi_{11} & \psi_{12} & \psi_{13} \\ \psi_{21} & \psi_{22} & \psi_{23} \\ \psi_{31} & \psi_{32} & \psi_{33} \end{bmatrix} \begin{bmatrix} h_1 \\ h_2 \\ h_3 \end{bmatrix} \right]
\end{aligned}
$$
$$
\begin{aligned}
y' = \phi'_0 + \phi'_1 h'_1 + \phi'_2 h'_2 + \phi'_3 h'_3
\quad\longrightarrow\quad
y' = \phi'_0 + \begin{bmatrix} \phi'_1 & \phi'_2 & \phi'_3 \end{bmatrix} \begin{bmatrix} h'_1 \\ h'_2 \\ h'_3 \end{bmatrix}
\end{aligned}
$$
$$
\begin{aligned}
\mathbf{h} &= \mathbf{a}\left[ \boldsymbol{\theta}_0 + \boldsymbol{\theta}x \right]
&\quad\longrightarrow\quad
\mathbf{h}_1 &= \mathbf{a}\left[ \boldsymbol{\beta}_0 + \boldsymbol{\Omega}_0 \mathbf{x} \right] \\
\\
\mathbf{h}' &= \mathbf{a}\left[ \boldsymbol{\psi}_0 + \mathbf{\Psi} \mathbf{h} \right]
&\quad\longrightarrow\quad
\mathbf{h}_2 &= \mathbf{a}\left[ \boldsymbol{\beta}_1 + \boldsymbol{\Omega}_1 \mathbf{h}_1 \right] \\
\\
y &= \phi'_0 + \boldsymbol{\phi}' \mathbf{h}'
&\quad\longrightarrow\quad
\mathbf{y} &= \boldsymbol{\beta}_2 + \boldsymbol{\Omega}_2 \mathbf{h}_2
\end{aligned}
$$
$\beta$: Bias vector (편향 벡터)
$\omega$: Weight matrix (가중치 행렬)

**$phi$ ,$theta$ 로 기호 표현 가능 x_1로 항번호, ^로 제곱표현 가능**

# AI 기계학습 방법론 4
* 신경망 적합 

## 선형회귀 예시
### 손실함수
손실함수 : 모델이 얼마나 잘못 예측하는지를 측정하는 함수
* 학습데이터셋 표기: $\{ \mathbf{x}_i, \mathbf{y}_i \}_{i=1}^{I}$
* 손실함수 정의: $L[\phi, \text{f}[\mathbf{x}_i, \phi], \{\mathbf{x}_i, \mathbf{y}_i\}_{i=1}^{I}]$
* 손실함수의 간단한 표현: $L[\phi]$
### 학습 
손실함수를 최소화하는 파라미터를 찾음
### 1D 선형회귀 예
선형회귀 손실함수는 예측값과 실제값의 차이를 제곱하여 합산한 값
학습의 목적 이 손실함수 최소화하는 직선 찾기
### 1D 선형회귀 학습
등고선 : 손실함수 값의 크기(기울기, 절편, 오차)
데이터와 선형함수(직선) : 데이터와 선의 오차가 크다면 손실값이 큼
* 경사 하강법 : 손실 함수의 값이 줄어드는 방향으로 파라미터를 이동하는 과정

## 수학 리뷰
### 미분을 이용한 최적화
미분결과 = 기울기를 의미 -> 기울기 0이 극값(최소값)
- Ex : 
$$
y = x^2 - 4x + 5
$$
$$
\frac{\partial y}{\partial x} = 2x - 4
$$
미분의 반대방향(기울기) , 방향과 절대값 확인
미분 방향 : 미분한 값이 양수냐 음수냐에 따라 양의 기울기 방향으로, 음의 기울기 방향으로 가는 것

## 경사 하강
### 경사 하강법
손실함수 L[$\phi$]를 최소화하기 위해 파라미터 $\phi$를 반복적으로 갱신
기울기 계산 : 손실함수 L[$\phi$]를 파라미터 $\phi$에 대해 편미분 진행
파라미터 업데이트 -> 기울기(미분값)의 반대방향으로 이동해야 손실 함수가 줄어듬
$$
\phi \leftarrow \phi - \alpha \frac{\partial L}{\partial \phi}
$$
### 경사 하강법 : 단계별 계산
1. 미분값 구하기 -> 손실함수의 미분 계산 : 각 데이터손실에 대한 편미분의 합으로표현
$$
\frac{\partial L}{\partial \mathbf{\phi}} = \frac{\partial}{\partial \mathbf{\phi}} \sum_{i=1}^{I} \ell_i = \sum_{i=1}^{I} \frac{\partial \ell_i}{\partial \mathbf{\phi}}
$$
$$
\frac{\partial \ell_i}{\partial \mathbf{\phi}} = \begin{bmatrix} \frac{\partial \ell_i}{\partial \phi_0} \\ \frac{\partial \ell_i}{\partial \phi_1} \end{bmatrix} = \begin{bmatrix} 2(\phi_0 + \phi_1 x_i - y_i) \\ 2x_i(\phi_0 + \phi_1 x_i - y_i) \end{bmatrix}
$$
2. 파라미터 업데이트 (반대방향 이동)
$$
\phi \leftarrow \phi - \alpha \frac{\partial L}{\partial \phi}
$$
 * $\alpha$ = 학습률(learning rate)
### 한눈으로 보는 경사 하강법
과정 
a. 데이터 
b. 손실함수 3D그래프 
c. 손실함수 등고선
d. 선형함수 업데이트
### 함수에 따른 최적화 난이도
Convex : 곡선이 항상 U자처럼 아래로볼록. 그래프 위 임의 두 점을 잇는 직선이 그래프위(>=)로 있음(쉬움)
Non-convex : 봉우리,골짜기,오목한 구간이 섞인 모양. 두 점을 이은 직선이 그래프 아래로 내려가는 구간이 생김.(어려움)
손실함수 모양에 따라 최적화 난이도가 달라짐 
Convex 문제 : 전역 최소값이 유일함 -> 최적화가 쉬움
Non-Convex 문제 : 여러 개의 지역 최소값 또는 새들(saddle)점이 있음 -> 최적화가 어려움
  * 새들점(안장점) - 다변수 함수에서 기울기가 0이지만 극대값도 극소값도 아닌, 말의 안장 모양처럼 방향에 따라 다른 성질을 보이는 특별한 지점
*기울기가 0 이라고 Loss가 0인것은 아님*

## 확률적 경사 하강법
### 경사 하강법 vs 확률적 경사 하강법
경사 하강법의 단점 : non-convex문제에서 지역(local) 최소점에 빠지기 쉽다(기울기 0 값이 많음)
매 스텝마다 전체 데이터 미분값 구하여 업데이트 : 스텝별 계산량 많음
대안 - 확.경.하
### 확률적 경사 하강법의 개념
방식 차이점
경사 하강법(GD) : 전체 데이터셋 사용 기울기 계산, 파라미터 업데이트
확률적 경사 하강법(SGD) : 무작위 확률로 샘플된 일부 데이터만 사용하여 기울기 계산
$$
\phi_{t+1} \leftarrow \phi_t - \alpha \sum_{i \in \mathcal{B}_t} \frac{\partial \ell_i[\phi_t]}{\partial \phi}
$$
<u>SGD : Stochastic Gradient Descent</u>

### 확률적 경사 하강법 결과 예시
전체데이터에 비해 sample 데이터에 따른 차이때문에 유동성이 큰 그래프 도출(무작위성)
### 확률적 경사 하강법의 특성
* 무작위 샘플데이터를 활용한 미분으로 경로의 무작위성이 있음
* Local 최소점에 빠질 위험이 상대적으로 적음
(국소 최솟값, 노이즈가 있지만 타당한 업데이트, 계산비용 절감, 수렴 특성)
milestone 지정 후 잘못되어도 재 수정 용이
평균적으로 올바른 방향을 가리켜 점진적 최적점 방향으로 수렴
convex 문제 - 일반 경사하강법보다 느릴 수 있음(지그재그 순회)

## 역전파(Backpropagation)
### 네트워크 파라미터의 미분
Layer별로 파라미터들이 존재함. 서로 영향을 미침
네트워크 - 합성함수
### 합성함수의 미분 : 연쇄법칙
파라미터는 $omega$를 갖는 합성 함수를 미분하면?
층(Layer)이 쌓인 네트워크는 결국 합성함수 형태: $y = f_1(f_0(x; \omega))$

**핵심 아이디어:** 합성함수의 변화율 = 바깥 함수의 변화율 $\times$ 안쪽 함수의 변화율
$$
\frac{\partial y}{\partial \mathbf{\Omega}} = \frac{\partial f_1(f_0(x; \mathbf{\Omega}))}{\partial f_0(x; \mathbf{\Omega})} \cdot \frac{\partial f_0(x; \mathbf{\Omega})}{\partial \mathbf{\Omega}}
$$
해석: $f_1$의 입력에 대한 기울기 구한뒤
그 값을 $f_0$의 파라미터 오메가에 대한 기울기와 곱한다
x는 파라미터가 아니므로 y/x 미분 필요없음
출력 오차를 기준으로 연쇄 법칙을 이용해 그래프를 거꾸로 따라가며 각 노드(파라미터 포함)의 미분값을 계산
### 역전파
출력오차를 기준으로 그래프를 거꾸로 따라가며 연쇄법칙으로 각 노드(파라미터 포함)의 미분값을 계산하는 절차
1. 각 단계별 계산을 분해
2. 각 layer별 값($f_k$)을 계산
3. 각 layer별 값($f_k$)에 대한 출력 손실($l_i$)의 미분 구함
4. 파라미터에 대한 미분을 구함 :
손실로부터 각 layer별 파라미터의 미분을 거꾸로 구할 수 있음
-> hidden layer가 늘어나는 것이 계산량 증가, 손실 증가로 연결
표현력이 좋아짐 -> 학습이 어려워진다
학습이 쉽다 -> 표현력이 부족하다
=========================
# MLP(다층 퍼셉트론)

## 왜 MLP인가? (딥러닝의 시작)
- 고전적 머신러닝(사람이 힌트를 준다)
- 딥러닝(스스로 힌트를 찾는다)
무조건 딥러닝이 정답은 아님, 상황에 따라 선택 및 판단
MLP의 p - 퍼셉트론(인공신경망의 가장 기초단위)
XOR문제 - 둘중 하나만 참일때 참(서로 다를 때)
하나의 직선으로 XOR 문제 해결 불가능
층을 여러 개 쌓았을 때 각 층의 가중치를 어떻게 학습시키지? 로 시작
퍼셉트론 여러층으로 쌓아 여러개 직선을 조합. -> 신경망의 시작

## MLP의 구조와 핵심원리
기본구조 : 다층퍼셉트론은 기본블록(퍼셉트론)을 여러층으로 쌓아만든 딥러닝 기본모델
입력층, 은닉층, 출력층으로 구성
선형 데이터만 받으므로 flatten 하여 표를 선형으로 전개
-> 이미지 구조에는 표현력이 약하다
- 층을 쌓으면 XOR이 풀리는 이유
은닉층 == '종이를 접는 손'
방법 1. 직선을 여러개 쓴다
방법 2. 같은 이야기를 그림으로 - '종이접기'
접는 주체 : 활성화함수 a - 비선형성을 추가함 
과거 시그모이드 -> 현재 ReLu
시그모이드 함수 - S자 곡선
기울기 최대 : 0.25
층을 10번 거친다 - 0.25 10제곱 - 0에수렴
퍼셉트론이 가중합을 구하는 이유 : 다음신호로 이렇게 고쳐라 선언
신호가 0에 수렴하여 앞쪽층까지 못가고 사라져버림
ReLu - 0보다 작으면 0 ,0 이상이면 해당 값 반환

## 모델은 어떻게 학습하는가(학습과정)
1. 순전파(Forward Propagation)
2. 손실 함수 계산(Loss Function)
3. 역전파(Back Propagation)
4. 옵티마이저
이 한바퀴를 계속 반복 -> 트레이닝(훈련)=학습
### 순전파
입력데이터가 입력층에서 시작해 은닉층을 거쳐 출력층까지 앞에서 뒤로 흘러가며 최종예측값 계산하는과정
초기 입력값 = 무작위(서로 다른 계산값을 구하도록)
### 손실함수(모델의 오답노트)
순전파로 나온 엉터리 예측 결과와 실제 정답을 비교하여 '얼마나 틀렸는지'를 하나의 숫자로 계산
### 역전파(분석가)
계산된 '손실'의 원인을 찾아 출력층에서부터 거꾸로 추적하며, 각 가중치를 어떻게 수정해야 손실이 줄어들지 계산하는 과정
### 옵티마이저(똑똑한 가중치 업데이트)
역전파로 계산된'책임(기울기)'을 바탕으로, 손실함수값이 최소가 되는 최적의 가중치를 효율적으로 찾아가는 알고리즘

## Pytorch 코드 연계
pytorch : 오픈소스 딥러닝 프레임워크
모델 설계, 데이터 준비
*GPU를 활용한 연산가속을 활용하는 소프트웨어*

## 똑똑하게 학습시키기(성능향상)
과적합과 규제
과적합 : 처음보는 데이터에 대한 응용력이 떨어지는 현상
규제 : 과적합을 막기 위해 모델에 제약을 거는 기법
드롭아웃 : 훈련시, 은닉층의 뉴런을 일정 확률로 랜덤하게 OFF
추론(평가)시에는 전부 ON
### 훈련 종료 시점 정하기
학습 루프 : 순전파 -> 가중치 업데이트(옵티마이저) 과정 수없이반복
조기 종료
* 훈련에 사용하지 않은 '검증데이터' 모의고사
* 검증데이터 손실이 증가하기 시작하면 과적합 신호로 보고, 가장 성능이 좋았던 시점에서 학습을 자동으로 멈추는 기법
* 가장 널리 사용되는 과적합 방지법

## 참고
### 옵티마이저(Adam)
머신러닝에서 손실함수의 값이 최소가 되는 최적의 가중치를 찾기 위해 사용되는 알고리즘
Adam(Adaptive Moment Estimation) : 적응적으로 학습률을 조정
1. 관성 도입 : 속도, 방향 기억 부드러운하강으로 지역최저 빠져나옴
2. 적응적 학습률 : 고유한 학습률 부여, 최적의 학습속도 유지 효율적으로 낮은 손실 지점 도달
==================================
# 자연어 처리 기본
언어를 어떻게 숫자로 표현할까?
순서와 의미의 연결성
순환 신경망의 동작

## 워드 임베딩
### 원 핫 인코딩
연구에서 단어를 원자적(쪼갤수 없는) 기호로 취급,
벡터공간관점 : 한 원소만 1이고 나머지는 모두 0인 벡터
차원 수(= 단어 사전 크기)
* 음성데이터 2만 - penn treebank 5만 - big vocab 50만 - google 1T 1300만 : 이를 원-핫 표현, 바꾸는 과정 = 원-핫 인코딩
- 문제점 : 검색 쿼리 벡터와 대상이 되는 문서 벡터들이 서로 '직교'하게 되어, 원-핫 벡터로는 유사도를 측정할 수 없다
직교 : 정보를 가진 1의 위치가 겹치지 않는다.
1. 차원의 저주 : 표현하고자 하는 차원이 커질수록 메모리 증가, 활용이 어렵고 표현이 어렵다
2. 의미적 정보 부족 : 비슷한 단어라도 유사한 벡터로 표현되지 않는다(직교)
### 워드 임베딩
주변 단어들(컨텍스트)를 통해 표현하면 많은 의미를 담을 수 있다.
주변단어와 맥락을 통해 관련도 표현 - 현대 통계적 자연어 처리에서 가장 성공적 아이디어
* 워드 임베딩 : 단어를 단어사이 의미적 관계를 포착할 수 있는 밀집되고 연속적 벡터 표현으로 나타내는 방법
앞뒤 단어 노출을 통해 의미나 단어를 예측할 수 있도록 한다
- word2vec : 각 단어와 그 주변 단어들 간의 관계를 예측한다.
1. skip-grams : 중심단어를 통해 주변 단어 예측 
   * 중심단어 위치 t, 중심단어 주변 몇개 단어를 통해 나타날 확률을 최대화 하는 모델
2. CBOW : 주변 단어들을 통해 중심 단어를 예측
   * input으로 나오는 주변 단어들을 통해 모델링 하여 결과로 나오는 단어의 확률분포 정리  
Skip gram : 적은데이터에도 잘 동작, 희귀단어/구 표현에 강함 - 학습속도 느림  
CBOW : 학습속도 빠름, 자주나오는 단어에 강함 - 희귀단어표현 약함   
샘플 숫자 SG >> CBOW

## 순차적 데이터(sequential data)
데이터가 입력되는 순서와 이 순서로 입력되는 데이터들 사이의 관계가 중요한 데이터
1. 순서 중요 : 순서가 바뀌면 의미 달라짐
2. 장기 의존성 : 멀리 떨어진 과거의 정보가 현재/미래에 영향
3. 가변 길이 : 길이가 일정하지 않고, 단어 수 제각각, 멀리 떨어진 단어들의 연결도/합침등 어려움
* Sequential Models 필요 : RNN, LSTM, Transformer 등

## RNN(순환 신경망)
전통적인 인공신경망(MLP, CNN)들은 가변길이 데이터 처리하기에 적합하지 않다
RNN은 가변길이 입력 받을 수 있고, 이전 입력을 기억할 수 있으므로 순차적데이터처리에 적합
전통신경망과 달리 'hidden state'를 가지고 있음. 입력벡터 $x_t$ 처리할 때, 각 시점마다 recurrence 수식 적용 - hidden state 업데이트
RNN 아키텍쳐 
$$
h_t = f_W(h_{t-1}, x_t)
$$
**주의:** RNN은 각 시점마다 동일한 가중치(weight)를 사용하기 때문에, 깊은 feedforward 신경망보다 **파라미터 효율적**이다.
* RNN은 한번에 하나의 요소를 처리하고, 정보를 앞으로 전달
* 펼쳐보면, RNN은 각 층이 하나의 시점을 나타내는 깊은 신경망처럼 보인다.
* RNN은 hidden state를 유지하면서 가변길이 데이터 처리 가능
* RNN의 출력은 과거 입력에 영향을 받는다는 점에서, feedforward 신경망과 다르다.
한계 : 기울기 소실 문제 - 역전파시 앞쪽 층 기울기 0에 수렴하여 장기의존성 학습 어려워지는 현상
(과거시점 오차신호 갈수록 더 작은 기울기 갖게 됨)

## LSTM
기울기 소실 문제를 해결하기 위해, 1997년에 제안된 RNN의 한 종류
특징 : 시점 $t$ 에서 RNN은 길이가 $n$인 벡터 hidden state와 cell state를 가진다
$h_t$ 는 단기정보, $C_t$는 장기정보 저장
Cell state에서 정보를 읽고 지우고 기록할 수 있다.
- RNN vs LSTM
중간과정 처리의 차이 - cell state 발생
3가지게이트 Forget, Input, Output 게이트를 통해 어떤 정보를 지우고,쓰고, 읽을 지 결정
매 시점마다 게이트의 각 요소 : 열림(1), 닫힘(0), 혹은 그 사이의 값으로 설정
Forget : 이전 cell state에서 무엇을 버리고 무엇을 유지할 지 결정(0인 부분은 날리고, 1인부분은 남김)
$$
f_t = \sigma(W_f \cdot [h_{t-1}, x_t] + b_f)
$$
Input : 새 정보 중 얼마나 cell state에 쓸지 결정
$$
\begin{aligned}
i_t &= \sigma(W_i \cdot [h_{t-1}, x_t] + b_i) \\
\tilde{C}_t &= \tanh(W_C \cdot [h_{t-1}, x_t] + b_C)
\end{aligned}
$$
New cell content : cell에 기록될 새로운 후보
$$
C_t = f_t * C_{t-1} + i_t * \tilde{C}_t
$$
Output : cell state 중 얼마나 hidden state로 내보낼지 결정
$$
\begin{aligned}
o_t &= \sigma(W_o [h_{t-1}, x_t] + b_o) \\
h_t &= o_t * \tanh(C_t)
\end{aligned}
$$

# 자연어 생성 모델(seq2seq, Attention)
언어모델이란 무엇일까?

## 언어모델이란?
인간의 두뇌가 자연어를 생성하는 능력을 모방한 모델(단어 시퀀스 전체에 확률을 부여, 문장의 자연스러움 측정)
* 한 문장의 확률은 각 단어의 조건부 확률들의 곱으로 표현
대표적 언어모델 N-gram 언어모델 n-gram : 연속된 n개의 단어 묶음
맥락을 제대로 활용하지 못하는 모델로서의 단점 존재 - 단순히 묶음들의 등장 확률만 계산
과거 : 번역모델과 언어모델로 나누어 확률로서 계산하여 번역 방식에 대한 모델링/영어 문장 쓰는 방식에 대한 모델링으로 나눔
(번역모델 - 코퍼스(말뭉치)로 한국어, 영어 학습/ 언어모델 - 영어데이터 학습)
SMT : 다양한 한계 - 유지 및 확장에 어려움 -> NMT 로 전환

## seq2seq
Neural Machine Translation : 인공 신경망을 이용해 기계번역을 수행
사용되는 신경망 구조 시퀀스 투 시퀀스(seq2seq) 두개의 RNNs 로 이루어짐
인코더와 디코더로 구성 (어순이 같지 않아도 정보를 통해 훈련이 가능해진다 - 의미와 맥락)
Encoder : 입력문장에 담긴 정보 인코딩
Decoder : 인코딩된 정보를 조건으로 하여 타겟문장(출력)을 생성 
텍스트 심볼 -> 숫자화 -> 디코더를 통해 다시 타겟 언어로 재출력
- 기계번역 외 다양한 태스크 적용
요약 : 긴 길이의 문서를 읽고 짧은길이로 요약된 텍스트 출력
대화 : 사용자의 발화 기반, 맥락에 맞는 대답(출력텍스트) 생성
코드 생성 : 자연어로 작성된 설명 혹은 명령어를 입력 받아, 그에 대응하는 프로그래밍 코드 혹은 쿼리 출력
seq2seq모델 하나의 통합 네트워크로 연결 - 디코더 발생 오차 역전파를 통해 인코더에서 End to End 로 동시에 최적화
- Teacher Forcing : 모델이 스스로 예측한 단어 대신 정답 단어를 디코더 입력으로 강제로 넣어줌, 안정적이고 빠르게 학습
  * 과적합과 같은 오류 발생 가능 - 훈련은 잘되지만 학습상 오류 가능
- Greedy Inference : 토큰을 출력하는 방법 중 하나로, 각 단계에서 가장 확률이 높은 단어 선택
  * 되돌리기가 불가능 - 오답 수정 불가(한 가지 후보)
- Beam Search : 매 단계마다 k개 후보 유지 - 후보가 EOS(끝) 도달, 완성된 문장 리스트추가 - EOS 문장 충분히 모이면 탐색종료 - 각 후보들의 점수를 로그 확률의 합으로 구해 최종 선택
  * k가 1이면 inference와 같다

## Attention
seq2seq 한계 : bottleneck problem
인코더는 입력문장 전체를 하나의 벡터로 요약, 마지막 hidden state에 문장의 모든 의미정보가 담긴다
$h_t$  
고정길이 벡터에 모든 의미 압축 - 정보손실(bottleneck problem)  
어텐션은 디코더가 단어를 생성할 때, 인코더 전체 hidden state 중 필요한 부분을 직접 참조할 수 있도록 한다.  
Attention : 어텐션은 각 디코더 시점에서, 인코더가 만들어 놓은 여러 정보 중 현재 디코더가 어떤 정보에 얼마나 집중해야 하는지를 가중치로 계산하는 방법입니다.  
- 어텐션 스코어 & 어텐션 가중치 ($a$)개념: 디코더가 다음 단어를 예측할 때, 인코더의 입력 단어 중 "어느 단어를 집중해서 볼지" 정하는 문맥상 중요도 점수입니다.소프트맥스의 역할: 인코더 단어들의 어텐션 점수를 합이 1(100%)이 되는 확률 비율로 변환하여 직관적인 중요도 가중치($a$)로 만들어 줍니다.출력값($y$)과의 연결: 높은 가중치를 받은 단어의 정보가 컨텍스트 벡터($c$)에 집중 반영되며, 이것이 디코더로 들어가 최종 단어($y$)를 결정하는 직접적인 힌트가 됩니다.

효과 : NMT 성능 향상, Bottleneck problem 해결, Vanishing Gradient Problem 완화  
BLEU score : 기계 번역의 출력이 사람 번역과 얼마나 비슷한 지 평가하는 지표  
해석가능성 : 어텐션 분포를 통해 모델이 내부적으로 참고한 근거 파악 -> 의사결정 과정 해석  
정렬 : 어텐션을 통해 decoder가 필요한 입력 단어에 자동으로 집중, 단어와 단어 간의 매핑 관계를 자연스럽게 학습한다.  
디코더 hs, 인코더 hs - Query와 Value의 관계로 볼 수 있다  
Query - value 유사도 계산(dot-product, multiplication, additive) - softmax 통해 확률분포 얻기 - 분포를 이용해 values 가중합

# Transformer
트랜스포머란 무엇이며, 왜 필요한가?

## Self-Attention
RNN이 꼭 필요한가? - 물리적으로 멀어질(길어질) 수록 기울기소실/폭발 발생 장기 의존성 학습 어려움
병렬화 - forward/backward pass 모두 시퀀스 길이 만큼의 단계 필요 -> 순차적인 연산, 병렬화 불가=대규모데이터학습 비효율
Attention 매커니즘을 한 문장 내부에서 적용 : Self-Attention(인코더 내, 디코더 내에서의 어텐션)
장점 : 연산 수가 시퀀스 길이와 비례 X, 최대 상호작용거리 =O(1) 모든 단어가 각 층에서 직접 상호작용
시퀀스에서의 모든 단어를 업데이트 하기위해 동시에 비슷한 맥락으로서 연결
1. 각 단어를 Query, Key, Value 벡터로 변환한다.
  * Query : 단어 i가 다른 단어로부터 어떤 정보를 찾을지 정의  
$$
\mathbf{q}_i = Q x_i \quad (\text{\textit{queries}})
$$
  * Key : 단어 i가 자신이 가진 정보의 특성을 표현
$$
\mathbf{k}_i = K x_i \quad (\text{\textit{keys}})
$$
  * Value : 실제로 참조되는 정보 내용을 담고 있는 벡터
$$
\mathbf{v}_i = V x_i \quad (\text{\textit{values}})
$$
  * 어텐션 단어 i 표현 Q,V / 셀프어텐션 단어 i표현 Q, K, V
2. Query, Keys 간의 유사도 계산해 softmax 확률분포 계산
$$
e_{ij} = \mathbf{q}_i^T \mathbf{k}_j \quad\quad\quad\quad \alpha_{ij} = \frac{\exp(e_{ij})}{\sum_{j'} \exp(e_{ij'})}
$$
3. 각 단어의 출력을 Values의 가중합으로 계산 
$$
o_i = \sum_{j} \alpha_{ij} \mathbf{v}_j
$$
self-attention의 한계 
1. 순서정보 부재 : 단어 간 유사도만 계산
2. 비선형성 부족 : 가중평균연산이라는 선형결합에 불과, 복잡한패턴/깊은표현력 담기 어렵다.
3. 미래 참조 문제 : self-attention은 모든 단어를 동시에 보기 때문에, 생성되지 않아야할 미래 단어를 참조
- 한계 해결 1 - Positional Encoding
각 단어위치 i를 나타내는 위치 벡터를 정의해, 단어 임베딩 값에 더해 최종 입력으로 사용한다.
두 가지 방법 : 서로 다른주기 사인/코사인 함수 합성(SPE), 위치벡터를 모두 학습파라미터로 설정해 데이터에 맞춰 최적화(LAPE)
순서 정보의 부재를 해결하기 위함
- 한계 해결 2 - Feed-Forward Network 추가하기
각 단어 출력 벡터에 feed-forward network를 추가해 self-attention이 만든 표현을 깊고 비선형적인 표현으로 확장
레이어와 레이어 사이 비선형 함수 추가하여 모든 단어에 대해 사용하여 비선형적인 구조 형성하도록
- 한계 해결 3 - Masked Self-Attention
어텐션 스코어 계산 시 미래 단어 항목 음의 무한으로 설정하여, 계산을 수행할 때 반영되지 않도록 하는 방법
셀프어텐션은 기본적으로 모든단어(미래 포함)를 동시참조하기 때문

Self-Attention : 문장내 모든 단어가 서로 직접 상호작용하여 1. 장거리 의존성을 효율적으로 포착, 2. 병렬처리를 가능하게 하는 매커니즘

## Transformer
self-attention을 핵심 메커니즘으로 하는 신경망 구조이다.
encoder-decoder 구조로 설계된 신경망 모델 / decoder가 언어 모델과 같은 방식으로 동작
- Multi-Headed Attention
문장에서 같은 단어라도 여러이유로 다른 단어에 주목할 수 있다.
하지만 단일 Self-attention head 로는 한가지 관점에서의 단어 간 관계밖에 파악할 수 없다.
-> 따라서 여러 attention head를 두어 다양한 관점에서 동시에 정보를 파악한다.
Attention Head 1 : 단어의 문맥적 관계에 Attention
               2 : 단어의 시제에 Attention
               3 : 명사에 Attention
관계나 주목하는 방식에 따라 같은 단어 $a$ 값이 달라질 수 있음
- Scaled Dot Product
query와 key의 차원이 커질수록 두 벡터의 내적 값도 자연스럽게 커진다.
값이 너무 크면 softmax 함수가 출력이 지나치게 뾰족해져, gradient vanishing 문제 발생 가능
$$
\text{output}_l = \text{softmax}(X Q_l K_l^T X^T) * X V_l
$$
$$
\text{softmax}([40, 50, 45]) \cong [0.000045, 0.99326, 0.00669]
$$
-> 내적 값을 그대로 사용하지 않고, 나눠주어 스케일 조정 : 
$$
\text{output}_l = \text{softmax}\left( \frac{X Q_l K_l^T X^T}{\sqrt{d/h}} \right) * X V_l
$$
나누는 값 $\sqrt{d/h}$ = 8로 스케일링
$$
\text{softmax}([5.00, 6.25, 5.625]) \cong [0.1573, 0.5489, 0.2938]
$$
값이 안정적으로 분포, 학습 속도 상승 및 안정성 상승
- Residual Connection
깊은 신경망 층이 깊을수록 학습 어려움, layer가 전체를 예측 X. 기존입력과의 차이만 학습
- Layer Normalization
층이 깊어질수록 히든벡터 값 확대/축소 학습 불안정
각 레이어 단위에서 히든벡터 값 정규화
Transformer decoder는 여러개의 디코더 블록 쌓아올려서 만든 구조 : 언어모델처럼 단방향 문맥만 활용
<-> Encoder 양방향 문맥을 모두 활용할 수 있음(입력문장 의미적표현 변환, 각 단어 양방향 문맥 모두 반영한 벡터로 인코딩)
차이점 : self-attention에서 masking 제거
- Encoder-Decoder 트랜스포머 인코더 디코더 : self-attention만 하는 것이 아니라, encoder의 출력 표현을 참조하는 cross-attention 추가
- Cross-Attention : 수행 시 Query - decoder, Key, Value - encoder에서 가져옴
기존 모델에 비해 성능과 학습효율 모두 향상됨

# 사전학습
## 사전학습이란?
대규모 데이터셋을 이용해, 모델이 데이터의 일반적인 특징과 표현을 학습하도록 하는 과정  
워드 임베딩 vs 언어모델 : 워드임베딩 사전학습 한계 존재 - 학습데이터 적음, 연결 네트워크 초기화되어 학습효율 낮음, 데이터 시간 많이 소요  
언어모델 : 모델 전체를 사전학습, 모델 구축 이점 - 언어에대한 표현 학습용 적합, 파라미터 초기화의 관점에서 효과적, 언어에 대한 확률 분포 학습을 통해 샘플링, 생성에 사용 가능  
파인튜닝 패러다임 : 언어패턴을 잘 학습한 파라미터로 초기화해 NLP 어플리케이션 성능 향상

## Encoder 모델
양방향 문맥 활용 가능 - 전통적인 언어모델과는 차이점이 있다.
입력 단어의 일부를 [MASK]토큰으로 치환해 올 단어 예측 : Masked Language Model(BERT)
BERT : Masked LM 방법 사전학습 수행
- 방법
  - 입력 토큰의 15% 무작위 선택
  - [MASK]토큰 치환(80%), 랜덤 토큰 치환(10%), 그대로 두기(10%)
모델이 마스크된 단어에만 집중하지 않고, 다양한 문맥 표현을 학습해 더 강건한 표현 학습
방법 2 : NSP - 두개의 연속된 텍스트 받아, 두번째 문장이 첫번째 문장의 실제 다음문장인지 여부 예측하는 next sentence prediction
- BERT 다운스트림 태스크 - sentence level
두 문장 관계 분류 태스크 MNLI - 전제 , 가설,  분류{함의, 모순, 중립}
QQP - Q1, Q2, 분류{중복, 비중복}
단일 문장 분류 태스크 SST2 - 문장, 분류{긍정, 부정}
- Token level
QA 태스크 : SQuAD - 질문, 문맥, 정답
**개체명 인식** : 문장, 라벨(단어별 분류 B,I,O 태그를 통해 완성)
여러 태스크를 적용하여 파인튜닝을 통해 여러 NLP과제에서 최첨단 성능 도출 - 범용화된 모델의 개발을 예견

## Encoder-decoder 모델
T5(text-to-text transfer transformer) : 모든 태스크를 Text-To-Text 포맷으로 변환해 하나의 모델로 학습
학습방법 - Span corruption
encoder 입력 문장 모두 보고, 그정보 바탕 Decoder 출력 생성
학습을 위해, Span corruption이라는 과정 수행
* 입력 문장에서 연속된 토큰을 무작위로 선택해 제거
* 제거된 부분을 특수 placeholder 토큰으로 치환
* 디코더는 이 placeholder에 해당하는 원래 span을 복원하도록 학습
언어 모델링처럼 훈련을 수행할 수 있다.

## Decoder 모델
Finetuning Decoder
* Transformer의 Decoder는 사전학습 단계에서 다음단어예측(Next Token Prediction)을 학습한다.
한쪽에 병렬구조를 중첩시켰을 때 모델의 성능이 올라간다(슈퍼컴퓨터)
* 생성 태스크 : 다음 단어 예측 방식 fine-tuning / 출력이 시퀀스인 태스크(대화나 요약 태스크)에 적합
* 분류 태스크 : 마지막 hidden state 위에 새로운 linear layer를 연결해 classifier로 사용  
해당원리(다음단어 예측, 분류태스크)를 통해 GPT 모델이 1~2까지 발전

## In-Context Learning
GPT-3 , parametersize를 키워(1750억개) 파인튜닝 없이 컨텍스트 안의 예시만 보고도 새로운 태스크 수행
해당 능력을 In-Context Learning이라고 한다.  
예시와 함께 어떤 태스크를 할 지 지정  
LLM의 기본과의 연결 - Chat GPT의 태동을 일으킨 모델 중첩을 통한 슈퍼컴퓨터의 연산 및 사이즈 확대
-> 성능 향상으로 직결
===========================
# 텍스트 파운데이션 모델
파운데이션 모델이란?

## 파운데이션 모델
통상 10억개 이상의 매개변수 - 대형 모델이라고 지칭  
다양한 작업에 범용적으로 활용할 수 있는 기초 역할  
예시 : 텍스트생성(ChatGPT), 비디오 생성(SORA), 멀티모달 출력(Gpt-4o)  
파운데이션 모델 이전 : 태스크 해결 위해 별도의 학습 필요  
현대 : 자세한설명(프롬프트) 입력만으로 충분해짐  
- 파운데이션 모델의 3가지 구성요소
  1. 빅데이터 : 인터넷에 존재하는 데이터 수가 기하급수적으로 증가(AI모델은 데이터 늘수록 성능 증가)
  2. 자가학습 알고리즘 : 사람이 정답을 알려줄 필요 없음
  3. 어텐션 기반 트랜스포머 모델 : 더 많은 데이터를 보다 잘 학습하고 이해할 수 있는 인공신경망 구조

## 텍스트 파운데이션 모델 살펴보기
### 텍스트 파운데이션 모델이란?
GPT-1, BERT같은 언어모델에도 3가지 구성요소 이미 포함됨  
but, 파운데이션 모델과 같은 능력을 보여주지 못함  
gpt-2 : 언어 모델이 추가학습 없이 텍스트 지시를 통해 새로운 태스크 수행 가능성 확인
- 특징
1. 규모의 법칙 : 더 많은 데이터, 큰 모델, 긴 학습 -> 더 좋은 성능
2. 창발성 : 특정 규모를 넘어서면 갑자기 모델에서 발현되는 성질(인컨텍스트 학습, 추론 능력)
### 거대 언어 모델 예시
기존 대비 1. 더 큰 모델(Bill)이 2. 더 많은 데이터(trill)에서 학습되어 창발성이 나타나기 시작한 언어모델 : LLM  
폐쇄형(closed) 거대 언어 모델 : 예시(gpt, claude, gemini) 등 세부정보 공개 X하여 사용되는 모델  
일반적 특징
* GPT : 유저 수 최다, 전반적 성능  
* Claude : 안전 지향, 코딩작업  
* Gemini : 가장 긴 입력출력 지원, 뛰어난 멀티모달  

개방형(open sourced) 거대 언어 모델 : LLaMA(Meta), Gemma(google), Qwen(Alibaba)  
* 장점 : 1. 무료, 2. 모든 정보(모델 구조, 소스코드) 공개  
* 단점 : 충분한 계산 자원(GPU 등) 필요, 상대적으로 폐쇄형 모델에 비해 성능이 낮음  

## 거대 언어 모델의 학습
GPT-3 : 거대언어모델의 시초  
학습방법 - 기존과 유사  
학습데이터 : 3000억 토큰(4TB 텍스트데이터 = 인터넷 + 양질의 텍스트북)  
학습비용 : 150억 정도로 추산  
다음 토큰 예측 기반 거대 언어 모델의 한계  
* 사람의 지시에 대해 올바르지 않은 응답을 생성하거나, 유해한 응답 생성 가능
정렬(Alignment)학습 : 거대 언어 모델의 출력이 사용자의 의도와 가치를 반영하도록 하는 것
1. 지시학습 : 지시에대해 어떤 응답이 생성
2. 선호학습 : 상대적으로 어떤 응답이 더 선호  
둘다 post-training - 사전 학습 이후 모델을 조정하는 과정  
### 지시학습(Instruction tuning)
지도학습을 통해 어떤 응답이 생성되도록 해야하는지 조정  
학습방법 자체는 기존모델(BERT)에서 지도 추가 학습과 동일  
기존언어모델은 각 태스크마다 별도의 지도 추가학습 및 모델 저장  
Idea : 모든 자연어 태스크는 텍스트 기반 지시와 응답으로 표현할 수 있지 않을까? (감정분석, 번역 예시)  
학습방법 : 주어진 입력을 받아서 이에 대한 응답을 따라하도록 지도 추가학습  
데이터에 따른게 아니라 '지시'의 형태를 학습하여 방법 자체를 학습하는 과정  
하나의 태스크 데이터를 지시데이터로 바꾸는 방법은 무한하게 가능하다. (정답이 없음)  
다양성 증대를 통해 더 강력한 학습능력을 발휘할 수 있음  
-> 실험결과 제로샷 형태에서도(프롬프트만 작성) 학습데이터 향상  
- 성능향상 핵심요소
1. 학습 태스크의 개수 : 다양한 종류의 지시 학습할수록 성능 향상
2. 추가학습하는 모델의 크기 : 특정규모 이하에서는 효과성 떨어짐
3. 지시를 주는 방법 : 자연어 지시로 사람에게 대화하듯이 가장 효과적
### 선호 학습(Preference Learning)
지시학습의 한계 : 주어진 입력에 대해 적절한 하나의 응답이 있다고 가정, 정해진 답의 경우 자연스러움  
but 정해지지 않은 개방형태스크(번역 등)에서는 한계 존재 -> 더 좋은 응답 생성 추구  
선호학습 : 다양한 응답 중 사람이 더 선호하는 응답을 생성하도록 추가학습  
* 다양한 응답은 모델이 생성, 응답간의 선호도는 사람이 제공  
ChatGPT 만드는 핵심 알고리즘. ChatGPT == InstructGPT + chat data

InstructGPT 핵심 아이디어 : 사람의 피드백을 통한 강화학습(RLHF)
* 사람의 피드백 = 응답에대한 선호도  

step 1. 지시 학습을 통한 텍스트파운데이션모델의 추가학습 : 다양한 지시입력 수집, 정답데이터 생성  
step 2. 사람의 선호 데이터를 수집하여, 보상 모델을 학습 : 선택지는 모델이 생성, 선호도는 사람이 생성  
 - 사람과 일치한 선호도를 출력하도록 모델 지도학습  

step 3. 보상이 높은 응답을 생성하도록 강화 학습을 통해 추가 학습  
핵심 : step1,2 에서 보지 못한 질문에 대해 사람의 추가적인 개입 없이 학습된 모델들을 통해 추가 학습이 진행  
-> 단순 프롬프팅이나 지시 학습에 비해 발전된 지시 수행능력  
기존대비 해로운응답과 거짓말(Hallucinations) 덜 생성하는 결과 - 안전하다  

## 거대 언어 모델의 추론
### 디코딩 알고리즘
거대언어모델의 자동회귀 생성  
* 학습이 완료된 거대 언어 모델은 어떻게 응답을 생성할까? -> 순차적 추론을 통한 "토큰별 생성"  

언제 추론 및 토큰 생성을 멈추고 응답을 제공? EOS토큰 생성 시 종료, 사전에 정의된 토큰 수 도달 시 종료  
EOS = End of sentence  
GOAL : 주어진 입력 $x$ = [$x_1$, $...$, $x_L$]에 대해 다음토큰 $x_{L+1}$ 생성  
* 거대언어모델 : 입력 x에대해 다음토큰에 대한 확률분포 $\hat{p}$($x$) 제공
* 디코딩 알고리즘 : $\hat{p}$($x$)로부터 $x_{L+1}$을 생성하는 알고리즘(다음 단어를 선택하는 방법)
디코딩 알고리즘 - 여러개 존재, 조절하는 여러개의 argument가 있다. 실제로는 널리 쓰이는 하나의 사례에 집중

1. Greedy decoding : 가장 확률이 높은 다음 토큰을 선택
2. Beam search : 확률이 높은 k개(beamsize)의 후보를 동시에 고려(빔 크기만큼 계산량 증가)
3. Sampling : 거대언어모델이 제공한 확률 기준으로 랜덤하게 생성
4. Sampling "with temperature" : 핵심파라미터 $T$를 통해 거대 언어 모델이 생성한 확률 분포를 임의로 조작
5. Top-k Sampling : 확률이 높은 K개 토큰들 중에서만 랜덤하게 확률에 따라 샘플링
6. Top-P Sampling(or Nucleus Sampling) : K를 고정하는 대신, 누적확률(P)에 집중하여 K를 자동으로 조절(누적해서 90%, 95% 등 기준설정)  

**디코딩 알고리즘별 장단점 요약 확인**
* ChatGPT 실제로 모델 설정을 하여(temperature, max tokens, Top P 등) 알고리즘 값을 간접적으로 제어 가능
### 프롬프트 엔지니어링
입력프롬프트 = 지시(instruction) + 예시(few-shot examples)  
원하는 답을 얻기 위해 모델에 주어지는 입력(프롬프트)을 설계.조정하는 기법  
지시 = 시스템프롬프트 + 유저 쿼리(질문)  
* 시스템 프롬프트 : 유저 쿼리와 무관하게 "기본값"으로 주어지는 LLM 입력(수정 가능/ 불가능 경우 다름)
  - LLM의 답변을 학습 이후에 "추가적으로" 컨트롤하기 위해
  - 활용 예제 : LLM 개인화(챗지피티 메모리 시스템), LLM 페르소나(가상 캐릭터 대화서비스)
* 유저 쿼리 : 감정 분류와 같은 쉬운 문제 뿐만 아니라 수학, 코딩과 같은 어려운 문제를 거대 언어 모델로 푸는 것에 많은 관심 집중  

Chain-of-Thought(CoT) 프롬프팅  
아이디어 : 단순히 질문과 응답만을 예시로 활용하는 것이 아니라, 추론 과정도 예시에 포함  
cot로 인한 성능 향상은 모델 크기가 커질 수록 더 확대됨(추론 ~= 창발성?)  
*창발성 : 모델크기가 커지며 갑자기 새로운 능력이 나타나는 현상*  
0-shot cot는 기존 0-shot 프롬프팅보다 훨씬 높은 성능 / 모델크기가 임계점을 넘어야 효과성 발휘  
추론능력 == 거대언어모델의 창발성 결과  
- 어떤 예시를 주느냐에 따라 LLM 성능이 크게 좌지우지됨
 * 해결책 : 풀고싶은 쿼리와 유사한 예시 활용(거리 기반 선택)  

프롬프트 엔지니어링 := "Skill.md"
 * 특정 task를 위해 엔지니어링된 프롬프트(공유가능, 사용자 정의 가능)

## 거대 언어 모델의 평가와 응용
### 거대 언어 모델의 평가
평가 : 구축한 시스템이 실제로 잘 동작하는지를 확인하는 단계  
3가지 요소 1. 목표, 2. 평가방법, 3. 평가 지표 (슬라이드 예시 - 배달어플 시간측정)  
AI 모델의 평가 : "테스트 데이터"
* 핵심 가정 : 학습단계에서 본적 없고, 질문과 정답을 알고 있음  
거대언어모델 평가의 특징 : 특정태스크에서 학습된 기존 AI모델들과 달리, 거대 언어 모델은 다양한 태스크에 대해 동시에 학습됨  
따라서 올바르게 평가하기 위해 많은 태스크에서의 성능을 종합적으로 판단해야함  
- 정답이 정해진 경우 : 예측과 정답을 비교하여 일치도를 측정(정확도)
- 정답이 정해지지 않은 경우
1. 사람이 임의의 정답을 작성 및 이와 예측을 비교
2. 정답과 무관하게 생성 텍스트 자체의 품질만을 측정
3. 생성된 텍스트의 "상대적 선호" 평가  
- 정해지지 않은 경우(ex 문서 요약, 스토리 생성)
1. 두 텍스트 간의 유사도 측정 - 단어 수준에서의 유사도 측정 : 정답, 모델, 겹친 단어 수 측정
                          - 벡터 공간 유사도(코사인 유사도 -1, 0, 1 계산)
2. Perplexity - 얼마나 문장이 확률적으로 자연스러운지 측정(언어모델 활용하여 다음단어 올 확률 계산)
- 단순한 등장 확률이 아니라, 여러가지 측면에서 평가를 하고 싶다면?
  - 기존 전문가 고용, -> 거대언어모델로 해당 역할을 대신 수행
  - LLM-as-judge(G-Eval) : 거대 언어 모델을 통해 생성 텍스트를 평가
  - GPT-4를 활용한 평가는 기존평가지표보다 더 사람과 유사한 결과를 보임
1. 생성 텍스트의 "상대적선호" 평가 : 대표 예시 - LMArena(실제 유저 피드백 활용, 성능측정방법 중 가장 좋은 방법 중 하나)  
 - 여론조작이나 신뢰도 낮은 데이터 수집에 대한 우려, 높은 평가비용과 시간 소요
 - G-Eval : 거대 언어 모델을 통해 생성 텍스트의 "상대적 선호"를 평가 가능
   한계점 * 위치편향 : 특정 위치 응답 상대적 선호
         * 길이편향 : 품질 무관 길이가 긴 응답 상대적 선호
         * 자기선호편향 : 생성모델이 평가 모델과 같은 경우, 선호  
보완법 : 위치편향 - 순서바꿔 두번 평가 후 평균, 길이편향 - 길이가 미치는 영향 통계적으로 제거
### 거대언어모델의 응용 및 한계
GPT-4o 의 o : omni (통합하여 텍스트, 이미지, 비디오, 오디오의 멀티 모달) 활용  
입력(이미지, 비디오, 오디오) 출력(오디오, 텍스트) 생성  
핵심아이디어 : 다른 모달리티 데이터를 거대 언어 모델이 이해할 수 있도록 토큰화 및 추가학습(구조 자체가 토큰화 아이디어)  
거대언어모델의 응용 : 합성데이터 생성  
Self-instruct : 175개의 데이터를 사람이 작성한 뒤, GPT-3를 통해 52000개의 합성 데이터 생성  
필터링 의의 : 합성데이터에서 중복데이터 제거, 모델이 더 폭넓은 지시문 학습이 가능케 instructon확보  
결과 -> 기존 InstructGPT에서 활용된 사람이 만든 데이터와 비슷한 성능 달성  
* Alpagasus : 프롬프팅을 통한 합성 데이터의 품질 평가 및 필터링 제안  
  의의 : 저품질 합성 데이터를 걸러내고 고품질만 학습해, 빠르고 강력한 성능 달성 목표  
  결과 : 전체 합성 데이터를 사용한 경우보다 높은 성능을 달성  

거대 언어 모델의 한계 : 환각(Hallucination) - 진위성 구별의 어려움  
* 사실과 다르거나, 전적으로 지어낸 내용임에도 불구하고 정확한 정보와 동일한 자신감과 유창함으로 응답 생성  
* 사전학습 데이터가 특정 시점까지의 정보만 포함하고 있어 환각 현상의 원인이 되기도 함

탈옥 - 프롬프트 엔지니어링을 통해 거대 언어 모델의 정렬을 우회할 수 있다는 것이 확인됨
- Do Anything Now(DAN) 프롬프팅
- 여러 단계의 학습 과정에서 기인한 근본적인 한계 때문에 발생했으며, 다양한 탈옥/방어 방법 탐구중
AI 텍스트 검출 : 거대언어모델 무분별 사용이 학교 및 회사에서 여러가지 새로운 문제 야기 중
- 어느정도 거대언어모델이 만든 텍스트를 구분/탐지 가능하다.

==========================
## GMS API 기본사용
폴더 내에 .env 파일 생성 후 GMS-Key 복사
Open AI API 중계(proxy)서비스
Open AI API서비스가 업계표준이 되어 openai 라이브러리 하나로 여러회사의 LLM 사용 가능
API 호출에 꼭 필요한 3가지 : 주소, 인증, 요청내용
openAI 두가지 방식 API
1. Chat completions API : 기존방식
2. Responses API : 권장방식(최신) 메서드, 입력, 지시파라미터 차이

프롬프트 입력시 기본 출력은 기존방식 출력가능성 높음  
에러 상태코드 (400번대 - 클라이언트 잘못)
- 401 : 인증실패 - 키 오타, 만료
- 400 : 잘못된 요청 - 모델 오류
- 404 : 대상없음
- 429 : 요청이 너무 많음
- 500/503 : 서버 오류

OpenAI developers - API 생성 및 재정의 관련 설정 도우미
스트리밍 응답 : 평소 우리가 web에서 사용하는 LLM의 순차적 답변 구조(빨라 보임)
* 조각조각 내서 첫 글자를 빨리 보이게하고 최종 출력시간은 동일
- 추론강도 조절
GPT-5 부터는 '추론 모델' - reasoning의 effort != temperature
temperature : 답을 고를 때의 무작위성을 조절 -> 다양성 달라짐
effort : 답을 내기 전 생각하는 양을 조절 -> 정확도와 비용이 달라짐
effort또한 높을수록 요금과 시간이 늘어난다. (생각하는 토큰도 과금 대상)
파라미터 -> 프롬프트 조절로 이동(프롬프트 엔지니어링 시초)

## JSON
딕셔너리처럼 키 : 값 형태의 데이터 형식   
프로그래밍 언어와 API통신에서 표준처럼 사용  
JSON responses 서로 옵션 이름 다르다, 중첩단계 다름  
Chat completions :  `{'type':'json_schema', 'json_schema': {...}}` 구조  
responses : `{'format': {'type':'json_schema', ...}}` 구조  
- 'Strict' : True 사용 시 형식을 보장 받을 수 있음  

스키마 작성 시 아래 조건을 **반드시** 지켜야 한다.  
* ① properties의 모든 키가 required에 들어가야 한다  
* ② additionalProperties 를 반드시 False 로 지정해야 한다
* ③ 스키마 name 에는 영문/숫자/언더바만 사용할 수 있다 (한글 불가)
### HTTPX(비동기통신)
여러 요청을 동시에 병렬로 처리하는 방식 * 빠르다  
핵심 키워드  
 * async def : 비동기 함수 선언
 * await : 완료까지 기다리되, 다른 비동기작업 계속 실행
 * asyncio.gather(*tasks) : 여러 비동기 작업 동시실행, 모든 결과 모아서 반환

주의점 : Rate limit, 하나 실패시 전체 종료(asyncio.gather), py/jpynb 문법 다름
- 여러개의 프롬프트를 요청하고 결과를 수령해야할 때 등장 예정

## 데이터 합성
실습 구조 1. 프롬프트 설계 2. 합성데이터 생성 3. LLM 채점 4. 선별
핵심개념  
1. **LLM 호출 함수화** — 재사용 가능한 API 호출 패턴
2. **프롬프팅 기법 비교** — Zero-shot, Few-shot, CoT의 차이를 체험
3. **구조화된 합성 데이터 생성** — JSON 스키마로 일관된 데이터 생성
4. **LLM as Judge 평가** — 생성 데이터의 품질을 LLM이 자동 평가
### 데이터 생성을 위한 prompt engineering
실제 수집한 데이터가 아닌, LLM을 통해 인공적으로 만들어낸 데이터  
합성과 증강의 차이  
* 합성 : 새로운 데아터 생성(소설 집필)
* 증강 : 기존 데이터를 변형하여 양을 늘림(소설의 문장순서 변경, 단어 교체)
증권사, 헬스케어 등 실제 데이터를 직접적으로 확인하기 어려운 분야에서 유용

한계 : 사실성 부족, 다양성 부족, 편향 증폭, 모델 붕괴

프롬프트 기본 구조  
LLM에게 명확한 가이드라인 제시 - 역할, 목표, 조건  

합성데이터 프롬프트의 핵심
1. 다양성 확보 : 모델이 매번 새롭고 다채로운 데이터를 생성하도록 무작위성 조절
temperature : 확률분포 모양, top_p : 후보단어 범위 조절하는 역할
 * 추론강도조절 - reasoning effort로 응답의 성격 조절
2. 일관성 확보(구조화 출력) : 데이터 구조를 항상 같도록 JSON 스키마 지정하여 출력형식 강제
 - 후속처리(DB저장, 모델학습 등) 자동화를 위해 필수적

프롬프팅 기법 정리
- Zero-shot : 예시 없이 그냥 지시
- Few-shot : 입출력 예시 2~3개 제공 - 예시로 패턴 보여주기
- Chain-of-Thought(CoT) : 생각 과정을 밖으로 꺼내기(중간추론과정 명시적으로 생성)
   * Zero-shot CoT : '단계별로 생각해봐' 한줄 추가
   * Few-shot CoT : 예시 자체에 풀이과정 담아서 제공  

Role-prompting : 역할 부여  
Self-Consistency : 여러번 풀고 다수결  
Self-Refine : 스스로 비판하고 고쳐쓰기  
Prompt Chaining : 작업 쪼개기(생성 -> 평가 구조도 일종의 체이닝)  
ReAct(Reasoning + Acting) : 생각과 행동을 번갈아  
*제공되는 GPT-5 nano는 이미 추론모델이므로 실습간 , 과제풀이간 CoT가 극적으로 효과가 나타나지 않을 수 있음*

### JSON파싱방법
text옵션(API 레벨) API파라미터로 JSON스키마 강제  
프롬프트에 JSON 형식 지정 + 수동 파싱  
* 파싱은 반드시 try / except로 감싼다  
JSON 양식 따라 구조화된 합성 데이터 생성  
- LLM as Judge : 다른 LLM을 통해 생성 데이터에 대한 평가  

구체적인 이유까지 생성하여 개선방향피드백도 받을 수 있다
평가 방식 : 점수형, 비교형, 정답대조(모범답안)
펑가 설계의 핵심 3가지
1. 평가 기준 설정 - 목적 명확
2. 일관성 확보 - 일관되고 객관적인 평가
3. 체계적인 평가 프롬프트 - 지시사항, 결과, 기준을 명시적으로 전달  
한계(편향) : 자기편향, 위치편향, 길이편향, 관대함편향  
temperature 대신 프롬프트를 통해 기준 정립 -> effort = low  
Rule을 넣어서 LLM as Judge의 변별력을 검증 - 평가 결과를 통해 준수 여부를 확인 하는가?  

### 핵심 정리 5가지

| # | 내용 |
|---|---|
| 1 | **합성은 무에서 유, 증강은 유에서 또 다른 유.** 데이터가 아예 없으면 합성이다 |
| 2 | **모호한 지시는 모호한 결과를 낳는다.** 역할·목표·조건을 구체적으로 |
| 3 | **형식은 부탁하는 게 아니라 강제한다.** JSON 스키마로 못 박는다 |
| 4 | **파라미터를 못 쓰면 프롬프트로 한다.** 다양성도, 일관성도 프롬프트로 확보 가능 |
| 5 | **LLM 채점은 1차 필터일 뿐이다.** 최종 판단과 표본 검수는 사람이 한다 |
#### 더 알아보기

**프롬프트 엔지니어링**
- 기법을 익히는 가장 좋은 방법은 **직접 바꿔가며 비교** 하는 것이다.
  오늘 실습 코드에서 프롬프트 한 줄씩 바꿔보고 결과가 어떻게 달라지는지 관찰해 보자.

**직접 해볼 만한 실험**

| 실험 | 방법 | 관찰할 것 |
|---|---|---|
| 장르 늘리기 | `genres` 리스트에 항목 추가 | 다양성이 실제로 늘어나는가 |
| RULE 바꾸기 | 말투 규칙을 다른 스타일로 | 채점 점수가 따라 움직이는가 |
| Few-shot 예시 수 | 예시를 1개 -> 5개로 | 형식 준수도가 계속 좋아지는가 |
| effort 비교 | 생성에 `low` / `high` 적용 | 품질 차이 vs 소요 시간 |
| 평가 기준 추가 | `criteria_list`에 기준 추가 | 어느 기준에서 점수가 낮은가 |
| 재평가 | 같은 셀을 두 번 실행 | 점수가 얼마나 흔들리는가 |
====================================
# 이미지 딥러닝 모델
이미지 기반 학습모델의 구조와 작동방식  
대표적 CNN 기반 이미지 모델의 변천사

## CNN 살펴보기
### CNN vs FCN
FCN : 완전 연결층(Fully-connected Layer)  
* 입력을 받아서 출력으로 변환하는 신경망의 기본 모듈  

FCN 변환 예시  
이미지 3차원 고차행렬 입력 ->  10x1의 1차원 벡터로 출력, 차수변환 (Flatten)  
모델 상수 $W$ : 모델이 학습해야 하는 파라미터  

합성곱 레이어  
* 입력 이미지를 필터와 연산하여 특징 맵(feature map)을 뽑아내는 모듈
* 1차원 구조로 변환하는 FCN과 달리 3차원 구조를 그대로 보존하면서 연산
* Convolution : 필터를 이미지 상에서 이동시키면서 내적을 반복수행, 내적으로 구한 모든 값을 출력 제공
    - CNN 이전부터 존재하던 수학적인 연산

특정위치에서 *필터*와 *동일 사이즈의 이미지 영역* 간 내적한 결과 값  
필요 연산은 (3*5*5=75)차원 내적 곱 + bias 벡터  
$$
w^T x + b
$$
* $x$: 필터가 덮는 이미지 영역  
* $w$: 필터의 가중치  
* $b$: 편향(보정값)

통상 여러개의 필터가 있다고 가정, 활성화 맵 - n개  
출력해상도 = 입력해상도 - 필터해상도 + 1  
입출력 해상도를 유지하고 싶다면
* 출력값 중 정의되지 않은 경우, 0 혹은 가장 가까운 출력값으로 대체/참조(패딩) 

nx1 벡터로 구성된 바이어스 추가  
배치 사이즈 수식 맨 앞값 새로운 숫자 -> 출력 배치 사이즈 비례  
- N * $C_in$ * H * W 입력(이미지)배치 -> N * $C_out$ * H' * W'
### 모델 구조
모델 상수(파라미터)를 증가시키면? 레이어 중첩하여 상수 증가  
비선형 블록과 함께하면 모델링 파워 향상 : Conv + ReLU  
필터 시각화
- 학습된 필터 시각화를 통해 각 모델(구조)이 학습한 정보를 이해 가능
  * 선형모델 : 필터는 각 카테고리의 템플렛(전형적모습) 역할 $y= W_x$
  * FCN : 다양한 템플렛 제공
  * CNN : 계층별로 이미지의 지역적 템플렛(예: 엣지 색상 등)

중첩과 수용 영역(Receptice Field)
* CNN 레이어는 이미지 작은 부분인 '지역 정보'추출에 유리하게 설계
* 이미지 활용 다양한 작업에서 전체(막랙) 의미 파악이 필요
  * 지역정보 + 이미지 전체 맥락 이해/활용하기 위한 설계 필요.
* 수용영역이란?
  * CNN이 이미지 처리하면서 한 번에 볼 수 있는 영역의 크기 / 네트워크의 시야
* 고해상도 이미지 처리시, 많은 레이어를 통과해야함.
연산, 비용 학습가능성 고려시 비 실용적
* 해법 : 입력 사이즈를 줄여 모델에 입력

풀링 : 효율적 연산 및 위치 변화의 강건성 확보
* CNN 레이어의 출력을 줄여 연산효율성 확보 - 입력 크기 감소
* 맥스풀링 : 정해진 커널사이즈(2*2)로 이미지를 나누어 각 영역 내 가장 큰 값을 선택하는 연산

스트라이드 합성곱 : 풀링 한계 개선
- 일반 합성곱 : 필터 1칸씩 이동하면서 연산 수행(출력값 계산)
- 스트라이드 합성곱 : 필터를 스트라이드 값만큼 (S칸) 이동한 후 출력 연산
- 효과 : 풀링 처럼 입력 해상도를 줄임(연산 효율과 위치 강건성)
풀링은 학습상수가없지만, 스트라이드는 커널을 동시에 학습

## CNN 기반 모델 변천사
2010~ 2017 모델 변천사 shallow - 8 layers - 22 layers 순 발전
### AlexNet
5개의 합성곱 계층과 3개의 완전연결 계층으로 구성된 8계층 CNN 모델  
특징
* 5개 합성곱레이어
* 맥스 풀링
* 3개 연결층 레이어
* ReLU 비선형 활성화

입출력 
- 입력 : 이미지 / 해상도
- 출력 : 레이블 / 벡터

Image input 224, 2*GPU : 227로 추후 정정, 당시 메모리 부족으로 2개 GPU 병렬 사용  
Image input 227, 1*GPU : 최신 GPU - 24~80GB VRAM 보유  
* AlexNet - 초기연구이므로 경험적 상수로 계산이 애매한 경우 o  
convolution layer : 특징 추출을 위해 단계적으로 중첩 사용 

최종 풀링층 -> 완전 연결층(FC Layers) -> 출력층(Output)  
출력층 : Softmax(1000개 클래스) 1024 - 24 , 1000개 클래스 확률분포 출력  

AlexNet 출력 사이즈 계산법
* 출력 채널 수 = 현재 레이어의 필터(커널) 개수  
* 출력 해상도 $W' = \frac{W - K + 2P}{S} + 1$

AlexNet 연산비용 계산법
* 출력 활성 메모리(KB) = $C \times H' \times W' = 96 \times 55 \times 55 = 290,400$  
* 출력값 메모리 사용 4 Byte $\rightarrow$ $4\text{ Byte} \times 290,400 = 1,161,600\text{ Byte} = 1,134\text{ KB}$
상수 k(파라미터 수)  
총 개수 = 출력 채널 수 x 입력 채널 수 x $K^2$(필터 웨이트) + 출력 채널 수(바이어스 웨이트)  
96x3x11x11+96 =34944  
FLOPs(연산량) = 총 연산 (곱하기만 고려) = 출력 원소 수 $\times$ 출력 원소별 연산  
  $= (H' \times W') \times (C_{\text{in}} \times K \times K \times C_{\text{out}})$  
  $\rightarrow (55 \times 55) \times (3 \times 11 \times 11 \times 96) \times 2 = 105,415,200 \text{ (105.4M FLOPs)}$

메모리 사용은 초기 레이어에 집중(conv1,2)  
Params - 모델 상수(K)는 연결층 레이어(FC)에 집중
FLOPs - 합성곱에서(conv2,4) 주로 발생  
### VGGNet
5개의 합성곱 블록 + 맥스 풀링 구조  
VGG-16 기준  
특징 
- 5개의 합성곱 블록
- 각 블록당 맥스 풀링
- 3개 연결층 레이어
- ReLU 비선형 활성화
- 총 16개의 합성곱/연결층 레이어
- 최종 소프트맥스 수행

AlexNet과의 차이  
필터 3x3 고정, 단계적인 채널사이즈 64 -> 2의 배수 증가  
* 작고 단순한 필터를 깊게 쌓으면 성능 향상 -> 새로운 설계 철학
* 3x3 합성곱/스트라이드1/패딩1 반복 적용
* 맥스풀링으로 크기 줄이기, 풀링 후, 채널 크기 2배 적용
  - 3x3 합성곱 두개 연달아 적용하면 5x5 합성곱과 동일 리셉티브 필드 확보  

| Option 1: | Option 2: |
| :--- | :--- |
| 합성곱($5 \times 5, C \rightarrow C$) | 합성곱($3 \times 3, C \rightarrow C$)<br>합성곱($3 \times 3, C \rightarrow C$) |
| Params: $25C^2$<br>FLOPs: $25C^2HW$ | Params: $18C^2$<br>FLOPs: $18C^2HW$ |  
alexnet에서는 레이어별 연산량 차이 큼. VGG 해상도 줄일 때마다 채널늘려 레이어별 연산량 차이 줄임

VGG: alexnet 메모리 약 25배  
모델상수 약 2.3배 연산량(FLOPs) 약 19.4배

VGG 모델 특징
* 단순함 + 깊이의 강력한 성능을 보임
* 단순 설계로 모델 해석에 이상적
* 특징추출기, 전이학습에 강력한 베이스라인
  * 파라미터가 많고 연산량 요구가 매우 큼
### ResNet
합성 블록과 잔차 블록 구조  
잔차블록은 블록입력을 그대로 출력에 더해주는 지름길 연결이 특징  
Inception 모델에서 차원 축소로 활용된 1x1 합성곱을 적용, 연산효율 개선
등장 배경
* 배치정규화 방식으로 10+ 레이어 학습 가능
  * 20-레이어 모델 : 학습이 안정적, 에러 감소
  * 56-레이어 모델 : 더 깊음에도 불구, training error 오히려 높음
* 깊다고 무조건 더 잘 학습되는 것은 아님
-> 단순히 레이어를 깊게하면 오히려 성능 하락

작은 모델의 성능은 "최소한" 누릴 수 있도록 작은 레이어의 추정값을 그대로 후속 레이어에 제공하여 최소한 작은 레이어 수준의 성능 보장  
-> Residual Block의 핵심 아이디어  
입력 X가 두 경로로 나뉘어 전달됨
1) 변환 경로 ($F(x)$)  
   $\rightarrow \text{Conv}(3 \times 3) \rightarrow \text{ReLU} \rightarrow \text{Conv}(3 \times 3) \rightarrow \text{ReLU} \rightarrow \text{출력 } F(x)$  

2) Shortcut 경로 (Identity mapping, 잔차 연결)  
   $\rightarrow \text{입력 } X\text{를 그대로 전달}$  
마지막에 두 값을 더함 : Output = F(x) + x  
학습이 잘 안되면 0으로 초기화하여 출력 x값 보장  
연산 효율을 위해 보틀넥 잔차 구조를 도입  
* FLOPs 비교 : $18HWC^2 \text{ vs } 17HWC^2$
3x3 2개를 3x3 1개에 앞뒤배치로 3개레이어로 변환
보틀넥 잔차구조 : 1x1 conv를 앞 뒤에 배치하여 입력과 출력의 사이즈를 동일하게 하여 잔차블록 형식 유지  
더 깊은 모델을 더 적은연산으로 달성 가능  

Stem 구조
* 기존 모델의 효율성 레시피를 잘 활용
  1. 스템 구조를 모델 초기에 도입, 입구 데이터 해상도를 1/4로 줄이고 이후 잔차블록 적용
  2. 여러개의 FC레이어 제거, 전역 평균 풀링 사용 -> 마지막 1개 FC적용
전역평균풀링 : 특성 맵(Feature Map)의 각 채널별 전체 픽셀 값들의 평균을 구해 채널당 하나의 값으로 압축  
VGG-16, Res 18, Res 34 비교  
* vgg : 성능은 유사, 연산량 high
* Res 18 : 얕고 가벼움, VGG보다 성능 약간 낮음
* Res 34 : 성능 high, VGG보다 정확 및 가벼움

Res 34 vs 50 : 보틀넥 잔차 블록 사용 여부의 차이  
50은 현재도 사용되는 CNN기반 베이스라인(기준 모델)  
최종 요약 : ResNet
* 깊은 모델 학습을 위한 중요한 전진(100+ 레이어 학습 가능)
* 깊은 모델의 강력함을 다시확인
* 제안 당시 모든 벤치마크에서 최고성능 달성
* 지금도 CNN기반 구조 중 가장 활발하게 활용
### MobileNet
모바일/임베디드 환경에서 구동 가능  
기존 합성곱은 공간과 채널을 동시처리하여 연산량 과도  
Mobilenet 핵심 아이디어 : 공간과 채널을 두 단계로 분리하여 처리
- 깊이별 합성곱 : 각 채널별 독립적 3x3합성곱 수행
- 화소별 합성곱 : 1x1 합성곱을 채널 방향으로 적용  

효과 
- 연산량 기존 대비 9배 가량 절감
- 모델 상수 대폭 감소

깊이별 합성곱 : 그룹은 유지하면서 각 깊이별로 채널 곱, 해상도 안에서 섞인다.  
화소별 합성곱 : 깊이 단계에서 곱, 화소 내부단계에서 곱셈  
각각의 연산을 나누고 최종에서는 깊이, 화소의 특징을 모두 구현하도록 접근  
$$
\begin{aligned}
\text{연산량 절감 비율} &= \frac{9C^2HW}{9CHW + C^2HW} \\
&= \frac{9C}{9 + C} \\
&\rightarrow 9 \quad (\text{as } C \rightarrow \infty)
\end{aligned}
$$
C가 무한으로 커질 경우 연산량 절감의 효율이 크게 증가할 수 있다.(9배)  
이후 모델 영향
* 가볍고 빠른 모델, 엣지디바이스(모바일 등)에서 딥러닝 모델 실시간 실행 가능성
* 효율형 모델 구조의 표준 제시 : 깊이별 합성곱, 보틀넥 구조 이후 경량모델에서 지속적 채택

===========================================
# 다양한 신경망 모델
학습목표 :  
CNN의 한계를 이해, 이미지인식에서 왜 새로운 접근이 필요한지 확인  
RNN, Attention 기반 Transformer의 핵심 아이디어와 구조를 파악, 모델이 해결하는 문제 이해  
모델별 설계 철학, 특징 비교, 데이터유형에따른 적절한 모델 선택 가능

## CNN의 한계
### CNN의 구조 및 장점
합성곱 구조 특성 :  
* 지역적인 특징 학습에 특화 : 작은필터로 학습
* 파라미터 효율성 우수 : 지역적 특징 추출 시 연산량 절감
* 위치변화와 노이즈에 견고 : 합성곱 - 세부차이 감소, 전체의미 집중
* 표준모델로 활약
### CNN 구조의 한계
- 데이터 순서를 무시
  - 순차적 데이터의 순서정보(텍스트/음성의 시간 흐름등)를 반영하기 어려움
- 긴 거리 의존성 고려 부족
  - CNN은 국소적 특징 추출에 강하지만, 이미지나 문장에서 멀리 떨어진 요소간의 관계를 잘 학습 못함
- 픽셀 단위 복원 한계
  - CNN은 이미지 전체의 정보를 압축(라벨)에 효과적이지만 위치정보 손실(pooling, stride 효과)

## 시퀀스 데이터 처리 : RNN
### RNN(Recurrent Neural Nets)
순차적 데이터를 처리하기위해 고안된 신경망 구조  
시계열/언어데이터 적합 : 문장, 음성신호, 센서데이터 등 시계열 데이터 처리에 관한 강력한 귀납 편향 제공  
-> input chars - input layer - hidden layer - output layer - Target chars   
RNN 셀 : 은닉층에서 활성화 함수를 통해 결과를 내보내는 역할  
 - 은닉 상태 : 메모리셀이 출력층 방향 또는 다음 시점이 t+1의 자신에게 보내는 값  

특징 : 시계열 의존성 - $h_t$가 $h_{t-1}$에 의존  
순수(vanilla RNN)
선형변환, 바이어스를 모델상수/비선형 (tanh)함수 추가로 구성된 단순한 형태
$$
h_t = \tanh(W_{hh} h_{t-1} + W_{xh} x_t)
$$
출력 계산 $y_t$ 최종출력 생성  
$$
y_t = W_{hy} h_t
$$
입출력 구조
- One-to-Many : 하나의입력에서 시간축으로 출력이 생성
- Many-to-One : 여러 시점 입력이 하나의 출력으로 압축
- Many-to-Many : 입력과 출력이 시간축을 따라 1:1혹은 n:m으로 맵핑
### 단순 RNN 한계
기울기 폭발 혹은 소실 발생  
- |W| 의 특이값 높음 - 곱할수록 무한히 커짐 -> 기울기 폭발  
- |W| 의 특이값 낮음 - 곱할수록 0에 수렴 -> 기울기 소실
- 이상적으로 W의 특이값 1이면 온전한 학습 가능
  - 특이값 : 행렬이 벡터를 얼마나 확장시키는지 크기로 나타낸 값 = 반복해서 곱할 때 출력이 터지거나 사라지는지 결정하는 값
폭발시에는 절삭 - but 기울기 소실은 해결 어려움
### 대안 모델
LSTM(Long Short-Term Memory) : 셀 상태, 게이트로 구성  
- 셀 상태 : 중요한 정보를 유지/기억하는 역할
- 게이트 : 정보의 흐름을 제어(스위치) - 선택적
- 구동철학 : 긴 시퀀스 데이터에서 필요한 정보는 유지, 불필요한 정보는 지워 안정적 학습 유도
$$
\begin{pmatrix} i \\ f \\ o \\ g \end{pmatrix} = \begin{pmatrix} \sigma \\ \sigma \\ \sigma \\ \tanh \end{pmatrix} W \begin{pmatrix} h_{t-1} \\ x_t \end{pmatrix}
$$
$$
\begin{aligned}
c_t &= f \odot c_{t-1} + i \odot g \quad && (\text{셀 상태}) \\
h_t &= o \odot \tanh(c_t) \quad && (\text{은닉 상태})
\end{aligned}
$$
게이트 : 입력게이트, 망각게이트, 출력게이트로 구성  
기울기 전파 시 선형가중치의 곱 이외에 덧셈 형태로 전달하여 기울기를 장기적으로 보존
* 각 상태에서의 기울기를 덧셈과 곱셈 경로로 분리 가능(소실 방지)  
but 정보 희석 문제의 한계 : 망각게이트가 과거의 정보를 조금씩 줄임 - 과거정보 약화  

## 긴거리 의존성 : 어텐션/ViT
### 어텐션 메커니즘
이미지를 통해 어텐션 이해
* 픽셀 거리와 상관없이 유사한 패치가 이미지에 존재
  * 패치 : 이미지를 일정 크기로 잘라낸 작은 조각 단위
* 관련 높은 패치/관련 낮은 패치 정보를 최종 결정에 반영
 - 쿼리 : 집중대상
 - 키 : 다른 패치
 - 값 : 키 패치 정보
 - 어텐션 : 쿼리, 키 유사도(확률분포)
 - 출력 : 어텐션으로 가감된 값의 총합
$$
\begin{aligned}
q &= \mathbf{x} W_q \\
k &= \mathbf{x} W_k \\
v &= \mathbf{x} W_v
\end{aligned}
$$

$$
a_{ij} = \text{softmax}\left( \frac{q_j \cdot k_i}{\sqrt{D}} \right)
$$

$$
y_j = \sum_{i} a_{ij} v_i
$$
* 쿼리와 키가 유사하면 해당 값에 가중치 높게

셀프 어텐션 :
- 하나의 입력 안에서 패치정보(쿼리/키/값) 정의
- 같은 이미지 내 유사도 반영
- 입력 내부 패치간 연결망 구축 -> 클러스터링 효과(모델의 특징파악률 상승)

교차 어텐션(Cross-attention) : 
- 둘 이상의 이종데이터에서 패치정보 정의
- 이종 데이터 간 유사도 반영, 서로다른 입력간 연결망 구축
### ViT(Vision Transformer) 위치 인코딩
모델별 특성 정리
- CNN : 지역적 특성, 장거리 맥락 이해 떨어짐, 데이터순서 반영 X
- RNN : 데이터 순서 반영, 장거리 맥락 학습 어려움(기울기 소실/폭발)
- LSTM : 데이터 순서 반영, 장거리 맥락 이해 가능, 정보 희석(시간서순)
- Attention : 장거리 맥락에 탁월, 정보희석 없음, 순서반영 가능?

위치 인코딩 : 패치순서(위치정보) 제공
- 각 입력 토큰에 위치 정보를 담은 벡터 추가

학습 가능한 위치 인코딩 
- 데이터에 알맞게 최적화
  - 장 : 해상도가 바뀌어도 동일한 룰, 단 : 해상도가 바뀌면 재학습 필요

사인파 인코딩 : 데이터 최적화
- 장점 학습과 동일 , 단 : 고정이라 데이터/과업에 특화된 방식이 아님

상대위치 인코딩 : 절대위치가 아닌 상대적 위치 인코딩
- 장 : 해상도 변화에도 적용 가능, 단 : 절대적 위치 고려가 어려움

회전 포지션 임베딩(2D RoPE)(교재 X) : 위치벡터를 더하지 않고, Q/K에 회전행렬 적용
- 상대 위치 표현(해상도 가변), 단 : 연산량 추가(매번회전연산), 절대 위치 소실

인코더 내부 구조 
- 정규화 - 다중헤드 어텐션 - 정규화 - 연결층으로 구성
- 인코더 L개 통과(일반적으로 12개 up)

전역 관계 학습
- CNN : 지역적 영역에서 수용 영역을 넓혀 전역 이해
- ViT : 수용영역을 넓히지 않아도 전역 맥락 이해 가능

최종 출력 : MLP head - 최종예측 수행  
ViT vs ResNet  
ImageNet의 경우 ResNet 성능이 더 우수,   
사전학습이후 미세조정한 경우, ViT도 우수  
ViT는 거대 데이터 세트 사전학습 시 우수
* 300M개의 데이터세트 - 사전학습 데이터 셋이 커질수록 성능이 개선을 보이기 시작
표 B,L,H - 모델크기 / 패치사이즈는 작을수록 큰 모델 의미  
*ViT 학습 기술 중요*
- 막대한 상수 보유
- 정규화 기법/데이터 증강 기술(좌우 변환, 슬라이싱 등) 매우 중요
  * 약한정도의 증강은 거의 항상 성능 향상에 도움이 된다.

증류학습 [Knowledge Distillation = 지식 증류]기술 효과적
* 학습 방식
  * 1단계 : "선생님 모델"(CNN 모델)을 이미지-레이블 활용해서 학습
  * 2단계 : "학생 ViT모델"은 선생님 모델 예측 따라가도록 학습(정답과 함께)  
크로스 엔트로피 손실 : 원핫 의 예측을 구현에 손실
KL 발산 손실 :  실제 데이터의 확률 분포 P와 모델이 예측한 확률 분포 Q의 차이를 측정하는 손실 함수  
* 강아지, 고양이 이미지 분석 - 이미지 관계성 정보 CNN -> ViT 학습
학생 ViT 모델은 학습 가능한 토큰을 추가로 입력받아, 선생님 CNN의 logit 출력과 동일하게 예측하도록 학습됨
#### ViT 트렌드
다양한 변종 존재 
- CNN처럼 이미지의 계층적 이해를 활용할 수 없을까?
  - Swin Transformer : 이미지 전역에 직접 어텐션 수행하는 것은 너무 고비용, 비효율
    1. 윈도우 영역을 지정, 그 내부의 어텐션에 집중
    2. 윈도우를 비껴가게 한번 더 설정 

Vision 백본 vs 데이터 확장성 vs 멀티모달 모델
- Vision 백본 : ViT-22B(220억개 상수 사용) - 구글 리서치
  - 지도학습 채택 : 대규모 비공개 레이블 데이터 기반(JFT-3B)
  - 일반화 성능이 우수/모델 상수 비공개(재현불가)
- 자기지도 Vision 백본 : DINOv3(최대 70억 상수) - 메타AI
  - 공개 웹 데이터 기반 17억장 대규모 데이터
  - 자기지도 학습 : 대조학습/증류 + 보간 기법
  - 가중치 공개, 최고의 조밀한 피쳐맵 제공

ViT 장점 :
* 전역 문맥을 한번에 고려 가능
* 시계열/시퀀스 데이터의 경우, 순서 고려 가능(위치 인코딩)

단점 : 
- 대규모 학습 자원 필요
- 학습 데이터 규모가 작을 경우, CNN보다 성능 저하
- 한번에 고려할 수 있는 토큰이 제한적

활용 : 
- 이미지 분류/탐지/분할/생성에서 모두 SoTA 성능 달성 * Sota : state-of-the-Art
- 멀티모달 기준에서 기준 아키텍쳐
- 기준모델에 활용중

# 이미지 모델 학습 전략
딥러닝 모델 학습의 안정성/효율성/일반화 성능을 높이기 위해, 모델 구조의 개선 못지 않게 학습 방법론이 중요한 요소임을 이해  
모델 학습 전/과정에 중요한 일련의 전략을 습득

## 학습 전략의 중요성
### 모델 구조
CNN의 구조와 발전 / CNN의 한계
### 좋은모델구조 = 우수한 성능?
좋은 구조만으로는 성능 보장 불가!  
- 동일한 구조, 동일한 데이터셋을 사용해도 성능차이가 크게 남
- 이유 :학습과정에서 발생하는 불안정성, 과적합, 수렴 속도 문제

학습에서 자주 겪는 문제들
* 학습 불안정 : 손실폭발, 학습멈춤, 수렴실패
* 과적합 : 훈련 데이터에는 맞지만 검증, 실전 성능 저하
* 느린 수렴 : 최적점에 도달하기까지 불필요하게 많은 에폭 소모로 인한 학습 효율 저하
### 학습 전략
구조 설계만큼 중요한 훈련 전략 제시  
적용시 기대효과 : 일반화 성능 + 학습 효율성 확보  
구조만으로 불충분 -> 훈련 전략 필요

## 모델 구성 : 악마는 디테일에..
### 활성화 함수
입력신호의 총합을 출력 신호로 변환하는 함수
* 예시 : 뉴런이 '켜질지/얼마나 반응할지'를 결정하는 스위치
역할 : 신경망에 비선형성 부여 -> 복잡한 패턴 학습 가능

활성화 함수가 없다면? 단순 선형모델과 동일  
Sigmoid : 0-1 사이 값으로 출력 값 제어, 확률 값처럼 해석 가능(자연로그 $e$ 활용 수식)  
문제
1. 입력이 매우 크거나 매우 작으면 출력이 거의 0이나 1 고정 -> 역전파 전달 X
2. 출력범위는 항상 양수 -> 편향된 업데이트 발생
3. 계산식의 지수함수 -> 계산비용 비교적 크다  

Tanh : 출력범위 [-1, 1] 데이터가 중심을 기준으로 대칭 -> 안정성 높음
문제 
1. 기울기 소실 문제 여전 -> 학습 정체
2. exp 연산 포함 -> 계산 비용 커짐
$$
\tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}
$$

ReLU(Rectified Linear Unit) : $f(x) = max(0,x)$  
장점
- 양의 영역에서 포화 x -> 기울기 소실 문제 크게 줄어듦
- 계산 효율성 높음 
- 학습속도 빠름 -> 시그모이드 tanh대비 6배이상 빠른수렴  

단점
- 죽은 뉴런 문제
- 0을 기준으로 비대칭 -> 왜곡 존재

Leaky ReLu :  $f(x) = max(0.01x,x)$   
장점 : 
- ReLU의 모든 장점 수용  
- 죽지 않는 뉴런(Dead ReLU)

문제점 
- 음수 영역 기울기값 설정의 임의성(여전히 너무작으면 소실, 크면 출력 왜곡 가능)
- 0 중심 대칭이 아님(한쪽 치우치는 경향 발생 가능)
- 항상 ReLU보다 우세하지 않음(일부문제 성능차이 거의 없음)  

Exponential Linear Unit (ELU)
$$
f(x) = \begin{cases}
x & \text{if } x > 0 \\
\alpha(\exp(x) - 1) & \text{if } x \le 0
\end{cases}
$$

장점 
- ReLU의 모든 장점 수용
- 평균 출력이 0 근처에 위치
- 음수 영역에서의 포화 구간 제공 -> 기울기 소실 및 왜곡 방어

문제점 
- 계산 복잡성 증가
- 하이퍼 파라미터 $a$ 설정 필요 -> 음수영역 기울기의 크기를 결정하는 $a$값에 따라 성능차이

결론 : 큰 고민 말고, 일단 ReLU부터 시도  
-> 성능 경쟁 중이라면 개선판 시도
### 데이터 전처리
데이터 형식 통일 : 이미지 조건을 일치
- 학습/검증/테스트에 모두 동일하게 적용
- 일반적 정규화 방식(평균 0, 표준편차 1로 정규화)

모델별 예시
AlexNet : 평균 이미지 빼기  
VGG : 채널별 평균 빼기  
ResNet : 평균빼기 + 채널별 표준 편차로 나누기
### 모델 상수 초기화
아이디어 1 : 0으로 초기화
- 모든 가중치와 편향을 0으로 초기화하면?
  - 모든 출력은 0 출력 동일, 기울기 동일 -> 학습이 이뤄지지 않음

아이디어 2 : 임의(랜덤 초기화)
작은랜덤 숫자(0.01곱 -> 0.01 분산) : Gaussian 분포를 따름, 0을 중심으로.
- Gaussian 분포 : 종 모양의 정규분포로 평균 0 근처 값이 가장 자주 발생 -> 값이 너무 크거나 작아지는 걸 막을 수 있음
- 깊지 않은 모델에서 동작하는 전략
- 깊은모델 : 작은 수에 분포할 경우 역전파 기울기를 구하는 과정에서 W를 계속 곱하면서 기울기 0으로 수렴
  - 상수가 크면 기울기 폭발

아이디어 3 : 자비에(Xavier) 초기화
- 가중치 초기 분포의 분산을 입력 차원으로 맞춤 std = 1/sqrt(Din)
- 분산을 입력 차원(뉴런수) 증가를 고려하여 설계
  -  모델상수 분산이 입력 차원과 같으면, 선형연산 이후 출력의 분산 = 입력의 분산임을 보일 수 있음

아이디어 4 : 허(He) 초기화
- 자비에 초기화 + ReLU의 궁합
  - 가중치 초기 분포의 분산을 입력 차원으로 맞춤.  
- 자비에에서 입출력 분산을 맞추는 전략은 입출력이 대칭적 분포를 보인다는 가정 하에서 설계
- 가중치 초기 분포의 분산을 2*입력차원으로 맞춤 std = sqrt(2/Din)
- 비대칭, 유사 분산이 레이어 후반에도 유지됨

ResiNet의 등장 배경
- 잔차 연결을 사용해 매우 깊은 신경망도 안정적으로 학습할 수 있는 구조
- 깊은 모델의 문제점
  - 깊어질 수록 기울기 소실/ 폭발 문제 발생 -> 학습 어려워짐 (ResiNet을 이용해 완화)

Resinet 철학 - 전층의 기울기 전달  
- 깊은층에서도 기울기가 끊기지 않고 전달 
- 초반 초기화가 잘못되면, 후반층 출력 분포값 계속 증폭할 수 있음.
- Resinet은 ReLU 사용구조이므로 He초기화 사용, Var(x+F(x)) = Var(x) 유도
  - 초반에는 skip의존, 초기엔 입력 = 출력, 분포 안정 -> 학습 안전하게 출발
### 모델 정규화
정규화를 통해 오차를 줄여보자
- 정규화 : 모델 복잡도를 제어해 훈련 성능과 검증 성능의 차이를 줄이는 방법
- 검증 오차 : 모델이 학습하지 않은 새로운 데이터에서 보이는 실제 성능 지표

아이디어 1 : 가중치 감소  
학습 과정에서 특정 가중치가 지나치게 커지는 현상 방지
- 너무 크면 입력 과도반응 -> 훈련데이터 외워버림
- 가중치 크기에 비례한 패널티
- 큰 가중치는 과적합을 부른다 -> 줄여주자
- 가중치 감소 완벽하게 만족
  - 모든 가중치 0인 모델 -> 학습 X
  - 원본 모델의 학습 목표와 함께 동작할 때 의미가 있음

L2 정규화  
- 작동방식 : 큰 가중치에 제곱으로 패널티 -> 극단적으로 큰 값 방지
- 결과 : 모든 가중치가 골고루 작아짐
$$
R(W) = \sum_k \sum_l W_{k,l}^2
$$

L1 정규화
- 작동방식 : 모든 가중치에 절댓값 크기만큼 동일하게 패널티
- 결과 : 중요하지 않은 가중치는 완전히 0이 됨(희소)
- 장점 : 자동으로 중요한 특성만 선택 -> 더 단순한 모델
$$
R(W) = \sum_k \sum_l |W_{k,l}|
$$

Elastic net(L1+L2)  
- L1과 L2의 장점을 합친 방식 : 특성선택 + 안정적 학습
- 변수가 많고 상관관계가 높을 때 특히 효과적
$$
R(W) = \sum_k \sum_l \beta W_{k,l}^2 + |W_{k,l}|
$$

아이디어 2 : 드롭아웃  
- 학습과정에서 일부 뉴런을 확률적으로 끔(0으로)
- 매 학습 스텝마다 다른 네트워크 구조가 샘플링되는 효과
- 직관 : 무작위로 뉴런을 비워서 특정 뉴런/특징에 과도하게 의존하지 않게 함
장점 
- 과적합 방지
- 여러 작은 모델을 합친 것과 비슷한 효과
- 간단, 매우 강력
단점
- 학습시간 증가
- 최적의 비율 찾는것 중요
- 모델에 따라 비선호

실행단계 동작 - 뉴런 확률적으로 끔.  
문제점 - > 성능저하  
- Inverted 드롭아웃
  - 학습 시 살아남은 뉴런의 출력을 1/(1-p) 배 스케일링

실행단계 해결책 - 기존 방식
(1-p)를 출력에 곱하여 평균치를 맞춤.  
단점 : 테스트 시 추가 연산 필요

## 학습 안정성 전략
### 학습 비율 조정
학습 비율을 어떻게 선정해야 할까?  
기본적 전략 : 학습 비율을 큰 값에서 시작하여 epoch 지날 수록 작은 값으로 조정  
Good learning rate의 경우, Loss가 안정적으로 감소

학습률 계단식 변경  
일정 에폭이 지날 때 마다 계단식으로 줄이는 방식  
- 에폭 : 학습 데이터를 1번 다 외움을 의미함
- K 에폭마다 감소계수 * 초기학습률 만큼 감소하는 전략

학습률 코사인 변경  
계단형 방식은 변경지점을 여러개 선정, 복잡하다는 단점  
코사인 파형에 따른 변경 
- 학습률을 코사인 함수 곡선처럼 점점 줄여가는 방식
- 직관 : 처음엔 크게, 나중엔 미세하게 조율
$$
\eta_t = \eta_0 \cdot \frac{1}{2}\left( 1 + \cos\left( \frac{\pi t}{T} \right) \right)
$$

학습률 선형 변경  
직선을 따라 변경
- 학습률을 학습이 진행될수록 선형(직선)으로 줄여나가는 방식
- 직관 : 처음엔 크게 배우고, 끝날수록 일정하게 줄여 수렴
- 사전학습/미세조정에서 주로 쓰임
$$
\eta_t = \eta_0 \cdot \left( 1 - \frac{t}{T} \right)
$$

그 외 다양한 감소 방식  
역 제곱 꼴, 일정값 유지

학습 종료   
과적합 방지를 위한 또다른방법
- 빠른종료 기법 : 학습 진행하며 성능이 나빠지기 시작하면 학습 조기종료
### 하이퍼 파라미터 선정
파라미터 vs 하이퍼파라미터  
파라미터 
- 학습을 통해 모델 스스로 얻는 값

하이퍼파라미터
- 학습 전에 사용자가 정해야 하는 값
- 학습 진행동안 고정됨

주요 하이퍼파라미터
- 학습관련 : 학습률, 배치사이즈, 에폭 수(조기종료)
- 최적화 관련 : 최적화 툴 선택, 모멘텀/최적화 계수, 가중치 감소
- 모델 구조 관련 : 네트워크 깊이, 채널 수/드롭아웃 비율, 정규화(BN, LN, GN 등)

하이퍼파라미터 값 탐색
- 그리드 탐색 : 후보들을 격자처럼 전부 조합
- 랜덤 탐색 : 무작위로 값 선택해 탐색 / 불필요한 축 탐색 x, 시간 절약

하이퍼파라미터 선택 비법  
- 모든 조합을 다 실험할 수 없다면
  - 빅테크조차 모든 조합 실험 x
  - 몇 가지 규칙과 직관을 활용하면 GPU가 많지 않아도 좋은 성능 도출 가능

- 체크리스트 1 : 데이터 입력 등 문제가 없을까? 
  - 초기 손실값 확인
- 체크리스트 2 : 학습률과 초기값 확인
  - 작은샘플 활용 고의로 과적합(손실이 줄지 않는다면 vs 손실이 폭발한다면)
- 체크리스트 3 : 2에서 얻은 구조, 최적화 툴 활용, 학습률 찾기
  - 모든 학습 데이터 활용, 작은 반복 - 빠르게 손실이 줄어드는 학습률 우선 탐색
  - 일반적 탐색 범위 로그스케일 1e-1, 1e-2, 1e-3, 1e-4
  ->  손실이 실제로 줄어들기 시작하는 좋은 학습률 시작점 다수 확보
  - 직관 : 손실이 실제로 줄어드는 학습률부터 시작하라
- 체크리스트 4 : 3에서 얻은 후보 학습률 + 후보 가중치 변형 방식 중, 좋은 조합 탐색
  - 상기 조합 활용, 1-5 에폭동안 모델 학습시켜 성능 비교
- 유효한 이유
  - 모든 조합 끝까지 학습 -> 연산자원 낭비
  - 좋은 조합은 초기 몇 에폭 내에 손실 감소
  - 짧은 학습만으로 경향성 관찰
  - 검증세트에서의 성능으로 관찰 -> 학습손실로 일반화성능 알 수 없음
- 체크리스트 5 : 체크리스트 4에서 얻은 좋은 조합들을 활용, 10-20 에폭까지 추가학습
  - 학습률 변경 적용 x
  - 학습률 변경은 성능을 더개선, 학습속도 느려짐
  - 순수 조합의 성능만으로 판단
- 체크리스트 6 : 학습률에 따른 손실곡선 관찰
  - 손실의 이동평균, 학습데이터/검증데이터 모두 확인
  - 관찰되는 곡선의 특징을 통해 문제점 판단 및 해결
**곡선 형태별 특징 확인 후 데이터 파악**


