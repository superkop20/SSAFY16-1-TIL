# 알고리즘 List 2-2

## 부분집합
비트연산자  
- & : 비트 단위로 AND 연산
- | : 비트 단위로 OR 연산
- <<(shift 연산자) : 피연산자의 비트 열을 왼쪽으로 이동
  - 왼쪽으로 밀고 밀려서 빈 칸은 0으로 채워짐
- (>>) : 피연산자의 비트 열을 오른쪽으로 이동

<< 연산자 활용
- 1 << n : $2^n$ 즉, 원소가 n개일 경우 모든 부분집합의 수를 의미
  
& 연산자 활용
- 1 & (1 << j) : i의 j 번째 비트가 1인지 아닌지를 검사

## 순차검색
검색 : 저장되어있는 자료 중 원하는 항목 찾는 작업/ 목적하는 탐색 키를 가진 항목을 찾는 것   
탐색 키 : 자료를 구별하여 인식할 수 있는 키

일렬로 되어있는 자료를 순서대로 검색하는 방법
- 가장 간단하고 직관적인 검색
- 순차구조로 구현된 자료구조에서 원하는 항목 찾을 때 유용
- 구현이 쉽지만, 검색 대상의 수가 많은 경우 수행시간이 급격히 증가하여 비효율적  
정렬 여부에 따라 구분

정렬되어있지 않은 경우  
- 찾고자 하는 원소의 순서에 따라 횟수 결정
- 정렬되지 않은 자료에서의 순차검색 성공 시 평균 비교횟수 (n+1)/2
- 첫 번째 원소 찾을 때 1번 비교, 두 번째 원소 찾을 때 2번 비교
- 검색 실패 시 마지막 원소까지 비교 - 횟수 n, 시간복잡도 O(n)

정렬되어있는 경우 *통상 오름차순
- 찾고자하는 원소의 순서에 따라 비교 횟수 결정
- 평균 비교횟수 반으로 절감 (n+1/2)

## 이진 검색
자료의 가운데에 있는 항목의 키 값과 비교하여 다음검색의 위치를 결정하고 검색을 계속 진행  
- 이진 검색을 위해서는 자료가 정렬되어야 함

과정 
1. 자료 중앙에 있는 원소 선택
2. 중앙 원소의 값과 목표 값 비교
3. 목표 값이 중앙값보다 작으면 왼쪽 반에 대해 새로 검색 수행, 크면 오른쪽 반에 대해 새로 검색 수행
4. 찾고자 하는 값을 찾을 때까지 1~3 과정 반복

### 인덱스
배열 인덱스를 추가
- 원본데이터에 데이터가 삽입될 경우 상대적으로 크기가 작은 인덱스 배열을 정렬 - 속도 빠름
- 대량 데이터 매번 정렬에 따른 프로그램 과부화 및 비효율 개선을 위해 배열 인덱스 사용

## 선택 정렬
차례대로 정리한다 - 선택 정렬  
과정
1. 주어진 리스트에서 최솟값을 찾는다
2. 리스트의 맨 앞에 위치한 값과 교환한다
3. 미정렬 리스트에서 최솟값을 찾는다
4. 리스트의 맨 앞에 위치한 값과 교환한다
 - 정렬 될때 까지 반복
- 미정렬 원소가 하나 남은 상황에서는 마지막 원소가 가장 큰 값을 갖게되므로, 실행 종료

## 셀렉션 알고리즘
저장되어있는 자료로부터 k번째로 큰 혹은 작은 원소를 찾는 방법을 셀렉션 알고리즘  
- 1번부터 k번째까지 작은 원소들을 찾아 배열의 앞쪽으로 이동시키고, 배열의 k번째를 반환한다.
- k가 비교적 작을 때 유용하며 O(kn)의 수행시간을 필요로 한다

**추가연습 문제 1979 / 1954**
