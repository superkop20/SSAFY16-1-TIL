# =============================================================
# [실습 2] 딕셔너리의 키와 값 뒤집기
# - {1: 10} 을 {10: [1]} 형태로 뒤집는 함수 작성
# - 실습 1과 마찬가지로 3가지 방법으로 풀어봄
#
# [핵심 고민] 왜 결과의 값이 [1] 처럼 '리스트'일까?
#    원래 딕셔너리에서 값(value)은 중복될 수 있기 때문입니다.
#    {1: 30, 4: 30} 처럼 값이 겹치면, 뒤집었을 때 키 30 하나에
#    1과 4가 모두 들어가야 하므로 => {30: [1, 4]}
#    => 그래서 모든 결과를 리스트로 통일해서 담습니다
# =============================================================

# 딕셔너리를 입력받아 value와 key를 뒤집은 결과를 반환하는 함수 `dict_invert()`를 작성하기


# =============================================================
# [실습 포인트 1] [] 표기법 - 두 경우로 나누어 처리
# - 새 딕셔너리를 만들고 items()로 (키, 값)을 함께 순회
# =============================================================

# 1. [] 표기법을 사용한 방법
def dict_invert_01(input_dict):
    # TODO: 빈 딕셔너리를 만들고 items()를 순회하며
    #       키와 값을 뒤집어 담으세요
    pass


# =============================================================
# [실습 포인트 2] get 메서드 - if문 없이 리스트 합치기
# - get(키, [])는 키가 없을 때 빈 리스트를 돌려줌
# - 힌트: new_dict[value] = new_dict.get(value, []) + [key]
# =============================================================

# 2. get 메서드를 사용한 방법
def dict_invert_02(input_dict):
    # TODO: get()의 기본값으로 빈 리스트를 활용해 작성하세요
    pass


# =============================================================
# [실습 포인트 3] defaultdict - 빈 리스트 자동 생성
# - defaultdict(list)는 없는 키를 조회하면 []를 자동 생성
# =============================================================

# 3. defaultdict를 사용한 방법
from collections import defaultdict


def dict_invert_03(input_dict):
    # TODO: defaultdict(list)를 사용해 가장 간결하게 작성하세요
    pass


# -------------------------------------------------------------
# 테스트 케이스
#  1) 값이 모두 다른 경우          => 각 리스트에 요소 1개씩
#  2) 값 30이 두 번 등장하는 경우  => {30: [3, 4]} 로 묶여야 함
#  3) 값이 모두 True인 경우        => {True: [1, 2, 3]} 로 하나에 모임
# 함수를 완성하기 전에는 pass 때문에 None이 출력됩니다
# -------------------------------------------------------------
print(dict_invert_01({1: 10, 2: 20, 3: 30}))  # {10: [1], 20: [2], 30: [3]}
print(
    dict_invert_01({1: 10, 2: 20, 3: 30, 4: 30})
)  # {10: [1], 20: [2], 30: [3, 4]}
print(dict_invert_01({1: True, 2: True, 3: True}))  # {True: [1, 2, 3]}


print(dict_invert_02({1: 10, 2: 20, 3: 30}))  # {10: [1], 20: [2], 30: [3]}
print(
    dict_invert_02({1: 10, 2: 20, 3: 30, 4: 30})
)  # {10: [1], 20: [2], 30: [3, 4]}
print(dict_invert_02({1: True, 2: True, 3: True}))  # {True: [1, 2, 3]}


print(dict_invert_03({1: 10, 2: 20, 3: 30}))  # {10: [1], 20: [2], 30: [3]}
print(
    dict_invert_03({1: 10, 2: 20, 3: 30, 4: 30})
)  # {10: [1], 20: [2], 30: [3, 4]}
print(dict_invert_03({1: True, 2: True, 3: True}))  # {True: [1, 2, 3]}
