# 이미지 딥러닝 모델
이미지 기반 학습모델의 구조와 작동방식  
대표적 CNN 기반 이미지 모델의 변천사

## CNN 살펴보기
### CNN vs FCN
FCN : 완전 연결층(Fully-connected Layer)  
* 입력을 받아서 출력으로 변환하는 신경망의 기본 모듈  

FCN 변환 예시  
이미지 3차원 고차행렬 입력 ->  10x1의 1차원 벡터로 출력, 차수변환 (Flatten)  
모델 상수 $W$ : 모델이 학습해야 하는 파라미터  

합성곱 레이어  
* 입력 이미지를 필터와 연산하여 특징 맵(feature map)을 뽑아내는 모듈
* 1차원 구조로 변환하는 FCN과 달리 3차원 구조를 그대로 보존하면서 연산
* Convolution : 필터를 이미지 상에서 이동시키면서 내적을 반복수행, 내적으로 구한 모든 값을 출력 제공
    - CNN 이전부터 존재하던 수학적인 연산

특정위치에서 *필터*와 *동일 사이즈의 이미지 영역* 간 내적한 결과 값  
필요 연산은 (3*5*5=75)차원 내적 곱 + bias 벡터  
$$
w^T x + b
$$
* $x$: 필터가 덮는 이미지 영역  
* $w$: 필터의 가중치  
* $b$: 편향(보정값)

통상 여러개의 필터가 있다고 가정, 활성화 맵 - n개  
출력해상도 = 입력해상도 - 필터해상도 + 1  
입출력 해상도를 유지하고 싶다면
* 출력값 중 정의되지 않은 경우, 0 혹은 가장 가까운 출력값으로 대체/참조(패딩) 

nx1 벡터로 구성된 바이어스 추가  
배치 사이즈 수식 맨 앞값 새로운 숫자 -> 출력 배치 사이즈 비례  
- N * $C_in$ * H * W 입력(이미지)배치 -> N * $C_out$ * H' * W'
### 모델 구조
모델 상수(파라미터)를 증가시키면? 레이어 중첩하여 상수 증가  
비선형 블록과 함께하면 모델링 파워 향상 : Conv + ReLU  
필터 시각화
- 학습된 필터 시각화를 통해 각 모델(구조)이 학습한 정보를 이해 가능
  * 선형모델 : 필터는 각 카테고리의 템플렛(전형적모습) 역할 $y= W_x$
  * FCN : 다양한 템플렛 제공
  * CNN : 계층별로 이미지의 지역적 템플렛(예: 엣지 색상 등)

중첩과 수용 영역(Receptice Field)
* CNN 레이어는 이미지 작은 부분인 '지역 정보'추출에 유리하게 설계
* 이미지 활용 다양한 작업에서 전체(막랙) 의미 파악이 필요
  * 지역정보 + 이미지 전체 맥락 이해/활용하기 위한 설계 필요.
* 수용영역이란?
  * CNN이 이미지 처리하면서 한 번에 볼 수 있는 영역의 크기 / 네트워크의 시야
* 고해상도 이미지 처리시, 많은 레이어를 통과해야함.
연산, 비용 학습가능성 고려시 비 실용적
* 해법 : 입력 사이즈를 줄여 모델에 입력

풀링 : 효율적 연산 및 위치 변화의 강건성 확보
* CNN 레이어의 출력을 줄여 연산효율성 확보 - 입력 크기 감소
* 맥스풀링 : 정해진 커널사이즈(2*2)로 이미지를 나누어 각 영역 내 가장 큰 값을 선택하는 연산

스트라이드 합성곱 : 풀링 한계 개선
- 일반 합성곱 : 필터 1칸씩 이동하면서 연산 수행(출력값 계산)
- 스트라이드 합성곱 : 필터를 스트라이드 값만큼 (S칸) 이동한 후 출력 연산
- 효과 : 풀링 처럼 입력 해상도를 줄임(연산 효율과 위치 강건성)
풀링은 학습상수가없지만, 스트라이드는 커널을 동시에 학습

## CNN 기반 모델 변천사
2010~ 2017 모델 변천사 shallow - 8 layers - 22 layers 순 발전
### AlexNet
5개의 합성곱 계층과 3개의 완전연결 계층으로 구성된 8계층 CNN 모델  
특징
* 5개 합성곱레이어
* 맥스 풀링
* 3개 연결층 레이어
* ReLU 비선형 활성화

입출력 
- 입력 : 이미지 / 해상도
- 출력 : 레이블 / 벡터

Image input 224, 2*GPU : 227로 추후 정정, 당시 메모리 부족으로 2개 GPU 병렬 사용  
Image input 227, 1*GPU : 최신 GPU - 24~80GB VRAM 보유  
* AlexNet - 초기연구이므로 경험적 상수로 계산이 애매한 경우 o  
convolution layer : 특징 추출을 위해 단계적으로 중첩 사용 

최종 풀링층 -> 완전 연결층(FC Layers) -> 출력층(Output)  
출력층 : Softmax(1000개 클래스) 1024 - 24 , 1000개 클래스 확률분포 출력  

AlexNet 출력 사이즈 계산법
* 출력 채널 수 = 현재 레이어의 필터(커널) 개수  
* 출력 해상도 $W' = \frac{W - K + 2P}{S} + 1$

AlexNet 연산비용 계산법
* 출력 활성 메모리(KB) = $C \times H' \times W' = 96 \times 55 \times 55 = 290,400$  
* 출력값 메모리 사용 4 Byte $\rightarrow$ $4\text{ Byte} \times 290,400 = 1,161,600\text{ Byte} = 1,134\text{ KB}$
상수 k(파라미터 수)  
총 개수 = 출력 채널 수 x 입력 채널 수 x $K^2$(필터 웨이트) + 출력 채널 수(바이어스 웨이트)  
96x3x11x11+96 =34944  
FLOPs(연산량) = 총 연산 (곱하기만 고려) = 출력 원소 수 $\times$ 출력 원소별 연산  
  $= (H' \times W') \times (C_{\text{in}} \times K \times K \times C_{\text{out}})$  
  $\rightarrow (55 \times 55) \times (3 \times 11 \times 11 \times 96) \times 2 = 105,415,200 \text{ (105.4M FLOPs)}$

메모리 사용은 초기 레이어에 집중(conv1,2)  
Params - 모델 상수(K)는 연결층 레이어(FC)에 집중
FLOPs - 합성곱에서(conv2,4) 주로 발생  
### VGGNet
5개의 합성곱 블록 + 맥스 풀링 구조  
VGG-16 기준  
특징 
- 5개의 합성곱 블록
- 각 블록당 맥스 풀링
- 3개 연결층 레이어
- ReLU 비선형 활성화
- 총 16개의 합성곱/연결층 레이어
- 최종 소프트맥스 수행

AlexNet과의 차이  
필터 3x3 고정, 단계적인 채널사이즈 64 -> 2의 배수 증가  
* 작고 단순한 필터를 깊게 쌓으면 성능 향상 -> 새로운 설계 철학
* 3x3 합성곱/스트라이드1/패딩1 반복 적용
* 맥스풀링으로 크기 줄이기, 풀링 후, 채널 크기 2배 적용
  - 3x3 합성곱 두개 연달아 적용하면 5x5 합성곱과 동일 리셉티브 필드 확보  

| Option 1: | Option 2: |
| :--- | :--- |
| 합성곱($5 \times 5, C \rightarrow C$) | 합성곱($3 \times 3, C \rightarrow C$)<br>합성곱($3 \times 3, C \rightarrow C$) |
| Params: $25C^2$<br>FLOPs: $25C^2HW$ | Params: $18C^2$<br>FLOPs: $18C^2HW$ |  
alexnet에서는 레이어별 연산량 차이 큼. VGG 해상도 줄일 때마다 채널늘려 레이어별 연산량 차이 줄임

VGG: alexnet 메모리 약 25배  
모델상수 약 2.3배 연산량(FLOPs) 약 19.4배

VGG 모델 특징
* 단순함 + 깊이의 강력한 성능을 보임
* 단순 설계로 모델 해석에 이상적
* 특징추출기, 전이학습에 강력한 베이스라인
  * 파라미터가 많고 연산량 요구가 매우 큼
### ResNet
합성 블록과 잔차 블록 구조  
잔차블록은 블록입력을 그대로 출력에 더해주는 지름길 연결이 특징  
Inception 모델에서 차원 축소로 활용된 1x1 합성곱을 적용, 연산효율 개선
등장 배경
* 배치정규화 방식으로 10+ 레이어 학습 가능
  * 20-레이어 모델 : 학습이 안정적, 에러 감소
  * 56-레이어 모델 : 더 깊음에도 불구, training error 오히려 높음
* 깊다고 무조건 더 잘 학습되는 것은 아님
-> 단순히 레이어를 깊게하면 오히려 성능 하락

작은 모델의 성능은 "최소한" 누릴 수 있도록 작은 레이어의 추정값을 그대로 후속 레이어에 제공하여 최소한 작은 레이어 수준의 성능 보장  
-> Residual Block의 핵심 아이디어  
입력 X가 두 경로로 나뉘어 전달됨
1) 변환 경로 ($F(x)$)  
   $\rightarrow \text{Conv}(3 \times 3) \rightarrow \text{ReLU} \rightarrow \text{Conv}(3 \times 3) \rightarrow \text{ReLU} \rightarrow \text{출력 } F(x)$  

2) Shortcut 경로 (Identity mapping, 잔차 연결)  
   $\rightarrow \text{입력 } X\text{를 그대로 전달}$  
마지막에 두 값을 더함 : Output = F(x) + x  
학습이 잘 안되면 0으로 초기화하여 출력 x값 보장  
연산 효율을 위해 보틀넥 잔차 구조를 도입  
* FLOPs 비교 : $18HWC^2 \text{ vs } 17HWC^2$
3x3 2개를 3x3 1개에 앞뒤배치로 3개레이어로 변환
보틀넥 잔차구조 : 1x1 conv를 앞 뒤에 배치하여 입력과 출력의 사이즈를 동일하게 하여 잔차블록 형식 유지  
더 깊은 모델을 더 적은연산으로 달성 가능  

Stem 구조
* 기존 모델의 효율성 레시피를 잘 활용
  1. 스템 구조를 모델 초기에 도입, 입구 데이터 해상도를 1/4로 줄이고 이후 잔차블록 적용
  2. 여러개의 FC레이어 제거, 전역 평균 풀링 사용 -> 마지막 1개 FC적용
전역평균풀링 : 특성 맵(Feature Map)의 각 채널별 전체 픽셀 값들의 평균을 구해 채널당 하나의 값으로 압축  
VGG-16, Res 18, Res 34 비교  
* vgg : 성능은 유사, 연산량 high
* Res 18 : 얕고 가벼움, VGG보다 성능 약간 낮음
* Res 34 : 성능 high, VGG보다 정확 및 가벼움

Res 34 vs 50 : 보틀넥 잔차 블록 사용 여부의 차이  
50은 현재도 사용되는 CNN기반 베이스라인(기준 모델)  
최종 요약 : ResNet
* 깊은 모델 학습을 위한 중요한 전진(100+ 레이어 학습 가능)
* 깊은 모델의 강력함을 다시확인
* 제안 당시 모든 벤치마크에서 최고성능 달성
* 지금도 CNN기반 구조 중 가장 활발하게 활용
### MobileNet
모바일/임베디드 환경에서 구동 가능  
기존 합성곱은 공간과 채널을 동시처리하여 연산량 과도  
Mobilenet 핵심 아이디어 : 공간과 채널을 두 단계로 분리하여 처리
- 깊이별 합성곱 : 각 채널별 독립적 3x3합성곱 수행
- 화소별 합성곱 : 1x1 합성곱을 채널 방향으로 적용  

효과 
- 연산량 기존 대비 9배 가량 절감
- 모델 상수 대폭 감소

깊이별 합성곱 : 그룹은 유지하면서 각 깊이별로 채널 곱, 해상도 안에서 섞인다.  
화소별 합성곱 : 깊이 단계에서 곱, 화소 내부단계에서 곱셈  
각각의 연산을 나누고 최종에서는 깊이, 화소의 특징을 모두 구현하도록 접근  
$$
\begin{aligned}
\text{연산량 절감 비율} &= \frac{9C^2HW}{9CHW + C^2HW} \\
&= \frac{9C}{9 + C} \\
&\rightarrow 9 \quad (\text{as } C \rightarrow \infty)
\end{aligned}
$$
C가 무한으로 커질 경우 연산량 절감의 효율이 크게 증가할 수 있다.(9배)  
이후 모델 영향
* 가볍고 빠른 모델, 엣지디바이스(모바일 등)에서 딥러닝 모델 실시간 실행 가능성
* 효율형 모델 구조의 표준 제시 : 깊이별 합성곱, 보틀넥 구조 이후 경량모델에서 지속적 채택

===========================================
# 다양한 신경망 모델
학습목표 :  
CNN의 한계를 이해, 이미지인식에서 왜 새로운 접근이 필요한지 확인  
RNN, Attention 기반 Transformer의 핵심 아이디어와 구조를 파악, 모델이 해결하는 문제 이해  
모델별 설계 철학, 특징 비교, 데이터유형에따른 적절한 모델 선택 가능

## CNN의 한계
### CNN의 구조 및 장점
합성곱 구조 특성 :  
* 지역적인 특징 학습에 특화 : 작은필터로 학습
* 파라미터 효율성 우수 : 지역적 특징 추출 시 연산량 절감
* 위치변화와 노이즈에 견고 : 합성곱 - 세부차이 감소, 전체의미 집중
* 표준모델로 활약
### CNN 구조의 한계
- 데이터 순서를 무시
  - 순차적 데이터의 순서정보(텍스트/음성의 시간 흐름등)를 반영하기 어려움
- 긴 거리 의존성 고려 부족
  - CNN은 국소적 특징 추출에 강하지만, 이미지나 문장에서 멀리 떨어진 요소간의 관계를 잘 학습 못함
- 픽셀 단위 복원 한계
  - CNN은 이미지 전체의 정보를 압축(라벨)에 효과적이지만 위치정보 손실(pooling, stride 효과)

## 시퀀스 데이터 처리 : RNN
### RNN(Recurrent Neural Nets)
순차적 데이터를 처리하기위해 고안된 신경망 구조  
시계열/언어데이터 적합 : 문장, 음성신호, 센서데이터 등 시계열 데이터 처리에 관한 강력한 귀납 편향 제공  
-> input chars - input layer - hidden layer - output layer - Target chars   
RNN 셀 : 은닉층에서 활성화 함수를 통해 결과를 내보내는 역할  
 - 은닉 상태 : 메모리셀이 출력층 방향 또는 다음 시점이 t+1의 자신에게 보내는 값  

특징 : 시계열 의존성 - $h_t$가 $h_{t-1}$에 의존  
순수(vanilla RNN)
선형변환, 바이어스를 모델상수/비선형 (tanh)함수 추가로 구성된 단순한 형태
$$
h_t = \tanh(W_{hh} h_{t-1} + W_{xh} x_t)
$$
출력 계산 $y_t$ 최종출력 생성  
$$
y_t = W_{hy} h_t
$$
입출력 구조
- One-to-Many : 하나의입력에서 시간축으로 출력이 생성
- Many-to-One : 여러 시점 입력이 하나의 출력으로 압축
- Many-to-Many : 입력과 출력이 시간축을 따라 1:1혹은 n:m으로 맵핑
### 단순 RNN 한계
기울기 폭발 혹은 소실 발생  
- |W| 의 특이값 높음 - 곱할수록 무한히 커짐 -> 기울기 폭발  
- |W| 의 특이값 낮음 - 곱할수록 0에 수렴 -> 기울기 소실
- 이상적으로 W의 특이값 1이면 온전한 학습 가능
  - 특이값 : 행렬이 벡터를 얼마나 확장시키는지 크기로 나타낸 값 = 반복해서 곱할 때 출력이 터지거나 사라지는지 결정하는 값
폭발시에는 절삭 - but 기울기 소실은 해결 어려움
### 대안 모델
LSTM(Long Short-Term Memory) : 셀 상태, 게이트로 구성  
- 셀 상태 : 중요한 정보를 유지/기억하는 역할
- 게이트 : 정보의 흐름을 제어(스위치) - 선택적
- 구동철학 : 긴 시퀀스 데이터에서 필요한 정보는 유지, 불필요한 정보는 지워 안정적 학습 유도
$$
\begin{pmatrix} i \\ f \\ o \\ g \end{pmatrix} = \begin{pmatrix} \sigma \\ \sigma \\ \sigma \\ \tanh \end{pmatrix} W \begin{pmatrix} h_{t-1} \\ x_t \end{pmatrix}
$$
$$
\begin{aligned}
c_t &= f \odot c_{t-1} + i \odot g \quad && (\text{셀 상태}) \\
h_t &= o \odot \tanh(c_t) \quad && (\text{은닉 상태})
\end{aligned}
$$
게이트 : 입력게이트, 망각게이트, 출력게이트로 구성  
기울기 전파 시 선형가중치의 곱 이외에 덧셈 형태로 전달하여 기울기를 장기적으로 보존
* 각 상태에서의 기울기를 덧셈과 곱셈 경로로 분리 가능(소실 방지)  
but 정보 희석 문제의 한계 : 망각게이트가 과거의 정보를 조금씩 줄임 - 과거정보 약화  

## 긴거리 의존성 : 어텐션/ViT
### 어텐션 메커니즘
이미지를 통해 어텐션 이해
* 픽셀 거리와 상관없이 유사한 패치가 이미지에 존재
  * 패치 : 이미지를 일정 크기로 잘라낸 작은 조각 단위
* 관련 높은 패치/관련 낮은 패치 정보를 최종 결정에 반영
 - 쿼리 : 집중대상
 - 키 : 다른 패치
 - 값 : 키 패치 정보
 - 어텐션 : 쿼리, 키 유사도(확률분포)
 - 출력 : 어텐션으로 가감된 값의 총합
$$
\begin{aligned}
q &= \mathbf{x} W_q \\
k &= \mathbf{x} W_k \\
v &= \mathbf{x} W_v
\end{aligned}
$$

$$
a_{ij} = \text{softmax}\left( \frac{q_j \cdot k_i}{\sqrt{D}} \right)
$$

$$
y_j = \sum_{i} a_{ij} v_i
$$
* 쿼리와 키가 유사하면 해당 값에 가중치 높게

셀프 어텐션 :
- 하나의 입력 안에서 패치정보(쿼리/키/값) 정의
- 같은 이미지 내 유사도 반영
- 입력 내부 패치간 연결망 구축 -> 클러스터링 효과(모델의 특징파악률 상승)

교차 어텐션(Cross-attention) : 
- 둘 이상의 이종데이터에서 패치정보 정의
- 이종 데이터 간 유사도 반영, 서로다른 입력간 연결망 구축
### ViT(Vision Transformer) 위치 인코딩
모델별 특성 정리
- CNN : 지역적 특성, 장거리 맥락 이해 떨어짐, 데이터순서 반영 X
- RNN : 데이터 순서 반영, 장거리 맥락 학습 어려움(기울기 소실/폭발)
- LSTM : 데이터 순서 반영, 장거리 맥락 이해 가능, 정보 희석(시간서순)
- Attention : 장거리 맥락에 탁월, 정보희석 없음, 순서반영 가능?

위치 인코딩 : 패치순서(위치정보) 제공
- 각 입력 토큰에 위치 정보를 담은 벡터 추가

학습 가능한 위치 인코딩 
- 데이터에 알맞게 최적화
  - 장 : 해상도가 바뀌어도 동일한 룰, 단 : 해상도가 바뀌면 재학습 필요

사인파 인코딩 : 데이터 최적화
- 장점 학습과 동일 , 단 : 고정이라 데이터/과업에 특화된 방식이 아님

상대위치 인코딩 : 절대위치가 아닌 상대적 위치 인코딩
- 장 : 해상도 변화에도 적용 가능, 단 : 절대적 위치 고려가 어려움

회전 포지션 임베딩(2D RoPE)(교재 X) : 위치벡터를 더하지 않고, Q/K에 회전행렬 적용
- 상대 위치 표현(해상도 가변), 단 : 연산량 추가(매번회전연산), 절대 위치 소실

인코더 내부 구조 
- 정규화 - 다중헤드 어텐션 - 정규화 - 연결층으로 구성
- 인코더 L개 통과(일반적으로 12개 up)

전역 관계 학습
- CNN : 지역적 영역에서 수용 영역을 넓혀 전역 이해
- ViT : 수용영역을 넓히지 않아도 전역 맥락 이해 가능

최종 출력 : MLP head - 최종예측 수행  
ViT vs ResNet  
ImageNet의 경우 ResNet 성능이 더 우수,   
사전학습이후 미세조정한 경우, ViT도 우수  
ViT는 거대 데이터 세트 사전학습 시 우수
* 300M개의 데이터세트 - 사전학습 데이터 셋이 커질수록 성능이 개선을 보이기 시작
표 B,L,H - 모델크기 / 패치사이즈는 작을수록 큰 모델 의미  
*ViT 학습 기술 중요*
- 막대한 상수 보유
- 정규화 기법/데이터 증강 기술(좌우 변환, 슬라이싱 등) 매우 중요
  * 약한정도의 증강은 거의 항상 성능 향상에 도움이 된다.

증류학습 [Knowledge Distillation = 지식 증류]기술 효과적
* 학습 방식
  * 1단계 : "선생님 모델"(CNN 모델)을 이미지-레이블 활용해서 학습
  * 2단계 : "학생 ViT모델"은 선생님 모델 예측 따라가도록 학습(정답과 함께)  
크로스 엔트로피 손실 : 원핫 의 예측을 구현에 손실
KL 발산 손실 :  실제 데이터의 확률 분포 P와 모델이 예측한 확률 분포 Q의 차이를 측정하는 손실 함수  
* 강아지, 고양이 이미지 분석 - 이미지 관계성 정보 CNN -> ViT 학습
학생 ViT 모델은 학습 가능한 토큰을 추가로 입력받아, 선생님 CNN의 logit 출력과 동일하게 예측하도록 학습됨
#### ViT 트렌드
다양한 변종 존재 
- CNN처럼 이미지의 계층적 이해를 활용할 수 없을까?
  - Swin Transformer : 이미지 전역에 직접 어텐션 수행하는 것은 너무 고비용, 비효율
    1. 윈도우 영역을 지정, 그 내부의 어텐션에 집중
    2. 윈도우를 비껴가게 한번 더 설정 

Vision 백본 vs 데이터 확장성 vs 멀티모달 모델
- Vision 백본 : ViT-22B(220억개 상수 사용) - 구글 리서치
  - 지도학습 채택 : 대규모 비공개 레이블 데이터 기반(JFT-3B)
  - 일반화 성능이 우수/모델 상수 비공개(재현불가)
- 자기지도 Vision 백본 : DINOv3(최대 70억 상수) - 메타AI
  - 공개 웹 데이터 기반 17억장 대규모 데이터
  - 자기지도 학습 : 대조학습/증류 + 보간 기법
  - 가중치 공개, 최고의 조밀한 피쳐맵 제공

ViT 장점 :
* 전역 문맥을 한번에 고려 가능
* 시계열/시퀀스 데이터의 경우, 순서 고려 가능(위치 인코딩)

단점 : 
- 대규모 학습 자원 필요
- 학습 데이터 규모가 작을 경우, CNN보다 성능 저하
- 한번에 고려할 수 있는 토큰이 제한적

활용 : 
- 이미지 분류/탐지/분할/생성에서 모두 SoTA 성능 달성 * Sota : state-of-the-Art
- 멀티모달 기준에서 기준 아키텍쳐
- 기준모델에 활용중

# 이미지 모델 학습 전략
딥러닝 모델 학습의 안정성/효율성/일반화 성능을 높이기 위해, 모델 구조의 개선 못지 않게 학습 방법론이 중요한 요소임을 이해   
모델 학습 전/과정에 중요한 일련의 전략을 습득

## 학습 전략의 중요성
### 모델 구조
CNN의 구조와 발전 / CNN의 한계
### 좋은모델구조 = 우수한 성능?
좋은 구조만으로는 성능 보장 불가!  
- 동일한 구조, 동일한 데이터셋을 사용해도 성능차이가 크게 남
- 이유 :학습과정에서 발생하는 불안정성, 과적합, 수렴 속도 문제

학습에서 자주 겪는 문제들
* 학습 불안정 : 손실폭발, 학습멈춤, 수렴실패
* 과적합 : 훈련 데이터에는 맞지만 검증, 실전 성능 저하
* 느린 수렴 : 최적점에 도달하기까지 불필요하게 많은 에폭 소모로 인한 학습 효율 저하
### 학습 전략
구조 설계만큼 중요한 훈련 전략 제시  
적용시 기대효과 : 일반화 성능 + 학습 효율성 확보  
구조만으로 불충분 -> 훈련 전략 필요

## 모델 구성 : 악마는 디테일에..
### 활성화 함수
입력신호의 총합을 출력 신호로 변환하는 함수
* 예시 : 뉴런이 '켜질지/얼마나 반응할지'를 결정하는 스위치
역할 : 신경망에 비선형성 부여 -> 복잡한 패턴 학습 가능

활성화 함수가 없다면? 단순 선형모델과 동일  
Sigmoid : 0-1 사이 값으로 출력 값 제어, 확률 값처럼 해석 가능(자연로그 $e$ 활용 수식)  
문제
1. 입력이 매우 크거나 매우 작으면 출력이 거의 0이나 1 고정 -> 역전파 전달 X
2. 출력범위는 항상 양수 -> 편향된 업데이트 발생
3. 계산식의 지수함수 -> 계산비용 비교적 크다  

Tanh : 출력범위 [-1, 1] 데이터가 중심을 기준으로 대칭 -> 안정성 높음
문제 
1. 기울기 소실 문제 여전 -> 학습 정체
2. exp 연산 포함 -> 계산 비용 커짐
$$
\tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}
$$

ReLU(Rectified Linear Unit) : $f(x) = max(0,x)$  
장점
- 양의 영역에서 포화 x -> 기울기 소실 문제 크게 줄어듦
- 계산 효율성 높음 
- 학습속도 빠름 -> 시그모이드 tanh대비 6배이상 빠른수렴  

단점
- 죽은 뉴런 문제
- 0을 기준으로 비대칭 -> 왜곡 존재

Leaky ReLu :  $f(x) = max(0.01x,x)$   
장점 : 
- ReLU의 모든 장점 수용  
- 죽지 않는 뉴런(Dead ReLU)

문제점 
- 음수 영역 기울기값 설정의 임의성(여전히 너무작으면 소실, 크면 출력 왜곡 가능)
- 0 중심 대칭이 아님(한쪽 치우치는 경향 발생 가능)
- 항상 ReLU보다 우세하지 않음(일부문제 성능차이 거의 없음)  

Exponential Linear Unit (ELU)
$$
f(x) = \begin{cases}
x & \text{if } x > 0 \\
\alpha(\exp(x) - 1) & \text{if } x \le 0
\end{cases}
$$

장점 
- ReLU의 모든 장점 수용
- 평균 출력이 0 근처에 위치
- 음수 영역에서의 포화 구간 제공 -> 기울기 소실 및 왜곡 방어

문제점 
- 계산 복잡성 증가
- 하이퍼 파라미터 $a$ 설정 필요 -> 음수영역 기울기의 크기를 결정하는 $a$값에 따라 성능차이

결론 : 큰 고민 말고, 일단 ReLU부터 시도  
-> 성능 경쟁 중이라면 개선판 시도
### 데이터 전처리
데이터 형식 통일 : 이미지 조건을 일치
- 학습/검증/테스트에 모두 동일하게 적용
- 일반적 정규화 방식(평균 0, 표준편차 1로 정규화)

모델별 예시
AlexNet : 평균 이미지 빼기  
VGG : 채널별 평균 빼기  
ResNet : 평균빼기 + 채널별 표준 편차로 나누기
### 모델 상수 초기화
아이디어 1 : 0으로 초기화
- 모든 가중치와 편향을 0으로 초기화하면?
  - 모든 출력은 0 출력 동일, 기울기 동일 -> 학습이 이뤄지지 않음

아이디어 2 : 임의(랜덤 초기화)
작은랜덤 숫자(0.01곱 -> 0.01 분산) : Gaussian 분포를 따름, 0을 중심으로.
- Gaussian 분포 : 종 모양의 정규분포로 평균 0 근처 값이 가장 자주 발생 -> 값이 너무 크거나 작아지는 걸 막을 수 있음
- 깊지 않은 모델에서 동작하는 전략
- 깊은모델 : 작은 수에 분포할 경우 역전파 기울기를 구하는 과정에서 W를 계속 곱하면서 기울기 0으로 수렴
  - 상수가 크면 기울기 폭발

아이디어 3 : 자비에(Xavier) 초기화
- 가중치 초기 분포의 분산을 입력 차원으로 맞춤 std = 1/sqrt(Din)
- 분산을 입력 차원(뉴런수) 증가를 고려하여 설계
  -  모델상수 분산이 입력 차원과 같으면, 선형연산 이후 출력의 분산 = 입력의 분산임을 보일 수 있음

아이디어 4 : 허(He) 초기화
- 자비에 초기화 + ReLU의 궁합
  - 가중치 초기 분포의 분산을 입력 차원으로 맞춤.  
- 자비에에서 입출력 분산을 맞추는 전략은 입출력이 대칭적 분포를 보인다는 가정 하에서 설계
- 가중치 초기 분포의 분산을 2*입력차원으로 맞춤 std = sqrt(2/Din)
- 비대칭, 유사 분산이 레이어 후반에도 유지됨

ResiNet의 등장 배경
- 잔차 연결을 사용해 매우 깊은 신경망도 안정적으로 학습할 수 있는 구조
- 깊은 모델의 문제점
  - 깊어질 수록 기울기 소실/ 폭발 문제 발생 -> 학습 어려워짐 (ResiNet을 이용해 완화)

Resinet 철학 - 전층의 기울기 전달  
- 깊은층에서도 기울기가 끊기지 않고 전달 
- 초반 초기화가 잘못되면, 후반층 출력 분포값 계속 증폭할 수 있음.
- Resinet은 ReLU 사용구조이므로 He초기화 사용, Var(x+F(x)) = Var(x) 유도
  - 초반에는 skip의존, 초기엔 입력 = 출력, 분포 안정 -> 학습 안전하게 출발
### 모델 정규화
정규화를 통해 오차를 줄여보자
- 정규화 : 모델 복잡도를 제어해 훈련 성능과 검증 성능의 차이를 줄이는 방법
- 검증 오차 : 모델이 학습하지 않은 새로운 데이터에서 보이는 실제 성능 지표

아이디어 1 : 가중치 감소  
학습 과정에서 특정 가중치가 지나치게 커지는 현상 방지
- 너무 크면 입력 과도반응 -> 훈련데이터 외워버림
- 가중치 크기에 비례한 패널티
- 큰 가중치는 과적합을 부른다 -> 줄여주자
- 가중치 감소 완벽하게 만족
  - 모든 가중치 0인 모델 -> 학습 X
  - 원본 모델의 학습 목표와 함께 동작할 때 의미가 있음

L2 정규화  
- 작동방식 : 큰 가중치에 제곱으로 패널티 -> 극단적으로 큰 값 방지
- 결과 : 모든 가중치가 골고루 작아짐
$$
R(W) = \sum_k \sum_l W_{k,l}^2
$$

L1 정규화
- 작동방식 : 모든 가중치에 절댓값 크기만큼 동일하게 패널티
- 결과 : 중요하지 않은 가중치는 완전히 0이 됨(희소)
- 장점 : 자동으로 중요한 특성만 선택 -> 더 단순한 모델
$$
R(W) = \sum_k \sum_l |W_{k,l}|
$$

Elastic net(L1+L2)  
- L1과 L2의 장점을 합친 방식 : 특성선택 + 안정적 학습
- 변수가 많고 상관관계가 높을 때 특히 효과적
$$
R(W) = \sum_k \sum_l \beta W_{k,l}^2 + |W_{k,l}|
$$

아이디어 2 : 드롭아웃  
- 학습과정에서 일부 뉴런을 확률적으로 끔(0으로)
- 매 학습 스텝마다 다른 네트워크 구조가 샘플링되는 효과
- 직관 : 무작위로 뉴런을 비워서 특정 뉴런/특징에 과도하게 의존하지 않게 함
장점 
- 과적합 방지
- 여러 작은 모델을 합친 것과 비슷한 효과
- 간단, 매우 강력
단점
- 학습시간 증가
- 최적의 비율 찾는것 중요
- 모델에 따라 비선호

실행단계 동작 - 뉴런 확률적으로 끔.  
문제점 - > 성능저하  
- Inverted 드롭아웃
  - 학습 시 살아남은 뉴런의 출력을 1/(1-p) 배 스케일링

실행단계 해결책 - 기존 방식
(1-p)를 출력에 곱하여 평균치를 맞춤.  
단점 : 테스트 시 추가 연산 필요

## 학습 안정성 전략
### 학습 비율 조정
학습 비율을 어떻게 선정해야 할까?  
기본적 전략 : 학습 비율을 큰 값에서 시작하여 epoch 지날 수록 작은 값으로 조정  
Good learning rate의 경우, Loss가 안정적으로 감소

학습률 계단식 변경  
일정 에폭이 지날 때 마다 계단식으로 줄이는 방식  
- 에폭 : 학습 데이터를 1번 다 외움을 의미함
- K 에폭마다 감소계수 * 초기학습률 만큼 감소하는 전략

학습률 코사인 변경  
계단형 방식은 변경지점을 여러개 선정, 복잡하다는 단점  
코사인 파형에 따른 변경 
- 학습률을 코사인 함수 곡선처럼 점점 줄여가는 방식
- 직관 : 처음엔 크게, 나중엔 미세하게 조율
$$
\eta_t = \eta_0 \cdot \frac{1}{2}\left( 1 + \cos\left( \frac{\pi t}{T} \right) \right)
$$

학습률 선형 변경  
직선을 따라 변경
- 학습률을 학습이 진행될수록 선형(직선)으로 줄여나가는 방식
- 직관 : 처음엔 크게 배우고, 끝날수록 일정하게 줄여 수렴
- 사전학습/미세조정에서 주로 쓰임
$$
\eta_t = \eta_0 \cdot \left( 1 - \frac{t}{T} \right)
$$

그 외 다양한 감소 방식  
역 제곱 꼴, 일정값 유지

학습 종료   
과적합 방지를 위한 또다른방법
- 빠른종료 기법 : 학습 진행하며 성능이 나빠지기 시작하면 학습 조기종료
### 하이퍼 파라미터 선정
파라미터 vs 하이퍼파라미터  
파라미터 
- 학습을 통해 모델 스스로 얻는 값

하이퍼파라미터
- 학습 전에 사용자가 정해야 하는 값
- 학습 진행동안 고정됨

주요 하이퍼파라미터
- 학습관련 : 학습률, 배치사이즈, 에폭 수(조기종료)
- 최적화 관련 : 최적화 툴 선택, 모멘텀/최적화 계수, 가중치 감소
- 모델 구조 관련 : 네트워크 깊이, 채널 수/드롭아웃 비율, 정규화(BN, LN, GN 등)

하이퍼파라미터 값 탐색
- 그리드 탐색 : 후보들을 격자처럼 전부 조합
- 랜덤 탐색 : 무작위로 값 선택해 탐색 / 불필요한 축 탐색 x, 시간 절약

하이퍼파라미터 선택 비법  
- 모든 조합을 다 실험할 수 없다면
  - 빅테크조차 모든 조합 실험 x
  - 몇 가지 규칙과 직관을 활용하면 GPU가 많지 않아도 좋은 성능 도출 가능

- 체크리스트 1 : 데이터 입력 등 문제가 없을까? 
  - 초기 손실값 확인
- 체크리스트 2 : 학습률과 초기값 확인
  - 작은샘플 활용 고의로 과적합(손실이 줄지 않는다면 vs 손실이 폭발한다면)
- 체크리스트 3 : 2에서 얻은 구조, 최적화 툴 활용, 학습률 찾기
  - 모든 학습 데이터 활용, 작은 반복 - 빠르게 손실이 줄어드는 학습률 우선 탐색
  - 일반적 탐색 범위 로그스케일 1e-1, 1e-2, 1e-3, 1e-4
  ->  손실이 실제로 줄어들기 시작하는 좋은 학습률 시작점 다수 확보
  - 직관 : 손실이 실제로 줄어드는 학습률부터 시작하라
- 체크리스트 4 : 3에서 얻은 후보 학습률 + 후보 가중치 변형 방식 중, 좋은 조합 탐색
  - 상기 조합 활용, 1-5 에폭동안 모델 학습시켜 성능 비교
- 유효한 이유
  - 모든 조합 끝까지 학습 -> 연산자원 낭비
  - 좋은 조합은 초기 몇 에폭 내에 손실 감소
  - 짧은 학습만으로 경향성 관찰
  - 검증세트에서의 성능으로 관찰 -> 학습손실로 일반화성능 알 수 없음
- 체크리스트 5 : 체크리스트 4에서 얻은 좋은 조합들을 활용, 10-20 에폭까지 추가학습
  - 학습률 변경 적용 x
  - 학습률 변경은 성능을 더개선, 학습속도 느려짐
  - 순수 조합의 성능만으로 판단
- 체크리스트 6 : 학습률에 따른 손실곡선 관찰
  - 손실의 이동평균, 학습데이터/검증데이터 모두 확인
  - 관찰되는 곡선의 특징을 통해 문제점 판단 및 해결
**곡선 형태별 특징 확인 후 데이터 파악**

