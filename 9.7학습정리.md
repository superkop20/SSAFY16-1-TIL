# 알고리즘 3-1 

## String 1
### 코드체계
문자에 대응되는 숫자를 정한 것  
주소가 부여되는 최소단위 : 바이트
- 메모리는 숫자만을 저장, 알파벳은 대소문자 52자이므로 6비트면 저장 가능

ASCII 체계 - 문자열 표준화 시스템, 32~126개의 표준 문자로 입력하도록 코드 부여  
확장 아스키 : 표준 문자 이외 부가적인 문자 128개 추가
- 표준 - 7비트 사용 문자표현, 확장 - 8비트 모두 사용 추가적인 문자 표현
- 표준 : 세계적으로 통용, 확장 : 해독가능하도록 사전 설계 필요

유니코드 
- 각 국가들 자국의 문자를 표현하기 위하여 코드 체계 만들어 사용
- 국가 사이의 정보 전달 간 표준화 체계 미흡으로 문제 발생
- 다국어 처리를 위한 표준 유니코드 개발
  * 이모지 등도 유니코드 문자에 포함

유니코드 -> 다시 Character Set으로 분류
- USC-2, USC-4
- 유니코드를 저장하는 변수의 크기 정의

바이트 단위 저장 순소
- 저장 순서가 정해지지 않은 경우 잘못된 해석 가능성
- 여러바이트로 이루어진 데이터 저장 방식 Endian
- Big-end : 상위 바이트 가장 낮은 주소
- Little-end : 하위바이트 가장 낮은 주소

유니코드 인코등 (UTF : Unicode Transformation Format) 
- UTF-8
  - MIN : 8bit, MAX : 32bit(1byte*4)
  - 2진법 표현으로 각각의 바이트에 대응되는 코드, 문자 표시(각각 가변길이)
- UTF-16
  - MIN : 16 MAX : 32(2byte*2)
- UTF-32(in unix)
  - MIN : 32, MAX:32(4byte*1)

파이참 인코딩 - CRLF, UTF-8 방식 사용  
- 윈도우 : CR(13) LF(10) 두 글자 사용
- Unix, macOS : LF(10) 한 글자 사용

코드체계의 핵심 
- 전 세계의 모든 문자를 컴퓨터가 일관되게 표현하고 처리하는 것

### 문자열
문자들이 순서대로 나열된 데이터  
파이썬 str 클래스 구조
- 길이 외에 다른 정보도 저장
  - pyObject_HEAD : 모든 파이썬 객체가 상속하는 공통 구조
  - length, hash, interned, kind, data 등

C언어에서 문자열
- 문자열은 문자들의 배열 형태로 구현된 응용 자료형
- 문자배열에 문자열을 저장할 때는 항상 마지막에 끝을 표시하는 널문자 필요
- 문자열 처리에 필요한 연산을 함수 형태로 제공함(strien 등)

자바에서 문자열
- 문자열 데이터를 저장, 처리해주는 클래스 제공
- String클래스 부여
- 문자열 처리에 필요한 연산을 연산자, 메소드 형태로 제공
  - +, length 등i

Python3 문자열
- 텍스트 데이터의 취급 방법이 통일되어있음
  - Unicode 기반이라 어떤 언어나 기호도 동일한 방식으로 처리합니다.
- 문자열 기호
  - 홑따옴표, 쌍따옴표, 홑따옴표 3개, 쌍따옴표 3개
- 연산
  - '+' 연결 - 이어붙여주는 역할
  - '*' 반복

====
## 스트링 3-1 실습
회문 검사 - algorithmic way(인덱스 활용 순회 검사)  
- 전체 길이의 절반 만큼 순회 - 홀수인 경우 몫, 짝수면 2로 나눈 값
  - 절반까지만 검사하면 됨
- word[i] 와 짝이되는 '뒤쪽문자' 비교
- 마지막 인덱스는 N이 아니라 N-1(인덱스이므로 N자리면 0부터 N-1 까지)
  - i = 0 일 때 짝은 마지막, i = 1 일 때 짝은 마지막 - 1 ...

### 문자열 패턴 매칭 구현 (Brute Force)
while 문 구현(직관적으로 비교 후 이동탐색)  
- 불일치 시 i = i - j + 1, 즉 패턴 매칭 시작 위치를 한 칸 뒤로 이동시키고, j=0으로 초기화
- "이미 비교한 만큼 뒤로 돌아가고 +1"이라는 논리

for문 구현(패턴이 '시작될 수 있는' 모든 위치 i 를 순회)
- 이중 반복문을 사용하여 텍스트를 순회하며 패턴 일치를 확인(N-M+1 번 시작 가능 N : 전체 길이, M : 패턴길이)

====
# 알고리즘 3-2
## String 2
### 연산
문자열 뒤집기
- 문자열을 역순으로 재정의 : Reverse 메서드 활용
- 리스트로 변환 후 다시 문자열로 변환

회문(回文) - 실습 내용참조  

문자열 비교
- == 연산자와 is 연산자
- == : 값이 같은지 비교
- is : 객체의 정체성, 같은 객체(메모리 주소) 인지 비교 (깊은복사 얕은복사 복기)

### 패턴 매칭
#### 고지식한 패턴 검색
고지식한 알고리즘(Brute Force)
- 단순한 방법
- 본문 문자열을 처음부터 끝까지 차례대로 순회하면서 패턴 내의 문자들을 일일이 비교하는 방식

**참고**
### KMP 알고리즘
패턴의 각 위치에서 매칭에 실패했을 때 돌아갈 위치를 미리 계산하는 알고리즘
- 불일치 발생한 경우 이동할 다음 위치를 계산하는 전처리 필요

### 보이어 무어 알고리즘
상용 소프트웨어에서 채택, 실제 검색에서 매우 빠른 성능  
불일치 문자 휴리스틱
- 오른쪽 끝에있는 문자가 불일치, 문자가 패턴 내에 존재할 경우 : 오른쪽에서 일치한 부분 고려 점프

* 문자열 매칭 알고리즘 비교 시간되면 한번 읽어볼 것

### 문자열 암호화
- 시저암호(카이사르 암호화)
- 문자 변환표를 이용한 암호화(단일 치환 암호화)
  - 모든 키의 조합이 필요, EX - 2차대전 암호메세지 대전
  - 세계 최초의 디지털 컴퓨터 : 콜로서스(영국학자들의 로렌츠 암호 해석)

* 문자열 압축
  - Run-length encoding 알고리즘
    - 같은 값이 몇 번 반복되는가를 나타내는 방식
  - 허프만 코딩 알고리즘
    - 더 효율적이고 일반적인 압축 방법
    - 자주 나오면 짧은 코드, 드물게 나오면 긴 이진 코드 부여. 전체 데이터 평균 비트 수 최소화 알고리즘
