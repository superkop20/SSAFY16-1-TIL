# 알고리즘 Queue 1

## 큐(Queue)
큐의 구조 - 선입선출(FIFO)
- 가장 먼저 넣은 자료가 가장 먼저 나오는 것
- 머리(Front) : 저장된 원소 중 첫번째 원소 (삭제방향)
- 꼬리(Rear) : 저장된 원소 중 마지막 원소 (삽입방향)

큐의 기본 연산
- 삽입 : enqueue
- 삭제 : dequeue
- 큐의 연산과정 7단계 - 삽입/삭제의 연속

## 선형 큐
데이터를 일렬로 저장하며, 앞에서 꺼내고 뒤에 넣는 기본 큐 구조  
구현
- 배열이나 연결리스트로 구현
- 배열의 경우 큐의 크기 = 배열의 크기
- front : 가장 최근 삭제원소, rear : 마지막으로 저장된원소

상태 표현
- 초기 상태 : front = rear = -1
- 공백 상태 : front == rear
- 포화 상태 : rear == n-1(n: 배열의 크기, n-1 : 배열의 마지막 인덱스)

선형 큐 구현
1. 초기공백 큐 생성 : create_queue()
2. 삽입 : enqueue(item)
3. 삭제  :dequeue()
4. 공백 상태 및 포화 상태 검사 : is_empty(), is_full()
5. 검색 : qpeek()

## 원형 큐
선형 큐 사용시 문제점
- 잘못된 포화상태 인식 (배열 앞부분 공간 있음에도 rear == n-1인 상태로 인식)

해결방법
1. 매 연산이 이뤄질 때마다 저장된 원소들을 배열의 앞부분으로 모두 이동
2. 1차원 배열 사용(처음과 끝이 연결된 원형 형태로 가정)

원형 큐
- 선형큐의 공간 낭비를 막기 위해 처음과 끝이 연결된 구조
- 델타연산과 같이 모듈러(%) 연산을 통해 반복순환구조 생성

원형큐 구조
- 초기공백상태
- Index 순환
- front 변수 : front자리는 항상 빈 자리로 둔다

원형 큐 구현(선형과 구조상 동일)
- front, rear 0으로 초기화
- 삽입, 삭제 인덱스 값 차이
  
## 연결 큐(Linked Queue)
연결 리스트를 이용해 구현한 큐

연결 큐의 구조
- 단순 연결 리스트(Linked List)를 이용한 큐
  - 큐의 원소 : 단순연결리스트의 노트
  - 큐의 원소순서 : 노드의 연결순서, 링크로 연결됨
- 상태 표현
- 초기, 공백 : front = rear = NULL

deque(덱)
- 컨테이너 자료형 중 하나로 양쪽 끝에서 빠르게 추가와 삭제를 할 수 있는 리스트류 컨테이너
  - 연결 리스트를 직접 만들지 않아도 됨
- deque의 연산
  - append(x) : 오른쪽에 x추가
  - popleft() : 왼쪽에서 요소를 제거하고 반환, 요소가 없으면 IndexError

## 우선순위 큐(Priority Queue)
우선순위를 가진 항목들을 저장하는 큐  
배열을 이용한 우선순위 큐
- 배열을 이용해 자료 저장
- 원소 삽입과정에서 우선순위 비교, 적절한 위치 삽입
- 최고우선순위 원소 - 가장 앞에 위치
- 문제점
  - 배열을 사용하므로 연산때마다 원소 재배치 -> 시간, 메모리 낭비

효율적인 우선순위 큐
- 트리구조인 힙(Heap) 이용

## 큐의 활용
### 버퍼
데이터를 한 곳에서 다른 한 곳으로 전송하는 동안 일시적으로 그 데이터를 보관하는 메모리영역
- 버퍼를 채우는 것을 버퍼링

버퍼의 자료구조
- 버퍼는 일반적으로 입출력 및 네트워크와 관련된 기능에서 이용됨
- 순서대로 입력/출력/전달되어야 하므로 FIFO 방식의 자료구조인 큐가 어울림  

버퍼의 예시 : 키보드 버퍼
- 사용자 키보드 입력 -> 키보드입력 버퍼를 거쳐서 명령 실행하는 구조

큐를 이용한 마이쮸 나눠주기 시뮬레이션
- 시뮬레이션 구현 : 1번이 줄서고, 다시줄섬. 2번들어와 새로 줄섬 
  - 매 턴마다 새로운 사람이 들어오고 1번이 받는 마이쮸의 양이 1씩 늘어난다
- 엔터를 칠 때마다 다음 정보를 화면에 출력
  - 큐에 있는 사람 수
  - 현재 1인당 나눠주는 사탕의 수
  - 현재까지 나눠준 사탕의 수

====
# 알고리즘 Queue 2

## BFS
너비 우선 탐색(Breath First Search)  
- 탐색시작 정점에 인접한 정점들을 모두 차례로 방문한 후에, 방문했던 정점을 시작점으로 하여 다시 인접한 정점들을 차례로 방문하는 방식

**문제풀이 1 할 수 있는 만큼 풀어보고 강의 보면서 부족부분 채우기**
      **Adv 1, 2번문제 핵심로직과 개념정리**

