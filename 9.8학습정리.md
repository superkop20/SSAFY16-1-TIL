# 알고리즘

## Stack
### Stack의 개념과 특성
스택 : 물건을 쌓아올리듯 자료를 쌓아올린 형태의 자료구조
- 특성 - 후입선출(LIFO) : 가장 먼저 넣은 자료가 가장 먼저 나오는 형태

### Stack의 기본연산
스택 구현
- 배열을 사용한 구현
  - 파이썬 : 리스트 사용 구현
- 저장소 자체를 스택으로 부르기도 함
- 스택에서 마지막 삽입된 원소의 위치  
  - 스택 포인터

스택의 연산
- 삽입(push) - 저장소에 자료를 저장하는 연산
- 삭제(pop)
  - 삽입한 자료의 역순으로 꺼내는 연산(후입선출)
- 스택이 공백인지 아닌지(isEmpty)
- 스택의 top에 있는 item(원소)을 반환하는 연산(peek)

Push 연산(연산 내용은 교재와 실습파일 참고)
- append 메서드를 통해 리스트의 마지막에 데이터를 삽입
- 인덱스 연산을 통해 추가/초과 판별하여 정리  
단순한 Push연산
- 크기가 정해진 리스트와 인덱스 연산 활용
  - 사전에 size 지정

Pop 연산
- 남은 데이터 중 가장 늦게 저장된 데이터 삭제  
인덱스 연산을 이용한 Pop연산
- 크기가 정해진 리스트와 인덱스 활용
* pop연산해도 그 자리가 이미 있으면 덮어씌워짐 - 값 삭제를 따로 하지 않음
  - Top 만 움직여서 인덱싱하는 개념

스택 구현 시 고려사항
- 1차원 배열 사용 구현
  - 장점 : 구현 용이
  - 단점 : 스택크기 변경 어려움
- 해결방법 : 저장소를 동적으로 할당하여 스택 구현(동적연결리스트 이용)
* 연결리스트 : 데이터와 다음 노드의 주소를 함께 저장하여 요소들이 순차적으로 연결된 자료구조
  - 장점 : 메모리 효율적 사용
  - 단점 : 구현 복잡

### Stack의 응용
#### 괄호 검사
스택을 이용한 괄호 검사
- if(  (  i==0  ) && (  j==0  )) - 괄호가 더 많은경우 오류- 괄호 잔존

### Function call
프로그램에서의 함수 호출과 복귀에 따른 수행 순서를 관리  
- 가장 마지막 호출된 함수가 가장 먼저 실행 완료하고 복귀하는 후입선출 구조
- 후입선출 구조의 스택을 이용하여 수행 순서 관리

시스템 스택
- 함수 수행에 필요한 지역변수, 매개변수 및 수행 후 복귀할 주소 등의 정보를 저장
- 함수 호출이 발생하면 스택 프레임에 저장하여 시스템 스택에 삽입합니다.
  * 스택프레임이 분리되어 있어 함수 내부의 인자들에 대해 스택구조는 독립적으로 축적

Function call 
- 함수의 실행이 끝나면 시스템 스택의 top원소 삭제, 저장된 복귀주소 확인 후 복귀
- 함수 호출과 복귀에 따라 과정 반복해 전체프로그램 수행 졸요되면 시스템 스택은 공백 스택

**04_call_stack 스스로점검하기 풀어볼 것** (추후에도 또 쓰이는 함수호출)

*Alt + J (Mac: Ctrl + G)를 누를 때마다 다음 위치의 동일한 stack 단어가 하나씩 추가 선택됩니다.*