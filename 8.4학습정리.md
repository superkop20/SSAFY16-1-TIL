# AI 파이썬 1

## API, HTTP, request기초
API의 동작방식은 식당의 주문과정:짜장면 조리과정 필요x 요리만 제공
클라이언트는 API를통해 서버내부정보없이, 요청을 보내고 응답을 받을 수있음
request - 요청 , response - 응답
값 전달 : parameter
- HTTP 메서드
http 요청은 목적에따라 서로 다른 메서드 사용
get, post, *put, patch, delete, options* (비동기통신)
- 상태 코드 : 요청 처리 결과를 숫자로 나타낸 값
200 : Ok(성공), 201 : created(P) 400번대: 오류메세지
- request 라이브러리
http 요청 보내고 응답받을때 사용하는 외부 라이브러리
request.get(URL) 지정한 URL에 Get요청을 보내고 응답객체 반환

### JSON 과 LLM
- Json 파싱 == dict 값 추출과 같은 원리
Json 데이터접근 - 키로 한칸씩 들어가기
기본 URL내부 구성은 txt 문자열구성
.json() 명령 -> 글자를 파이썬 자료형으로 변환(list/dict)
- LLM 호출
챗봇의 답 만들기 구조 : API통해 서버 요청 및 응답
- API 키 보안 유지 
OS 환경변수 {로컬} , (.env) 환경변수 텍스트{다양환경}
GPT -> response API -> create(파이썬 코드 제공)
유니코드 - pprint 하면 해석해서 출력
* TODO 13~16 부분 영상과 대조하여 한번더 보기

## 참고(클라이언트와 서버)
API - application programming interface
두 소프트웨어(시스템)이 서로 통신할 수 있게하는 매커니즘
연동과 연결에 대한 구조적 정의
API 키 : client에게 API 키 발급 -> 요청 마다 APIkey를 함께 보냄
서버는 key 일치 여부 확인 후 응답 제공
* 보안을 강화, 호출횟수/사용량 모니터링
- request 예시(request_guide 폴더)
파이썬 변수가 대문자 : 상수형
pip install requests 후 import를 통해 모듈 활용
request, response 관련 2번 가이드 파일 확인
**.text   : 받은 그대로의 문자열 (str)**
**.json() : 그 문자열을 파이썬 자료형(list / dict) 으로 '번역' 한 결과**
* 리스트 컴프리헨션, 딕셔너리 컴프리헨션 개념 완벽이해

=====================
## 데이터 다루기
**numpy_pandas_guide 참조**

### Numpy(1차원 숫자 배열)
==숫자 데이터가 넘어옴, 어떤식으로 계산하는지==
- 엑셀 수작업 : 시간과 비효율 증가
- 데이터 처리 : 간단한 코드로 데이터 정렬과 집계 가능
넘파이와 판다스 : pandas - 표형태의 데이터
넘파이 배열 : 숫자데이터 다루는 넘파이 자료구조
- 리스트(pandas)와 Numpy배열
파이썬 리스트 - 연산같은걸 하기에 번거롭고 제약이 많음.
- Numpy배열의 특징
배열 : *같은 자료형의 데이터*를 담는 다차원 자료구조
벡터화 : 반복문 직접작성 x 배열전체에 연산 적용  
연산속도 - 벡터화 시 좀 더 빠르다
집계함수 사용하여 배열의 값 계산
0차원 = 스칼라(값) 1차원 = 벡터(줄) 2차원 = 매트릭스(표) 다차원 = 텐서(중첩)- 2차이상
Numpy : 벡터화 연산으로 처리하여 빠르다(컴마 없이 리스트 정리)
ndarray : 하나의 데이터 타입으로 일원화 (파이썬 .0은 생략가능 실수형 시 .만 출력)
- Numpy 배열생성
np.array(리스트) : 리스트를 넘파이배열 변환
np.arange(n) : 0~n-1 까지 정수로 배열 생성
np.zeros(n), np.ones(n) : 0또는 1로 채운 배열 생성
np.linspace(a,b,n) : a부터 b까지 같은 간격의 값 n개 생성
.shape : 배열의 각 차원 크기를 확인
* ndim (Number of Dimensions): 배열이 "몇 차원인가?"를 나타내는 정수
shape (형상/모양): 배열의 "각 차원마다 요소가 몇 개씩 들어있는가?"를 나타내는 튜플
- 벡터 연산
배열과 숫자의 연산은 배열의 모든 원소에 적용
크기가 같은 배열 연산 - 같은 위치의 원소를 계산
전치행렬 : array.T == array.transpose() 행과 열 전환
* reshape와 축 연산 : 원소수 유지하면서 배열의 형태를 변경
-1 : 전체 원소 수에 맞춰 해당 차원의 크기를 계산
- 집계함수{.mean, .sum,.max,.min,.std(표준편차)}
- 브로드캐스팅 : 크기가 다른 배열을 연산할 수 있도록 작은 배열을 확장하는 방식 # 방송하려면 데이터 송출, 채널 동기화라고 생각
1차원, 스칼라 : 스칼라를 배열의 모든 원소에 적용
1차원, 2차원배열 연산조건에 맞는다 : 실질적으로 값의 위치가 일치
{행이나 열이 확장했을 때 형태를 맞출 수 있는가}
* 참고 : 배열곱셈, 조건부 필터링[&(교집합), |(합집합)]

### Pandas(python data analysis)[데이터 행렬]
- Numpy처리의 문제점(Pandas 사용 이유)
여러 타입이 섞여 있을 경우 전부 문자열로 됨 ::> 나이의 평균 구하기 힘듬
열 이름이 없음 ::> 요금을 알아 보려면 data[:, 4] ::> 4번째 인덱스가 요금을 나타내는지 매번 확인 필요
결측치를 처리할 방법이 없음 ::> 나이가 비어 있는 데이터의 경우 어떻게 표현?
DataFrame : 행과 열로 구성된 2차원 표(열마다 다른 자료형 사용가능)
Series : dataframe의 한 열을 구성하는 1차원 자료구조
csv : Comma-Separated Values/ read_csv를 통해 읽기 로딩
EDA : 어떤 데이터를 뚫어져라보고 감 잡기
- 각각 언제 사용?
Pandas : 데이터를 읽고, 정리하고, 탐색하는 단계(전처리)
Numpy : 정리가 끝난 순수 숫자 행렬을 계산하는 단계(학습)
pandas : "이름표 달린 numpy" +"엑셀"
인덱스(index) : 가로 행 이름, 컬럼(column) : 세로 열 이름
Dataframe : 조회, 집계, 필터, 분석과정
- 각 메서드 확인
and, or 대신 &, | 사용 연산자 우선순위 고려 괄호 활용
- loc 라벨 인덱싱(.loc[행조건, 열이름])
행의 조건과 열 이름으로 데이터를 선택(추출 동작)
조건으로 고르는건 loc[조건, 열 이름]만 가능
* iloc : 행 인덱스번호로 추출
- groupby("컬럼") 집계 : 같은 값을 가진 행을 그룹으로 묶음
그룹별로 집계함수 적용 / 피벗테이블과 비슷한 방식으로 요약
다중 집계 : 두개 이상 컬럼 기준 그룹생성
agg - 컬럼마다 다른 집계함수, reset_index : 기준인덱스 일반컬럼변환
- pivot_table : 행과 열의 두 기준으로 데이터를 교차 집계
축구처럼 기준을 하나 잡고 고정으로 다른각도 데이터 관찰
핵심은 다각화 : 그래서 행, 열 쌍을 기준으로 돌림
- groupby 주의사항 *NaN(Not a Number): 결측값 표현식

* AI 과목평가 : 교수님들 이론강의 내용 위주
     월말평가 : 실습 내용 위주
이론 : 평가를 준비한다는 생각. 키워드 정리
실습 : 실제 개발에서는 AI를 이렇게 활용하는구나(기법, 활용 부분)
